<!-- Generated: 2026-01-24 15:45:00 KST -->

# 010 - 스테이징 서버 접속 정보 (sunjin-erp)

## 문서 목적

스테이징 서버(192.168.75.194)에서 sunjin-erp가 사용하는 접속 정보를 정리합니다.
기존 시스템(en-zine, zine-alpaca, Artisan OCR)과의 자원 공유 구분을 명확히 합니다.

---

## 서버 SSH 접속 (공유)

```bash
# 서버 정보
호스트: 192.168.75.194 (Oracle Linux 9)
사용자: pro301 / 비밀번호: 1234
Root: root / 비밀번호: fc866383

# SSH 접속
ssh pro301@192.168.75.194

# sunjin-erp 작업 디렉토리
/home/pro301/sunjin-erp
```

> **참고**: SSH 접속은 기존 시스템과 동일한 서버 계정을 공유합니다.
> 작업 디렉토리는 `/home/pro301/sunjin-erp`로 기존 `/home/pro301/git/en-zine`과 분리합니다.

---

## 자원 분류 요약

| 구분 | 자원 | sunjin-erp 접속 정보 |
|------|------|---------------------|
| **공유** | Oracle DB 인스턴스 | 192.168.75.194:1521 (별도 스키마) |
| **공유** | SSH 서버 접속 | pro301@192.168.75.194 |
| **공유** | Docker Engine | 동일 데몬, 별도 네트워크 |
| **공유** | Prometheus | 192.168.75.194:9090 (별도 job) |
| **공유** | Grafana | 192.168.75.194:3100 (별도 대시보드) |
| **독립** | sunjin-erp App | 192.168.75.194:3200 |
| **독립** | Oracle 스키마 | sunjin_admin (격리) |
| **독립** | Docker Network | sunjin-network (172.21.0.0/16) |
| **사용불가** | Redis | 기존 시스템 전용 |
| **사용불가** | Artisan OCR | 기존 시스템 전용 |
| **사용불가** | zine-alpaca | 기존 시스템 전용 |

---

## 공유 자원 접속 정보

### Oracle Database (공유 인스턴스, 별도 스키마)

```bash
# sunjin-erp 전용 접속 정보
호스트: 192.168.75.194
포트: 1521
서비스명: XEPDB1
스키마: sunjin_admin
계정: sunjin_admin / <sunjin-erp-password>
컨테이너: zine-oracle-xe (기존 컨테이너 공유)
볼륨: oracle-staging-direct-data (공유 볼륨)

# SQLPlus 접속 (외부)
sqlplus sunjin_admin/<password>@192.168.75.194:1521/XEPDB1

# SQLPlus 접속 (서버 내부)
docker exec -it zine-oracle-xe sqlplus sunjin_admin/<password>@XEPDB1

# 테이블 목록 확인
docker exec -it zine-oracle-xe bash -c "echo 'SELECT table_name FROM user_tables;' | sqlplus -s sunjin_admin/<password>@XEPDB1"

# 웹 관리 도구
http://192.168.75.194:5500/em
```

#### sunjin_admin 사용자 생성 (초기 1회)

```sql
-- SYS 권한으로 실행
CREATE USER sunjin_admin IDENTIFIED BY <sunjin-erp-password>
  DEFAULT TABLESPACE USERS
  TEMPORARY TABLESPACE TEMP
  QUOTA UNLIMITED ON USERS;

GRANT CONNECT, RESOURCE TO sunjin_admin;
GRANT CREATE SESSION TO sunjin_admin;
GRANT CREATE TABLE TO sunjin_admin;
GRANT CREATE SEQUENCE TO sunjin_admin;
GRANT CREATE VIEW TO sunjin_admin;
GRANT CREATE PROCEDURE TO sunjin_admin;
```

#### 접근 권한 정책

| 스키마 | sunjin-erp 접근 | 비고 |
|--------|----------------|------|
| sunjin_admin | READ/WRITE | 전체 권한 |
| ocr_admin | 접근 불가 | 기존 시스템 전용 |

### Prometheus (공유 인스턴스, 별도 job)

```bash
URL: http://192.168.75.194:9090
Health: http://192.168.75.194:9090/-/healthy
컨테이너: prometheus-staging-direct

# sunjin-erp 메트릭 쿼리 예시
# PromQL: {job="sunjin-erp"}
```

### Grafana (공유 인스턴스, 별도 대시보드)

```bash
URL: http://192.168.75.194:3100
계정: admin / staging_admin_password
Health: http://192.168.75.194:3100/api/health
컨테이너: grafana-staging-direct

# sunjin-erp 전용 대시보드 폴더 생성 필요
```

---

## sunjin-erp 독립 자원 접속 정보

### sunjin-erp Application (Next.js)

```bash
# 기본 정보
URL: http://192.168.75.194:3200
컨테이너: sunjin-erp-app
포트: 3200 (외부) → 3000 (내부)
네트워크: sunjin-network (172.21.0.0/16)

# 헬스체크
curl http://192.168.75.194:3200

# Docker 로그 확인
docker logs -f sunjin-erp-app
```

### Docker 네트워크

```bash
# sunjin-erp 전용 네트워크
네트워크명: sunjin-network
서브넷: 172.21.0.0/16

# 네트워크 생성
docker network create --driver bridge --subnet 172.21.0.0/16 sunjin-network

# 네트워크 확인
docker network inspect sunjin-network
```

### 파일 스토리지

```bash
# 업로드 디렉토리
서버 경로: /home/pro301/sunjin-erp/uploads
컨테이너 경로: /app/uploads
볼륨명: sunjin-erp-uploads
```

---

## 기존 시스템 전용 (사용 불가)

아래 자원은 기존 시스템(en-zine, zine-alpaca) 전용입니다. sunjin-erp에서 접근하지 마세요.

| 서비스 | 포트 | 비고 |
|--------|------|------|
| zine-alpaca Frontend | 3000 | React 앱 |
| zine-alpaca Backend | 3001 | Node.js API |
| Redis | 6379 | 캐시/세션/큐 |
| Artisan WebSocket | 8081 | OCR WebSocket |
| Artisan HTTP | 8082 | OCR HTTP |
| Artisan REST API | 8083 | OCR REST |
| WS Metrics | 9091 | Artisan 메트릭 |
| API Metrics | 9092 | Artisan 메트릭 |

---

## 환경 변수 (sunjin-erp 전용)

### .env 파일

```bash
# Database (공유 Oracle 인스턴스, 별도 스키마)
ORACLE_HOST=192.168.75.194
ORACLE_PORT=1521
ORACLE_SERVICE_NAME=XEPDB1
ORACLE_USERNAME=sunjin_admin
ORACLE_PASSWORD=<sunjin-erp-password>

# NextAuth
NEXTAUTH_SECRET=<32+ character random string>
NEXTAUTH_URL=http://192.168.75.194:3200

# File Storage
UPLOAD_DIR=/app/uploads
```

### Secret 생성

```bash
# NEXTAUTH_SECRET 생성 (32+ characters)
openssl rand -base64 32

# .env 파일 권한 설정
chmod 600 .env
```

---

## 포트 할당 전체 현황

| 포트 | 서비스 | 소속 | sunjin-erp 사용 |
|------|--------|------|----------------|
| 1521 | Oracle XE | 공유 | O (별도 스키마) |
| 5500 | Oracle EM | 공유 | O (관리 콘솔) |
| 3000 | zine-alpaca Frontend | 기존 | X |
| 3001 | zine-alpaca Backend | 기존 | X |
| 3100 | Grafana | 공유 | O (별도 대시보드) |
| 3101 | Loki | 공유 | O (별도 label) |
| **3200** | **sunjin-erp App** | **신규** | **O (전용)** |
| 6379 | Redis | 기존 | X |
| 8081 | Artisan WebSocket | 기존 | X |
| 8082 | Artisan HTTP | 기존 | X |
| 8083 | Artisan REST API | 기존 | X |
| 9090 | Prometheus | 공유 | O (별도 job) |
| 9091 | WS Metrics | 기존 | X |
| 9092 | API Metrics | 기존 | X |

---

## 배포 워크플로우 (sunjin-erp)

```
로컬 (Mac)                        서버 (Oracle Linux 9)
─────────────                     ───────────────────────
1. 코드 수정                       4. Docker 이미지 빌드
   ↓                                  docker compose build
2. Git Commit & Push               5. 서비스 기동
   git push origin main               docker compose up -d
   ↓                                  ↓
3. 서버 Git Pull                   6. 확인
   ssh + git pull                     curl http://192.168.75.194:3200
```

---

## 주의사항

1. **ocr_admin 스키마 접근 금지** — 기존 시스템 데이터 보호
2. **zine-network 연결 금지** — 네트워크 격리 유지
3. **포트 3000, 3001 사용 금지** — 기존 서비스 충돌 방지
4. **Redis 사용 금지** — sunjin-erp에서 캐시가 필요할 경우 별도 인스턴스 구성
5. **GPU 사용 안 함** — Artisan OCR 전용 자원

---

**마지막 업데이트**: 2026-01-24 15:45:00 KST
