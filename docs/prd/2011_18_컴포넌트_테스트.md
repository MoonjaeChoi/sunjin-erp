<!-- Generated: 2026-01-24 22:50:00 KST -->

# 컴포넌트 테스트

**문서 번호**: 2011_18
**원본 PRD**: 2011_대시보드_및_일정관리_prd_v2.md
**PRD 참조**: 전체 PRD UI/UX 섹션 참조
**구현 범위**: React Testing Library 기반 캘린더 뷰 + TaskForm 컴포넌트 테스트
**복잡도**: M
**의존성**: 2011_11~16

---

## 구현 목표

프론트엔드 컴포넌트(Calendar Views, TaskForm, DayDetailPanel, TeamCalendar)에 대한 통합 컴포넌트 테스트를 작성한다. React Testing Library + user-event를 사용한다.

---

## 구현 내용

### 파일 구조

```
src/
└── __tests__/
    └── components/
        └── features/
            └── dashboard/
                ├── Calendar.test.tsx
                ├── CalendarHeader.test.tsx
                ├── MonthView.test.tsx
                ├── WeekView.test.tsx
                ├── DayView.test.tsx
                ├── DayDetailPanel.test.tsx
                ├── TaskForm.test.tsx
                └── TeamCalendar.test.tsx
```

### 테스트 범위

**1. Calendar.test.tsx**
```typescript
describe('Calendar', () => {
  it('should render MonthView by default (no view param)');
  it('should render WeekView when view=week');
  it('should render DayView when view=day');
  it('should pass date from URL to child views');
  it('should render DayDetailPanel when selectedDate is set');
});
```

**2. CalendarHeader.test.tsx**
```typescript
describe('CalendarHeader', () => {
  it('should display current month in Korean ("2026년 1월")');
  it('should call onViewChange when tab is clicked');
  it('should call onDateChange with next month on ">" click');
  it('should call onDateChange with prev month on "<" click');
  it('should call onDateChange with today on "오늘" click');
  it('should highlight active view tab');
});
```

**3. MonthView.test.tsx**
```typescript
describe('MonthView', () => {
  it('should render 7 day-of-week headers (월~일)');
  it('should render correct number of day cells (42)');
  it('should highlight today with blue circle');
  it('should dim days outside current month');
  it('should display office task count in blue badge');
  it('should display field task count in orange badge');
  it('should show "+N more" when tasks > 5');
  it('should call setSelectedDate on cell click');
  it('should call openTaskForm on cell double click');
});
```

**4. TaskForm.test.tsx**
```typescript
describe('TaskForm', () => {
  describe('Create mode', () => {
    it('should render empty form');
    it('should require title field');
    it('should require task_date field');
    it('should require task_type select');
    it('should require work_type select');
    it('should validate start_time < end_time');
    it('should show time overlap warning');
    it('should call createMutation on submit');
    it('should disable submit when isPending');
    it('should close form on success');
  });

  describe('Edit mode', () => {
    it('should populate form with existing task data');
    it('should call updateMutation on submit');
    it('should show delete button');
    it('should show confirmation dialog on delete');
    it('should show extra warning for DONE task delete');
  });

  describe('Responsive', () => {
    it('should render as Dialog on desktop');
    it('should render as Sheet on mobile');
  });
});
```

**5. DayDetailPanel.test.tsx**
```typescript
describe('DayDetailPanel', () => {
  it('should display date in Korean format');
  it('should render task list with status badges');
  it('should show tech support section (empty)');
  it('should close panel on X button click');
  it('should allow status change via dropdown');
  it('should disable status change when mutation isPending');
});
```

**6. TeamCalendar.test.tsx**
```typescript
describe('TeamCalendar', () => {
  it('should show mobile-only message under 768px');
  it('should render employee filter select');
  it('should filter calendar by selected employee');
  it('should apply color palette per employee');
  it('should show loading state');
});
```

### 테스트 유틸리티

```typescript
// test-utils.tsx
import { render } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

function createTestQueryClient() {
  return new QueryClient({
    defaultOptions: { queries: { retry: false } },
  });
}

export function renderWithProviders(ui: React.ReactElement) {
  const queryClient = createTestQueryClient();
  return render(
    <QueryClientProvider client={queryClient}>
      {ui}
    </QueryClientProvider>
  );
}
```

### 테스트 원칙

- ✅ React Testing Library 쿼리 우선순위: `getByRole` > `getByLabelText` > `getByText` > `getByTestId`
- ✅ `userEvent` 사용 (fireEvent 금지)
- ✅ `waitFor` / `findBy*` 비동기 대기
- ✅ Mock: useSearchParams, useRouter, fetch, session

---

## Acceptance Criteria

- [ ] 8개 컴포넌트 테스트 파일 생성
- [ ] `npm run test` 전체 통과
- [ ] React Testing Library 쿼리 우선순위 준수
- [ ] userEvent 사용 (fireEvent 미사용)
- [ ] 라인 커버리지 80%+
- [ ] 인터랙션 테스트 (클릭, 입력, 제출)
- [ ] 상태 변경 테스트 (Zustand, URL)

---

## 완료 체크리스트

- [ ] `npm run test` 전체 통과
- [ ] 컴포넌트별 주요 시나리오 커버
- [ ] Accessibility 관련 쿼리 사용 (getByRole)
- [ ] 비동기 테스트 패턴 적용

---

**다음 문서**: 2011_19_E2E_테스트.md
