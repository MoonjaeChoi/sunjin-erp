<!-- Generated: 2026-01-25 03:00:00 KST -->

# 검색 페이지 컴포넌트 (SC + TaskSearchClient)

**문서 번호**: 2021_04
**원본 PRD**: 2021_업무_검색_prd_v2.md
**PRD 참조**: 원본 PRD의 'Section 5.1 Next.js App Router Structure' 및 'Section 5.4 State Management' 참조
**구현 범위**: 검색 페이지 SC + TaskSearchClient CC (URL 상태 관리, 컴포넌트 조합)
**복잡도**: M
**의존성**: 2021_03

---

## 구현 목표

업무 검색 페이지의 Server Component와 메인 Client Component를 구현한다. SC는 searchParams를 전달하고, CC(TaskSearchClient)는 URL 상태를 Single Source of Truth로 사용하여 필터, 테이블, Dialog를 조합한다.

---

## 구현 내용

### 파일 구조

```
src/app/(main)/tasks/
└── page.tsx                            # Server Component (기존 placeholder 교체)
src/components/features/tasks/
└── TaskSearchClient.tsx                # 'use client' - 메인 컨테이너
```

### 구현 상세

#### 1. page.tsx (Server Component)

- Next.js App Router의 `searchParams` prop을 받아 TaskSearchClient에 전달
- metadata export로 페이지 타이틀 설정

```typescript
import { Metadata } from 'next';
import { TaskSearchClient } from '@/components/features/tasks/TaskSearchClient';

export const metadata: Metadata = {
  title: '업무 검색',
};

export default function TasksPage() {
  return <TaskSearchClient />;
}
```

#### 2. TaskSearchClient (Client Component)

**핵심 책임:**
- `useSearchParams()` + `useRouter()`로 URL 상태 관리
- debounce된 필터 값을 URL param으로 반영
- URL params에서 `TaskSearchParams`를 파싱하여 `useTaskSearchQuery`에 전달
- 초기 로드 시 기본 날짜 범위(현재 월)로 자동 검색
- `detail` param으로 Dialog 열림 상태 관리

**URL → State 동기화 플로우:**
```
URL params → parseSearchParams() → TaskSearchParams → useTaskSearchQuery()
                                                            ↓
필터 변경 → debounce → updateURL() → URL 변경 → 자동 refetch
```

**기본 날짜 범위 계산:**
- `date_from`: 현재 월 1일 (YYYY-MM-01)
- `date_to`: 현재 월 말일

**URL param 업데이트 규칙:**
- 빈 값/기본값은 URL에서 제거 (깔끔한 URL 유지)
- `page`는 필터 변경 시 1로 리셋
- `detail`은 Dialog 열림/닫힘 시 설정/제거

### 핵심 인터페이스

```typescript
'use client';

import { useSearchParams, useRouter, usePathname } from 'next/navigation';
import { useCallback, useMemo } from 'react';
import { TaskSearchFilters } from './TaskSearchFilters';
import { TaskDataTable } from './TaskDataTable';
import { TaskDetailDialog } from './TaskDetailDialog';
import { useTaskSearchQuery } from '@/hooks/tasks';
import { TaskSearchParams, DEFAULT_PAGE_SIZE, DEFAULT_SORT_BY, DEFAULT_SORT_ORDER } from '@/types/task-search';

export function TaskSearchClient() {
  const searchParams = useSearchParams();
  const router = useRouter();
  const pathname = usePathname();

  // URL params → TaskSearchParams 파싱
  const params: TaskSearchParams = useMemo(() => {
    const now = new Date();
    const defaultFrom = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-01`;
    const lastDay = new Date(now.getFullYear(), now.getMonth() + 1, 0).getDate();
    const defaultTo = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${lastDay}`;

    return {
      date_from: searchParams.get('date_from') || defaultFrom,
      date_to: searchParams.get('date_to') || defaultTo,
      type: searchParams.get('type') || undefined,
      work_type: searchParams.get('work_type') || undefined,
      status: searchParams.get('status') || undefined,
      keyword: searchParams.get('keyword') || undefined,
      page: Number(searchParams.get('page')) || 1,
      page_size: Number(searchParams.get('page_size')) || DEFAULT_PAGE_SIZE,
      sort_by: searchParams.get('sort_by') || DEFAULT_SORT_BY,
      sort_order: searchParams.get('sort_order') || DEFAULT_SORT_ORDER,
    };
  }, [searchParams]);

  // URL 업데이트 함수
  const updateURL = useCallback((updates: Partial<TaskSearchParams>) => {
    const newParams = new URLSearchParams(searchParams.toString());
    // ... param 업데이트 로직
    router.replace(`${pathname}?${newParams.toString()}`, { scroll: false });
  }, [searchParams, router, pathname]);

  // Dialog 상태
  const detailId = searchParams.get('detail') ? Number(searchParams.get('detail')) : null;

  // TanStack Query
  const { data, isLoading, isPlaceholderData } = useTaskSearchQuery(params);

  return (
    <div className="space-y-4">
      {/* 헤더 */}
      <div className="flex items-center justify-between">
        <h1 className="text-xl font-semibold">업무 검색</h1>
      </div>

      {/* 필터 */}
      <TaskSearchFilters params={params} onUpdate={updateURL} />

      {/* 테이블 */}
      <TaskDataTable
        data={data}
        isLoading={isLoading}
        isPlaceholderData={isPlaceholderData}
        params={params}
        onUpdate={updateURL}
      />

      {/* 상세 Dialog */}
      <TaskDetailDialog
        taskId={detailId}
        onClose={() => updateURL({ detail: undefined })}
      />
    </div>
  );
}
```

---

## Acceptance Criteria

- [ ] 페이지 최초 접근 시 현재 월 범위로 자동 검색
- [ ] URL params가 있으면 해당 조건으로 검색
- [ ] 브라우저 뒤로가기 시 이전 검색 조건 복원
- [ ] 필터 변경 시 page가 1로 리셋
- [ ] `detail` param 존재 시 Dialog 자동 열림
- [ ] 기본값은 URL에 표시되지 않음 (깔끔한 URL)
- [ ] SC/CC 분리 패턴 준수 (page.tsx는 SC)
- [ ] 날짜 범위 365일 초과 시 에러 메시지 표시

---

## 테스트 전략

### TypeScript & Lint

```bash
npm run build
npm run type-check
npm run lint
```

### 단위 테스트

**테스트 파일 위치**: `src/__tests__/components/features/tasks/TaskSearchClient.test.tsx`

```typescript
describe('TaskSearchClient', () => {
  it('should render filters, table, and dialog', () => {});
  it('should parse URL params into search params', () => {});
  it('should use default date range when no URL params', () => {});
  it('should reset page to 1 on filter change', () => {});
  it('should open dialog when detail param exists', () => {});
});
```

### 검증 방법

1. `npm run build` 성공
2. `npm run dev` → `/tasks` 접근 시 자동 검색 동작
3. URL 직접 입력 시 해당 조건으로 검색
4. 브라우저 뒤로가기/앞으로가기 동작

---

## 완료 체크리스트

- [ ] TypeScript 빌드 성공
- [ ] ESLint/Prettier 통과
- [ ] SC/CC 분리 준수
- [ ] URL 상태 동기화 동작
- [ ] 기본 날짜 범위 자동 설정
- [ ] 스테이징 서버 검증

---

**다음 문서**: 2021_05_TaskSearchFilters_컴포넌트.md
