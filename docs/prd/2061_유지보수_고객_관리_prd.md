<!-- Generated: 2026-01-25 22:00:00 KST -->

# Product Requirement Document: 유지보수 고객 관리

문서번호: 2061
작성일: 2026-01-25
상위문서: 1000_초안.prd, 2001_전체_레이아웃_설정_prd_v2.md

## 1. Overview

본 PRD는 sunjin-erp 시스템의 "유지보수 고객 관리" 기능에 대한 요구사항을 정의한다.
이 기능은 유지보수 계약이 체결된 고객사의 계약 정보, 담당 영업 사원 할당, 유지보수 계약서 파일 관리를 통해 계약 생명주기를 효율적으로 관리하는 시스템을 구축한다.

**대상 모듈:** 유지보수 고객 관리 (Maintenance Customer Management)
**대상 사용자 역할:** USER / MANAGER / ADMIN
**주요 기능:** 유지보수 계약 조회/생성/수정/삭제, 계약서 파일 첨부, 담당자 배정, 계약 상태 관리

## 2. Goals & Objectives

*   **계약 생명주기 관리:** 유지보수 계약 체결부터 만료까지의 전체 라이프사이클을 관리하여 계약 현황을 명확히 한다.
*   **담당자 할당 관리:** 계약별 영업 담당자를 명확하게 지정하여 책임 소재를 명확히 하고 커뮤니케이션을 원활하게 한다.
*   **계약서 문서 관리:** 계약서(PDF, Word 등) 파일을 중앙 집중식으로 관리하여 필요 시 신속한 문서 검색 및 조회를 가능하게 한다.
*   **계약 현황 시각화:** 계약 상태(활성/종료/갱신 예정) 기반으로 한눈에 계약 현황을 파악할 수 있도록 대시보드 제공.
*   **계약 갱신 추적:** 계약 종료일 임박 알림, 갱신 이력 기록으로 계약 중단 방지 및 사업 기회 확보.

## 3. User Stories / Use Cases

*   **User Story 1: 유지보수 계약 목록 조회**
    - **역할:** USER / MANAGER / ADMIN
    - **설명:** 사용자로서, 현재 체결된 유지보수 계약 목록을 조회하고 싶다. 그래서 어떤 고객사와 계약이 진행 중인지 파악할 수 있다.
    - *Acceptance Criteria:*
        - 계약 목록 페이지에서 모든 활성 유지보수 계약이 테이블 형식으로 표시된다
        - 각 행에 고객사명, 계약명, 계약 기간(시작/종료), 담당 영업자, 계약 상태가 표시된다
        - 기본 정렬: 계약 종료일(임박순)
        - 페이지네이션: 기본 20개 항목/페이지, 최대 100개 항목/페이지

*   **User Story 2: 필터링 및 검색**
    - **역할:** USER / MANAGER / ADMIN
    - **설명:** 사용자로서, 특정 고객사의 계약만 필터링하거나 계약명으로 검색하고 싶다. 그래서 원하는 계약을 빠르게 찾을 수 있다.
    - *Acceptance Criteria:*
        - 필터 조건: 계약 상태(활성/종료/갱신예정), 담당자(단일), 고객사(검색), 계약 기간(범위)
        - 검색: 계약명, 고객사명 (부분 검색, 대소문자 무시)
        - 필터 적용 시 URL query parameter 업데이트
        - 필터 리셋: "초기화" 버튼으로 모든 필터 제거 + 기본값 복원

*   **User Story 3: 계약 상세 조회**
    - **역할:** USER / MANAGER / ADMIN
    - **설명:** 사용자로서, 특정 계약의 상세 정보(계약 조건, 담당자, 첨부 문서)를 확인하고 싶다. 그래서 계약의 모든 세부사항을 파악할 수 있다.
    - *Acceptance Criteria:*
        - 계약 상세 페이지: 기본 정보 + 첨부 문서 + 갱신 이력
        - 기본 정보: ID, 고객사명, 계약명, 계약 기간(시작/종료), 계약 금액, 계약 상태, 담당 영업자, 계약 유형
        - 첨부 문서: 계약서 파일 목록 (파일명, 업로드일, 담당자)
        - 갱신 이력: 과거 계약 갱신 기록 (이전 계약 기간, 갱신일, 담당자)

*   **User Story 4: 유지보수 계약 신규 생성**
    - **역할:** MANAGER / ADMIN
    - **설명:** MANAGER/ADMIN으로서, 신규 유지보수 계약을 시스템에 등록하고 싶다. 그래서 새로운 고객사와의 계약을 관리 대상으로 추가할 수 있다.
    - *Acceptance Criteria:*
        - 신규 계약 폼: 고객사(선택/검색), 계약명, 시작일, 종료일, 계약 금액(선택), 담당 영업자, 계약 유형, 비고(선택)
        - 필드 검증: 고객사/계약명/기간/담당자 필수, 시작일 ≤ 종료일
        - 제출 후: 상태 자동 설정 "활성", 계약 생성 이력 자동 기록
        - 성공 메시지 + 상세 페이지로 자동 이동

*   **User Story 5: 계약 정보 수정**
    - **역할:** MANAGER / ADMIN
    - **설명:** MANAGER/ADMIN으로서, 기존 계약의 정보(담당자, 기간, 금액)를 수정하고 싶다. 그래서 변경된 계약 정보를 반영할 수 있다.
    - *Acceptance Criteria:*
        - 수정 폼: 계약명, 기간, 금액, 담당자, 유형 수정 가능
        - 고객사명은 수정 불가 (생성 후 고정)
        - 상태 변경: "활성" ↔ "종료", "갱신예정"으로 수정 가능
        - 변경사항 저장 시 업데이트 이력 자동 기록

*   **User Story 6: 계약서 파일 첨부**
    - **역할:** MANAGER / ADMIN
    - **설명:** MANAGER/ADMIN으로서, 유지보수 계약서(PDF, Word) 파일을 계약에 첨부하고 싶다. 그래서 계약 문서를 중앙에서 관리할 수 있다.
    - *Acceptance Criteria:*
        - 파일 업로드: 계약 상세 페이지에서 "파일 추가" 버튼으로 업로드
        - 허용 파일: PDF, DOCX, DOC (최대 10MB, 파일당 5개 한정)
        - 업로드 시: 파일명, 업로드자, 업로드 날짜 자동 기록
        - 첨부 파일 목록: 다운로드, 삭제(ADMIN만) 가능

*   **User Story 7: 계약 상태 관리**
    - **역할:** MANAGER / ADMIN
    - **설명:** MANAGER/ADMIN으로서, 계약의 상태를 변경(활성/종료/갱신예정)하고 싶다. 그래서 계약 라이프사이클을 명확히 관리할 수 있다.
    - *Acceptance Criteria:*
        - 상태 전이:
            - 활성 → 종료 (계약 만료 시)
            - 활성 → 갱신예정 (계약 만료 3개월 전)
            - 갱신예정 → 활성 (갱신 완료 시)
            - 종료 → 활성 (재계약 시)
        - 상태 변경 시: 사유 필수 기입, 변경자/변경일 자동 기록

*   **User Story 8: 계약 만료 알림**
    - **역할:** MANAGER / ADMIN
    - **설명:** MANAGER/ADMIN으로서, 계약 만료 예정인 계약을 시각적으로 식별하고 싶다. 그래서 갱신 기한을 놓치지 않을 수 있다.
    - *Acceptance Criteria:*
        - 목록 페이지에서 30일 내 종료 예정 계약은 경고 배지(Red) 표시
        - 60일 내 종료 예정 계약은 주의 배지(Yellow) 표시
        - 상세 페이지에서 남은 기간 카운트다운 표시
        - 필터: "30일 내 만료" 등의 빠른 필터 제공

*   **User Story 9: 계약 갱신 이력**
    - **역할:** MANAGER / ADMIN
    - **설명:** MANAGER/ADMIN으로서, 과거 계약 갱신 이력을 조회하고 싶다. 그래서 계약의 역사와 현황을 파악할 수 있다.
    - *Acceptance Criteria:*
        - 상세 페이지 "갱신 이력" 탭에서 과거 갱신 기록 표시
        - 각 이력: 이전 종료일, 새로운 종료일, 갱신일, 갱신자, 비고
        - 시간순 정렬 (최신순)

*   **User Story 10: 계약 삭제 (소프트 삭제)**
    - **역할:** ADMIN만
    - **설명:** ADMIN으로서, 더 이상 관리하지 않는 계약을 삭제하고 싶다. 그래서 불필요한 정보를 제거할 수 있다.
    - *Acceptance Criteria:*
        - 삭제 버튼: ADMIN만 표시
        - 삭제 시: 확인 다이얼로그 표시 (실수 방지)
        - 소프트 삭제: `deleted_at` 설정, 목록에서 제외
        - 삭제 이력 자동 기록

## 4. Scope

### 4.1 In-Scope

*   유지보수 계약 목록 조회 (필터링, 검색, 정렬, 페이지네이션)
*   계약 상세 조회 (기본 정보 + 첨부 문서 + 갱신 이력)
*   유지보수 계약 신규 생성
*   계약 정보 수정
*   계약 상태 변경 (활성/종료/갱신예정)
*   계약서 파일 첨부 (PDF, DOCX, DOC, 최대 5개)
*   첨부 파일 다운로드/삭제 (ADMIN만)
*   계약 갱신 이력 조회
*   계약 만료 알림 (30일/60일 경고 배지)
*   소프트 삭제 (ADMIN만)
*   권한 기반 접근 제어 (USER: 조회만, MANAGER: CRUD, ADMIN: 전체)
*   고객사 정보와의 연계 (Customer 테이블)
*   영업 담당자 정보 연계 (Employee 테이블)

### 4.2 Out-of-Scope

*   계약 승인 워크플로우 (2단계 구현)
*   자동 계약 갱신 (2단계)
*   계약 금액 기반 매출 인식 (회계 모듈과 연계)
*   계약 서명/전자 서명 (별도 전자계약 시스템)
*   계약 분석 대시보드 (고급 분석)
*   다국어 지원

## 5. Technical Considerations & Architecture Alignment

### 5.1 Next.js App Router Structure

*   **Page Routes:**
    - `src/app/(main)/maintenance/page.tsx` — 계약 목록 페이지 (Server Component)
    - `src/app/(main)/maintenance/[id]/page.tsx` — 계약 상세 페이지 (Server Component)
    - `src/app/(main)/maintenance/new/page.tsx` — 계약 등록 페이지 (Server Component + Client Form)

*   **Layout:** `src/app/(main)/layout.tsx`에서 공통 레이아웃 상속

*   **Server vs Client Components:**
    - `maintenance/page.tsx`: Server Component — 데이터 페칭, 권한 검증
    - `maintenance/[id]/page.tsx`: Server Component — 계약 + 첨부 + 이력 조회
    - 폼 컴포넌트 (`MaintenanceContractForm`, `StatusChangeForm` 등): Client Component — 폼 상태, 유효성 검증

### 5.2 API Route Handlers

*   `GET /api/maintenance` — 유지보수 계약 목록 조회 (필터, 정렬, 페이지네이션)
*   `POST /api/maintenance` — 신규 계약 생성
*   `GET /api/maintenance/[id]` — 계약 상세 조회
*   `PUT /api/maintenance/[id]` — 계약 정보 수정
*   `DELETE /api/maintenance/[id]` — 소프트 삭제 (ADMIN만)
*   `POST /api/maintenance/[id]/status` — 계약 상태 변경
*   `POST /api/maintenance/[id]/attachments` — 계약서 파일 업로드
*   `DELETE /api/maintenance/[id]/attachments/[attachmentId]` — 파일 삭제 (ADMIN만)
*   `GET /api/maintenance/[id]/history` — 갱신 이력 조회
*   `GET /api/maintenance/stats` — 계약 통계 (상태별 집계)

### 5.3 Database (Oracle XE 21c + TypeORM)

#### Entity 정의: `src/entities/MaintenanceContract.ts`

```
[MaintenanceContract]
├── id: number (PK)
├── customer_id: number (FK → Customer.id, ON DELETE RESTRICT)
├── contract_name: string (VARCHAR2) — 계약명
├── contract_type: string (VARCHAR2) — 계약 유형 (예: 유지보수, 보증, 기술지원)
├── start_date: Date — 계약 시작일
├── end_date: Date — 계약 종료일
├── contract_amount: number (NUMBER, nullable) — 계약 금액
├── assigned_employee_id: number (FK → Employee.id, ON DELETE RESTRICT) — 담당 영업자
├── contract_status: string (VARCHAR2) — 상태 (활성/종료/갱신예정)
├── notes: string (CLOB, nullable) — 비고
├── created_by_id: number (FK → Employee.id)
├── updated_by_id: number (FK → Employee.id)
├── created_at: DateTime — 생성일
├── updated_at: DateTime — 수정일
└── deleted_at: DateTime (nullable) — 소프트 삭제일
```

#### Entity 정의: `src/entities/MaintenanceContractAttachment.ts`

```
[MaintenanceContractAttachment]
├── id: number (PK)
├── maintenance_contract_id: number (FK → MaintenanceContract.id, ON DELETE RESTRICT)
├── file_name: string (VARCHAR2) — 파일명
├── file_path: string (VARCHAR2) — 파일 경로
├── file_size: number (NUMBER) — 파일 크기 (bytes)
├── uploaded_by_id: number (FK → Employee.id)
├── created_at: DateTime — 업로드 일시
└── deleted_at: DateTime (nullable) — 소프트 삭제일
```

#### Entity 정의: `src/entities/MaintenanceContractHistory.ts`

```
[MaintenanceContractHistory]
├── id: number (PK)
├── maintenance_contract_id: number (FK → MaintenanceContract.id, ON DELETE RESTRICT)
├── change_type: string (VARCHAR2) — 변경 유형 (갱신/상태변경/정보수정)
├── previous_end_date: Date (nullable) — 이전 종료일
├── new_end_date: Date (nullable) — 새 종료일
├── reason: string (VARCHAR2, nullable) — 사유
├── changed_by_id: number (FK → Employee.id)
├── changed_at: DateTime — 변경 일시
└── deleted_at: DateTime (nullable) — 소프트 삭제 (논리적 불사용)
```

#### Migration 파일

```
src/migrations/
├── 1706300000000-create-maintenance-contract-table.ts
├── 1706300000001-create-maintenance-contract-attachment-table.ts
└── 1706300000002-create-maintenance-contract-history-table.ts
```

#### Oracle 규칙 준수

*   VARCHAR2 사용 (VARCHAR 아님)
*   CLOB 사용 (TEXT 아님)
*   모든 FK: `ON DELETE RESTRICT`
*   `contract_status` 제약: CHECK 또는 애플리케이션 검증 (활성/종료/갱신예정)
*   `start_date` ≤ `end_date` 제약 (CHECK 또는 애플리케이션)
*   Soft delete 필수 (deleted_at 컬럼)

### 5.4 State Management

*   **Server State:** TanStack Query로 API 데이터 페칭/캐싱
    - `useMaintenanceContractList()` — 계약 목록, 필터 상태 관리
    - `useMaintenanceContractDetail()` — 계약 상세 + 첨부 + 이력
    - `useMaintenanceContractStats()` — 통계 데이터

*   **Client State:** Zustand 또는 React useState
    - 폼 입력 상태 (계약 등록/수정/상태변경 폼)
    - 필터 선택값 (상태, 담당자, 고객사)
    - 페이지네이션 상태 (현재 페이지, 페이지 크기)

*   **Form State:** React Hook Form
    - 계약 등록 폼, 계약 수정 폼, 상태변경 폼
    - 실시간 검증, 에러 메시지 표시

### 5.5 Authentication & Authorization

*   **NextAuth.js:** Session 기반 인증
*   **권한 검증:**
    - `USER`: 목록 조회, 상세 조회 읽기만 가능
    - `MANAGER`: 조회 + 계약 생성/수정/상태변경 + 파일 업로드 가능
    - `ADMIN`: 모든 기능 (+ 삭제/파일 삭제)
*   **API Route:** 각 핸들러에서 `getServerSession()` → `user.role` 검증
*   **Page Route:** 클라이언트에서 `useSession()` 후 권한에 따라 UI 렌더링 (수정/삭제 버튼 숨김 등)
*   **Server Component:** 권한 검증 후 데이터만 전달, 폼 렌더링 제어

## 6. UI/UX Considerations

### 6.1 Component Library

*   **shadcn/ui 활용:**
    - `Table` — 계약 목록 (정렬, 필터 기능)
    - `Dialog` / `Sheet` — 계약 등록/수정/상태변경 모달
    - `Button` — 액션 버튼 (신규, 수정, 상태변경 등)
    - `Input`, `Select`, `Textarea` — 폼 입력
    - `Badge` — 계약 상태 표시 (활성/종료/갱신예정 색상 구분)
    - `Card` — 통계 패널
    - `Skeleton` — 로딩 UI
    - `Toast` / `Alert` — 성공/에러 메시지

*   **Lucide React Icons:**
    - FileText — 계약서 아이콘
    - Download — 다운로드 아이콘
    - Trash2 — 삭제 아이콘
    - AlertTriangle — 경고 상태

### 6.2 Responsive Design

*   **Desktop (1280px+):**
    - 테이블 전체 너비 표시
    - 필터 사이드바 좌측 고정
    - 통계 패널: 2-3 컬럼 레이아웃

*   **Tablet (768px~1279px):**
    - 테이블 스크롤 가능 (수평)
    - 필터: 아코디언 또는 토글 방식
    - 통계: 1-2 컬럼 레이아웃

*   **모달 폼:** 모든 기기에서 responsive (최대 너비 제한)

### 6.3 Status Badge 색상

| 상태 | 색상 | 설명 | 배지 |
|------|------|------|------|
| 활성 | green | 계약 진행 중 | 활성 |
| 종료 | gray | 계약 만료됨 | 종료 |
| 갱신예정 | blue | 곧 갱신 필요 | 갱신예정 |
| 30일 내 만료 | red | 긴급 알림 | 주의 |
| 60일 내 만료 | yellow | 경고 | 예고 |

### 6.4 User Feedback

*   **로딩:** 스켈레톤 UI (테이블, 통계)
*   **성공:** Toast 메시지 (색상: green, 지속시간: 3초)
*   **에러:** Toast 메시지 (색상: red) + 폼 필드 에러 표시
*   **검증:** 실시간 (폼 입력 중 에러 표시)
*   **파일 업로드:** 진행률 표시 + 완료 알림

## 7. Success Metrics

*   **기능 완성도:**
    - 모든 User Story 구현 (10/10 ✓)
    - API 테스트 커버리지 ≥ 80%

*   **사용성:**
    - 계약 목록 로드 시간 < 500ms (p95)
    - 필터 적용 반응 < 200ms
    - 계약 생성 폼 제출 성공률 > 99%
    - 파일 업로드 성공률 > 99%

*   **데이터 무결성:**
    - 중복 계약 등록 방지 (중복 검사)
    - 상태 전이 규칙 위반 방지 (100%)
    - 이력 기록 완전성 (모든 변경)

*   **Technical Performance:**
    - API Response Time: p95 < 200ms
    - Page Load (FCP): < 1.5s
    - File Upload: 10MB 파일 < 5초
    - Cumulative Layout Shift (CLS): < 0.1

## 8. Security Considerations

*   **Authentication:** 모든 API route에서 `getServerSession()` 검증, 미인증 시 401 반환
*   **Authorization:** Role-based access control
    - USER: GET only
    - MANAGER: GET/POST/PUT (계약 수정 가능)
    - ADMIN: GET/POST/PUT/DELETE (모든 기능)
*   **Input Validation:** 서버사이드 필수
    - 계약명: 길이 제약 (1-255자)
    - 날짜: 유효한 날짜 형식, start_date ≤ end_date
    - 금액: 숫자 형식, 양수 (nullable)
    - 파일: MIME 타입 검증 (PDF, DOCX), 크기 제약 (10MB)
*   **SQL Injection 방지:** TypeORM parameterized queries 사용
*   **XSS 방지:** React 자동 escaping, 사용자 입력은 항상 sanitize
*   **File Upload Security:**
    - 파일 확장자 화이트리스트 (.pdf, .docx, .doc)
    - 저장 경로: 공개 폴더 외부 (`/uploads/maintenance/`)
    - 파일명: UUID + 원본 파일명으로 저장 (경로 탈출 방지)
*   **Data Protection:** 민감 정보 없음 (계약 정보는 내부용)
*   **Soft Delete:** 물리 삭제 금지, `deleted_at` 필터로 논리 삭제

## 9. Open Questions & Dependencies

*   **Dependency 1:** Customer 엔티티 및 API 구현 필수 (Phase 1에서 완료)
*   **Dependency 2:** Employee 엔티티의 역할(role) 정의 필수 (Phase 1에서 완료)
*   **Dependency 3:** 전체 레이아웃 구현 완료 필요 (2001_PRD 기반)
*   **Question 1:** 계약 금액은 필수인가, 아니면 선택사항인가?
    - **결정:** 선택사항 (nullable). 비용 정보가 없는 경우도 있을 수 있음.
*   **Question 2:** 계약 유형의 종류가 정해졌는가? (유지보수, 보증, 기술지원 등)
    - **결정:** 1단계는 자유 텍스트. 2단계에서 코드화 고려.
*   **Question 3:** 파일 업로드는 로컬 스토리지만 지원하는가?
    - **결정:** 로컬 스토리지만 (`./uploads/maintenance/`). 클라우드는 2단계.

## 10. Component File Structure

```
src/
├── app/(main)/
│   └── maintenance/
│       ├── page.tsx                              # 계약 목록 (SC)
│       ├── new/page.tsx                          # 계약 등록 (SC)
│       ├── [id]/
│       │   └── page.tsx                          # 계약 상세 (SC)
│       └── loading.tsx                           # 로딩 스켈레톤
│
├── components/features/maintenance/
│   ├── MaintenanceContractTable.tsx              # 계약 목록 테이블 (CC)
│   ├── MaintenanceContractFilters.tsx            # 필터 패널 (CC)
│   ├── MaintenanceContractStats.tsx              # 통계 패널 (CC)
│   ├── MaintenanceContractDetailView.tsx         # 계약 상세 뷰 (SC/CC)
│   ├── MaintenanceContractAttachments.tsx        # 첨부 파일 관리 (CC)
│   ├── MaintenanceContractHistory.tsx            # 갱신 이력 (CC)
│   ├── forms/
│   │   ├── CreateMaintenanceContractForm.tsx     # 계약 생성 폼 (CC)
│   │   ├── UpdateMaintenanceContractForm.tsx     # 계약 수정 폼 (CC)
│   │   └── StatusChangeForm.tsx                  # 상태변경 폼 (CC)
│   └── dialogs/
│       └── MaintenanceContractActionsDialog.tsx  # 액션 모달 (CC)
│
├── entities/
│   ├── MaintenanceContract.ts                    # 계약 엔티티
│   ├── MaintenanceContractAttachment.ts          # 첨부 엔티티
│   └── MaintenanceContractHistory.ts             # 이력 엔티티
│
├── lib/
│   ├── maintenance-service.ts                    # 비즈니스 로직 (상태 전이 등)
│   └── maintenance-constants.ts                  # 상수 (상태, 유형)
│
├── hooks/
│   ├── useMaintenanceContractList.ts             # 목록 쿼리
│   ├── useMaintenanceContractDetail.ts           # 상세 쿼리
│   ├── useMaintenanceContractStats.ts            # 통계 쿼리
│   └── useMaintenanceContractMutations.ts        # 뮤테이션
│
└── app/api/maintenance/
    ├── route.ts                                  # GET, POST
    ├── [id]/route.ts                             # GET, PUT, DELETE
    ├── [id]/status/route.ts                      # POST (상태변경)
    ├── [id]/attachments/route.ts                 # POST (파일 업로드)
    ├── [id]/attachments/[attachmentId]/route.ts  # DELETE (파일 삭제)
    ├── [id]/history/route.ts                     # GET (이력)
    └── stats/route.ts                            # GET (통계)
```

## 11. Implementation Phases

### Phase 1: Core CRUD + List
*   계약 목록 조회 (필터링, 정렬, 페이지네이션)
*   계약 상세 조회 (기본 정보)
*   계약 신규 생성
*   계약 정보 수정
*   API Route Handlers (기본)
*   Entity + Migration

### Phase 2: Advanced Features
*   계약 상태 관리 + 상태 전이 검증
*   계약서 파일 첨부 + 관리
*   계약 갱신 이력 추적
*   계약 만료 알림 (30일/60일)
*   통계 대시보드
*   권한별 UI 제어

### Phase 3: Polish & Analytics
*   자동 계약 갱신 알림
*   계약 분석 대시보드
*   고급 검색/필터
*   성능 최적화
*   테스트 강화
