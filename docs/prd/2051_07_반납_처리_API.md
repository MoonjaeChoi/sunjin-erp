<!-- Generated: 2026-01-25 21:30:00 KST -->

# 반납 처리 API

**문서 번호**: 2051_07
**원본 PRD**: 2051_재고_관리_prd_v2.md (User Story 8)
**구현 범위**: `src/app/api/inventory/[id]/checkin/route.ts`
**복잡도**: M
**의존성**: 2051_02, 2051_12, 2051_13

---

## 구현 목표

출고 상태인 장비를 반납 처리하는 API를 구현한다. 상태 전이 규칙(출고→재고), 반납 위치 기록, 반납 이력 자동 생성을 포함한다.

**Endpoint**: `POST /api/inventory/[id]/checkin`

---

## Request Body

```typescript
{
  checkin_location: string;     // 필수, 반납 위치
  checked_in_by: string;        // 필수, 반납자 명
}
```

## Response Format

```typescript
{
  id: number;
  current_status: '재고';
  current_location: string;     // 반납 위치로 업데이트
  updated_at: string;           // ISO 8601 timestamp
  
  history: {
    id: number;
    change_type: '반납';
    previous_status: '출고';
    new_status: '재고';
    previous_location: string | null;
    new_location: string;       // 반납 위치
    changed_by_id: number;
    changed_at: string;
  };
}
```

## 구현 상세

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { getServerSession } from 'next-auth/next';
import { authOptions } from '@/lib/auth';
import { getRepository } from '@/lib/database';
import { Inventory } from '@/entities/Inventory';
import { InventoryHistory } from '@/entities/InventoryHistory';
import { InventoryService } from '@/lib/inventory-service';
import { IsNull } from 'typeorm';

export async function POST(
  request: NextRequest,
  { params }: { params: { id: string } }
): Promise<NextResponse> {
  try {
    // 1. 인증 및 권한 검증
    const session = await getServerSession(authOptions);
    if (!session?.user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    if (!['MANAGER', 'ADMIN'].includes(session.user.role)) {
      return NextResponse.json(
        { error: 'Forbidden: Only MANAGER and ADMIN can checkin inventory' },
        { status: 403 }
      );
    }

    const inventoryId = parseInt(params.id);
    if (isNaN(inventoryId)) {
      return NextResponse.json({ error: 'Invalid ID' }, { status: 400 });
    }

    // 2. Request Body 파싱
    const body = await request.json();
    const { checkin_location, checked_in_by } = body;

    if (!checkin_location || !checked_in_by) {
      return NextResponse.json(
        { error: 'checkin_location and checked_in_by are required' },
        { status: 400 }
      );
    }

    // 3. Inventory 조회
    const inventoryRepository = getRepository(Inventory);
    const inventory = await inventoryRepository.findOne({
      where: { id: inventoryId, deleted_at: IsNull() },
    });

    if (!inventory) {
      return NextResponse.json(
        { error: 'Inventory not found' },
        { status: 404 }
      );
    }

    // 4. 상태 전이 검증 (출고 → 재고)
    const validation = InventoryService.validateStateTransition(
      inventory.current_status,
      '재고'
    );

    if (!validation.valid) {
      return NextResponse.json(
        { error: validation.error },
        { status: 422 }
      );
    }

    // 5. Inventory 상태 업데이트
    const previousLocation = inventory.current_location;
    inventory.current_status = '재고';
    inventory.current_location = checkin_location;
    inventory.updated_by_id = session.user.id;
    inventory.updated_at = new Date();

    const updatedInventory = await inventoryRepository.save(inventory);

    // 6. 반납 이력 생성
    const historyRepository = getRepository(InventoryHistory);
    const history = new InventoryHistory();
    history.inventory_id = inventoryId;
    history.change_type = '반납';
    history.previous_status = '출고';
    history.new_status = '재고';
    history.previous_location = previousLocation;
    history.new_location = checkin_location;
    history.checkout_location = null; // 초기화
    history.expected_checkin_date = null;
    history.reason = null;
    history.changed_by_id = session.user.id;

    const savedHistory = await historyRepository.save(history);

    return NextResponse.json(
      {
        id: updatedInventory.id,
        current_status: updatedInventory.current_status,
        current_location: updatedInventory.current_location,
        updated_at: updatedInventory.updated_at.toISOString(),
        history: {
          id: savedHistory.id,
          change_type: savedHistory.change_type,
          previous_status: savedHistory.previous_status,
          new_status: savedHistory.new_status,
          previous_location: savedHistory.previous_location,
          new_location: savedHistory.new_location,
          changed_by_id: savedHistory.changed_by_id,
          changed_at: savedHistory.changed_at.toISOString(),
        },
      },
      { status: 200 }
    );
  } catch (error) {
    console.error('POST /api/inventory/[id]/checkin error:', error);
    return NextResponse.json(
      { error: 'Internal Server Error' },
      { status: 500 }
    );
  }
}
```

---

## Acceptance Criteria

- [ ] POST /api/inventory/[id]/checkin handler 구현
- [ ] 인증 및 권한 검증 (MANAGER/ADMIN only)
- [ ] 필수 필드 검증 (checkin_location, checked_in_by)
- [ ] 상태 전이 검증 (출고→재고만 가능)
- [ ] Inventory 상태 업데이트 (출고→재고)
- [ ] current_location 업데이트 (반납 위치)
- [ ] 반납 이력 생성 (change_type='반납')
- [ ] 응답 포맷 (200 OK)
- [ ] 에러 처리 (400, 401, 403, 404, 422, 500)

---

**다음 문서**: 2051_08_위치_변경_API.md
