<!-- Generated: 2026-01-25 KST -->

# Project + ProjectAttachment Entity 정의

**문서 번호**: 2041_01
**원본 PRD**: 2041_프로젝트_관리_prd_v2.md
**구현 범위**: Project, ProjectAttachment TypeORM Entity 정의
**복잡도**: M
**의존성**: 없음 (선행 작업)

---

## 구현 목표

프로젝트 관리 모듈의 핵심 데이터 모델인 Project와 ProjectAttachment Entity를 TypeORM 기반으로 정의한다. Oracle XE 21c 타입 규칙(VARCHAR2, NUMBER, CLOB, TIMESTAMP)을 준수하며, 8단계 Sales Pipeline 체크리스트 컬럼과 복수 첨부파일 관계를 포함한다.

---

## 구현 내용

### 파일 구조

```
src/
├── entities/
│   ├── Project.ts              # Project Entity 정의
│   └── ProjectAttachment.ts    # ProjectAttachment Entity 정의
```

### 구현 상세

#### 1. Project Entity (`src/entities/Project.ts`)

- Table: `PROJECT`
- PK: `id` (NUMBER, `PROJECT_ID_SEQ` 시퀀스 기반)
- 비즈니스 필드: project_code(UNIQUE, nullable), project_name, customer_id(FK), employee_id(FK), status, start_date, end_date, contract_amount, description(CLOB)
- 체크리스트 필드: stage_meeting_at, stage_proposal_at, stage_quotation_at, stage_contract_at, stage_kickoff_at, stage_development_at, stage_delivery_at, stage_handover_at (모두 TIMESTAMP, nullable)
- 감사 필드: created_at, updated_at (DEFAULT CURRENT_TIMESTAMP), deleted_at (nullable, soft delete)
- status 기본값: `'PREPARING'`
- Relations: ManyToOne → Customer, ManyToOne → Employee, OneToMany → ProjectAttachment

#### 2. ProjectAttachment Entity (`src/entities/ProjectAttachment.ts`)

- Table: `PROJECT_ATTACHMENT`
- PK: `id` (NUMBER, `PROJECT_ATTACHMENT_ID_SEQ` 시퀀스 기반)
- 필드: project_id(FK), file_path, file_name, file_size, category
- category: `'CONTRACT' | 'PROPOSAL' | 'QUOTATION' | 'REPORT' | 'OTHER'`
- 감사 필드: created_at (DEFAULT CURRENT_TIMESTAMP)
- Relations: ManyToOne → Project

### 핵심 인터페이스

```typescript
// src/entities/Project.ts
export type ProjectStatus = 'PREPARING' | 'IN_PROGRESS' | 'COMPLETED' | 'ON_HOLD';

@Entity('PROJECT')
export class Project {
  @PrimaryGeneratedColumn({ type: 'number' })
  id!: number;

  @Column({ name: 'project_code', type: 'varchar2', length: 30, nullable: true, unique: true })
  project_code!: string | null;

  @Column({ name: 'project_name', type: 'varchar2', length: 200, nullable: false })
  project_name!: string;

  @Column({ name: 'customer_id', type: 'number', nullable: false })
  customer_id!: number;

  @Column({ name: 'employee_id', type: 'number', nullable: false })
  employee_id!: number;

  @Column({ name: 'status', type: 'varchar2', length: 20, default: "'PREPARING'" })
  status!: ProjectStatus;

  @Column({ name: 'start_date', type: 'date', nullable: true })
  start_date!: Date | null;

  @Column({ name: 'end_date', type: 'date', nullable: true })
  end_date!: Date | null;

  @Column({ name: 'contract_amount', type: 'number', nullable: true })
  contract_amount!: number | null;

  @Column({ name: 'description', type: 'clob', nullable: true })
  description!: string | null;

  // 8단계 Sales Pipeline 체크리스트 (TIMESTAMP, nullable)
  @Column({ name: 'stage_meeting_at', type: 'timestamp', nullable: true })
  stage_meeting_at!: Date | null;

  @Column({ name: 'stage_proposal_at', type: 'timestamp', nullable: true })
  stage_proposal_at!: Date | null;

  @Column({ name: 'stage_quotation_at', type: 'timestamp', nullable: true })
  stage_quotation_at!: Date | null;

  @Column({ name: 'stage_contract_at', type: 'timestamp', nullable: true })
  stage_contract_at!: Date | null;

  @Column({ name: 'stage_kickoff_at', type: 'timestamp', nullable: true })
  stage_kickoff_at!: Date | null;

  @Column({ name: 'stage_development_at', type: 'timestamp', nullable: true })
  stage_development_at!: Date | null;

  @Column({ name: 'stage_delivery_at', type: 'timestamp', nullable: true })
  stage_delivery_at!: Date | null;

  @Column({ name: 'stage_handover_at', type: 'timestamp', nullable: true })
  stage_handover_at!: Date | null;

  @CreateDateColumn({ name: 'created_at', type: 'timestamp' })
  created_at!: Date;

  @UpdateDateColumn({ name: 'updated_at', type: 'timestamp' })
  updated_at!: Date;

  @DeleteDateColumn({ name: 'deleted_at', type: 'timestamp', nullable: true })
  deleted_at!: Date | null;

  // Relations
  @ManyToOne(() => Customer)
  @JoinColumn({ name: 'customer_id' })
  customer!: Customer;

  @ManyToOne(() => Employee)
  @JoinColumn({ name: 'employee_id' })
  employee!: Employee;

  @OneToMany(() => ProjectAttachment, (a) => a.project)
  attachments!: ProjectAttachment[];
}
```

```typescript
// src/entities/ProjectAttachment.ts
export type AttachmentCategory = 'CONTRACT' | 'PROPOSAL' | 'QUOTATION' | 'REPORT' | 'OTHER';

@Entity('PROJECT_ATTACHMENT')
export class ProjectAttachment {
  @PrimaryGeneratedColumn({ type: 'number' })
  id!: number;

  @Column({ name: 'project_id', type: 'number', nullable: false })
  project_id!: number;

  @Column({ name: 'file_path', type: 'varchar2', length: 500, nullable: false })
  file_path!: string;

  @Column({ name: 'file_name', type: 'varchar2', length: 200, nullable: false })
  file_name!: string;

  @Column({ name: 'file_size', type: 'number', nullable: false })
  file_size!: number;

  @Column({ name: 'category', type: 'varchar2', length: 20, nullable: false })
  category!: AttachmentCategory;

  @CreateDateColumn({ name: 'created_at', type: 'timestamp' })
  created_at!: Date;

  // Relations
  @ManyToOne(() => Project, (p) => p.attachments)
  @JoinColumn({ name: 'project_id' })
  project!: Project;
}
```

---

## Acceptance Criteria

- [ ] Project Entity 파일 생성 완료 (`src/entities/Project.ts`)
- [ ] ProjectAttachment Entity 파일 생성 완료 (`src/entities/ProjectAttachment.ts`)
- [ ] Oracle 타입 규칙 준수 (VARCHAR2, NUMBER, CLOB, TIMESTAMP)
- [ ] PrimaryGeneratedColumn에 시퀀스 연결 설정
- [ ] 8단계 체크리스트 컬럼 (stage_*_at) 모두 nullable TIMESTAMP
- [ ] status 기본값 'PREPARING' 설정
- [ ] project_code UNIQUE + nullable 설정
- [ ] ManyToOne/OneToMany 관계 설정 (Customer, Employee, ProjectAttachment)
- [ ] Soft delete (deleted_at) 컬럼 포함
- [ ] `npm run type-check` 통과
- [ ] `npm run build` 성공

---

## 테스트 전략

### TypeScript & Lint

```bash
npm run type-check
npm run build
npm run lint
```

### 검증 방법

1. Entity 파일 작성 후 TypeScript 빌드 성공 확인
2. TypeORM decorator 문법 오류 없음 확인
3. 관계 설정 양방향 참조 정확성 확인
4. 후속 Migration(2041_02)에서 실제 테이블 생성 시 검증

---

**다음 문서**: 2041_02_Migration_생성.md
