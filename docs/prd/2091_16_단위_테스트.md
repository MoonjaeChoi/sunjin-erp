<!-- Generated: 2026-01-28 05:00:00 KST -->

# 단위 테스트

**문서 번호**: 2091_16
**원본 PRD**: 2091_직원_관리_prd_v2.md
**PRD 참조**: 원본 PRD의 'Section 4.1 In-Scope (Unit Testing)' 참조
**구현 범위**: API Route, Hooks, Components 단위 테스트
**복잡도**: L (2-3일)
**의존성**: 2091_15

---

## 구현 목표

직원 관리 모듈의 모든 API Route, TanStack Query Hooks, 컴포넌트에 대한 단위 테스트를 구현합니다.

---

## 구현 내용

### 파일 구조

```
src/__tests__/
├── api/
│   ├── employees.test.ts           # 직원 API 테스트
│   ├── departments.test.ts         # 부서 API 테스트
│   └── positions.test.ts           # 직급 API 테스트
├── hooks/
│   └── useEmployees.test.ts        # Hooks 테스트
└── components/
    └── employees/
        ├── EmployeeList.test.tsx   # 목록 컴포넌트 테스트
        ├── EmployeeForm.test.tsx   # 폼 컴포넌트 테스트
        └── EmployeeDetail.test.tsx # 상세 컴포넌트 테스트
```

### 테스트 범위

#### API Route 테스트

```typescript
// src/__tests__/api/employees.test.ts

describe('GET /api/employees', () => {
  it('should return paginated employee list for ADMIN', async () => { });
  it('should filter by department for MANAGER', async () => { });
  it('should reject unauthorized users', async () => { });
  it('should search by name or email', async () => { });
});

describe('POST /api/employees', () => {
  it('should create employee for ADMIN', async () => { });
  it('should reject duplicate email', async () => { });
  it('should validate required fields', async () => { });
  it('should record CREATE history', async () => { });
});

describe('PUT /api/employees/[id]', () => {
  it('should update employee for ADMIN', async () => { });
  it('should reject MANAGER edit attempt', async () => { });
  it('should record UPDATE history', async () => { });
});

describe('DELETE /api/employees/[id]', () => {
  it('should reject deletion with dependencies', async () => { });
  it('should return detailed dependency list', async () => { });
  it('should hard delete when no dependencies', async () => { });
});
```

#### 부서 API 테스트

```typescript
// src/__tests__/api/departments.test.ts

describe('POST /api/departments', () => {
  it('should enforce max depth of 5', async () => { });
  it('should prevent duplicate name', async () => { });
});

describe('PUT /api/departments/[id]', () => {
  it('should prevent circular reference', async () => { });
});

describe('DELETE /api/departments/[id]', () => {
  it('should reject with child departments', async () => { });
  it('should reject with active employees', async () => { });
});
```

#### 계정 API 테스트

```typescript
// src/__tests__/api/accounts.test.ts

describe('POST /api/employees/[id]/accounts', () => {
  it('should create account with bcrypt hash', async () => { });
  it('should validate password strength', async () => { });
  it('should return temporary password once', async () => { });
});

describe('POST /api/employees/[id]/deactivate', () => {
  it('should deactivate account', async () => { });
  it('should prevent self-deactivation', async () => { });
  it('should record DEACTIVATE history', async () => { });
});
```

#### Hooks 테스트

```typescript
// src/__tests__/hooks/useEmployees.test.ts

describe('useEmployees', () => {
  it('should fetch employee list with query params', async () => { });
  it('should cache data with correct staleTime', async () => { });
});

describe('useCreateEmployee', () => {
  it('should invalidate list cache on success', async () => { });
});

describe('useDeactivateEmployee', () => {
  it('should invalidate detail and list cache', async () => { });
});
```

#### 컴포넌트 테스트

```typescript
// src/__tests__/components/employees/EmployeeList.test.tsx

describe('EmployeeList', () => {
  it('should render employee table', async () => { });
  it('should show loading skeleton', async () => { });
  it('should hide create button for MANAGER', async () => { });
  it('should filter by department for MANAGER', async () => { });
});
```

---

## 테스트 실행

```bash
# 전체 테스트
npm run test

# 커버리지 포함
npm run test -- --coverage

# 특정 파일
npm run test -- src/__tests__/api/employees.test.ts

# Watch 모드
npm run test -- --watch
```

---

## 커버리지 목표

| 영역 | 라인 | 브랜치 |
|------|------|--------|
| API Routes | 80%+ | 75%+ |
| Hooks | 80%+ | 75%+ |
| Components | 70%+ | 60%+ |
| **전체** | **80%+** | **75%+** |

---

## Acceptance Criteria

- [ ] API Route 테스트 구현 (employees, departments, positions, accounts)
- [ ] RBAC 권한 테스트 (ADMIN/MANAGER 분기)
- [ ] 유효성 검증 테스트
- [ ] 의존성 검증 테스트 (삭제 거부)
- [ ] 이력 기록 테스트
- [ ] Hooks 테스트 구현
- [ ] 컴포넌트 테스트 구현
- [ ] 커버리지 80%+ 달성
- [ ] 모든 테스트 통과

---

## 완료 체크리스트

- [ ] API 테스트 파일 생성
- [ ] Hooks 테스트 파일 생성
- [ ] 컴포넌트 테스트 파일 생성
- [ ] 모든 테스트 통과
- [ ] 커버리지 목표 달성
- [ ] CI/CD 파이프라인 통과

---

**구현 완료 후**: 전체 모듈 통합 테스트 및 스테이징 배포
