<!-- Generated: 2026-01-28 15:30:00 KST -->

# API: 게시물 목록 조회 및 작성

**문서 번호**: 2101_04
**원본 PRD**: 2101_공지사항_prd_v2.md
**PRD 참조**: 원본 PRD의 'Section 6.2 API Route Handlers' 참조
**구현 범위**: GET /api/notices (목록), POST /api/notices (작성)
**복잡도**: M (1~2일)
**의존성**: 2101_01~03 (테이블, 타입)

---

## 구현 목표

게시물 목록 조회와 신규 작성 API를 구현합니다.
- GET: 페이지네이션, 필터링, 검색, 정렬
- POST: 게시물 작성 + 파일 업로드

---

## 구현 내용

### 파일 구조

```
src/app/api/notices/
└── route.ts
```

### GET /api/notices 구현

```typescript
// Generated: 2026-01-28 15:30:00 KST

import { NextRequest, NextResponse } from 'next/server';
import { getServerSession } from 'next-auth';
import { authOptions } from '@/lib/auth';
import { executeQuery } from '@/lib/db-direct';
import type { NoticeListItem, NoticeListResponse, NoticeType } from '@/types/notice';

export const dynamic = 'force-dynamic';

const ALLOWED_SORT_BY = ['latest', 'viewCount', 'commentCount'] as const;
const VALID_TYPES: NoticeType[] = ['공지', '공유', '지시', '건의', '자유'];

export async function GET(req: NextRequest): Promise<NextResponse> {
  try {
    // 1. 인증 검증
    const session = await getServerSession(authOptions);
    if (!session?.user) {
      return NextResponse.json({ message: 'Unauthorized' }, { status: 401 });
    }

    // 2. 쿼리 파라미터 파싱
    const searchParams = req.nextUrl.searchParams;
    const page = Math.max(1, parseInt(searchParams.get('page') || '1', 10));
    const limit = Math.min(100, Math.max(1, parseInt(searchParams.get('limit') || '20', 10)));
    const type = searchParams.get('type') as NoticeType | 'all' | null;
    const search = searchParams.get('search');
    const sortBy = searchParams.get('sortBy') || 'latest';
    const startDate = searchParams.get('startDate');
    const endDate = searchParams.get('endDate');
    const authorId = searchParams.get('authorId');

    // 3. WHERE 절 구성
    const whereClauses: string[] = ['n."deleted_at" IS NULL'];
    const params: Record<string, any> = {};
    let paramIndex = 0;

    // 3.1 게시판 유형 필터
    if (type && type !== 'all' && VALID_TYPES.includes(type as NoticeType)) {
      whereClauses.push(`n.TYPE = :type${paramIndex}`);
      params[`type${paramIndex}`] = type;
      paramIndex++;
    }

    // 3.2 검색 (제목, 내용)
    if (search) {
      whereClauses.push(`(
        LOWER(n.TITLE) LIKE LOWER(:search${paramIndex})
        OR LOWER(DBMS_LOB.SUBSTR(n.CONTENT, 4000, 1)) LIKE LOWER(:search${paramIndex})
      )`);
      params[`search${paramIndex}`] = `%${search}%`;
      paramIndex++;
    }

    // 3.3 날짜 범위
    if (startDate) {
      whereClauses.push(`TRUNC(n."created_at") >= TO_DATE(:startDate${paramIndex}, 'YYYY-MM-DD')`);
      params[`startDate${paramIndex}`] = startDate;
      paramIndex++;
    }

    if (endDate) {
      whereClauses.push(`TRUNC(n."created_at") <= TO_DATE(:endDate${paramIndex}, 'YYYY-MM-DD')`);
      params[`endDate${paramIndex}`] = endDate;
      paramIndex++;
    }

    // 3.4 작성자 필터
    if (authorId) {
      whereClauses.push(`n.AUTHOR_ID = :authorId${paramIndex}`);
      params[`authorId${paramIndex}`] = parseInt(authorId, 10);
      paramIndex++;
    }

    // 4. 정렬 설정
    let orderBy = 'n."created_at" DESC';
    if (sortBy === 'viewCount') {
      orderBy = 'n.VIEW_COUNT DESC, n."created_at" DESC';
    } else if (sortBy === 'commentCount') {
      orderBy = 'comment_count DESC, n."created_at" DESC';
    }

    // 5. 데이터 조회
    const offset = (page - 1) * limit;
    params.offset = offset;
    params.pageSize = limit;

    const whereClause = whereClauses.join(' AND ');

    const listQuery = `
      SELECT
        n.ID,
        n.TITLE,
        n.TYPE,
        n.AUTHOR_ID,
        e.NAME AS AUTHOR_NAME,
        n.VIEW_COUNT,
        n."created_at" AS CREATED_AT,
        n."updated_at" AS UPDATED_AT,
        (SELECT COUNT(*) FROM COMMENT c WHERE c.NOTICE_ID = n.ID AND c."deleted_at" IS NULL) AS COMMENT_COUNT,
        (SELECT COUNT(*) FROM NOTICE_ATTACHMENT a WHERE a.NOTICE_ID = n.ID AND a."deleted_at" IS NULL) AS ATTACHMENT_COUNT
      FROM NOTICE n
      LEFT JOIN EMPLOYEE e ON e.ID = n.AUTHOR_ID AND e.DELETED_AT IS NULL
      WHERE ${whereClause}
      ORDER BY ${orderBy}
      OFFSET :offset ROWS FETCH NEXT :pageSize ROWS ONLY
    `;

    const countQuery = `
      SELECT COUNT(*) AS TOTAL
      FROM NOTICE n
      WHERE ${whereClause}
    `;

    // Count 쿼리에서 offset/pageSize 제외
    const countParams = { ...params };
    delete countParams.offset;
    delete countParams.pageSize;

    const [listResult, countResult] = await Promise.all([
      executeQuery<any>(listQuery, params),
      executeQuery<any>(countQuery, countParams),
    ]);

    const total = parseInt(countResult.rows[0]?.TOTAL || '0', 10);

    // 6. 응답 변환
    const notices: NoticeListItem[] = listResult.rows.map((row: any) => ({
      id: row.ID,
      title: row.TITLE,
      type: row.TYPE as NoticeType,
      authorId: row.AUTHOR_ID,
      authorName: row.AUTHOR_NAME || '',
      viewCount: row.VIEW_COUNT || 0,
      commentCount: row.COMMENT_COUNT || 0,
      attachmentCount: row.ATTACHMENT_COUNT || 0,
      createdAt: row.CREATED_AT?.toISOString() || '',
      updatedAt: row.UPDATED_AT?.toISOString() || '',
    }));

    return NextResponse.json<NoticeListResponse>({
      data: notices,
      pagination: {
        page,
        pageSize: limit,
        total,
        totalPages: Math.ceil(total / limit),
      },
    });
  } catch (error) {
    console.error('GET /api/notices error:', error);
    return NextResponse.json({ message: 'Internal server error' }, { status: 500 });
  }
}
```

### POST /api/notices 구현

```typescript
export async function POST(req: NextRequest): Promise<NextResponse> {
  try {
    // 1. 인증 검증
    const session = await getServerSession(authOptions);
    if (!session?.user) {
      return NextResponse.json({ message: 'Unauthorized' }, { status: 401 });
    }

    const user = session.user as any;
    const userId = user.id;
    const userRole = user.role;

    // 2. 권한 검증 (MANAGER 이상)
    if (!['MANAGER', 'ADMIN'].includes(userRole)) {
      return NextResponse.json({ message: 'Forbidden' }, { status: 403 });
    }

    // 3. FormData 파싱 (파일 업로드 포함)
    const formData = await req.formData();
    const title = formData.get('title') as string;
    const content = formData.get('content') as string;
    const type = formData.get('type') as NoticeType;
    const files = formData.getAll('files') as File[];

    // 4. 필드 검증
    const errors: Record<string, string> = {};

    if (!title || title.length < 5 || title.length > 200) {
      errors.title = '제목은 5~200자 사이여야 합니다';
    }

    if (!content || content.length < 10 || content.length > 10000) {
      errors.content = '내용은 10~10,000자 사이여야 합니다';
    }

    if (!type || !VALID_TYPES.includes(type)) {
      errors.type = '게시판 유형을 선택해주세요';
    }

    // 4.1 파일 검증
    if (files.length > 5) {
      errors.files = '첨부파일은 최대 5개까지 가능합니다';
    }

    for (const file of files) {
      if (file.size > 10 * 1024 * 1024) {
        errors.files = '파일 크기는 최대 10MB입니다';
        break;
      }
    }

    if (Object.keys(errors).length > 0) {
      return NextResponse.json({ message: 'Validation failed', errors }, { status: 400 });
    }

    // 5. 게시물 INSERT
    const now = new Date();

    // ID 생성 (IDENTITY 컬럼 사용)
    const insertResult = await executeQuery<any>(
      `INSERT INTO NOTICE (TITLE, CONTENT, TYPE, AUTHOR_ID, VIEW_COUNT, "created_at", "updated_at")
       VALUES (:title, :content, :type, :authorId, 0, :now, :now)
       RETURNING ID INTO :id`,
      {
        title,
        content,
        type,
        authorId: userId,
        now,
        id: { dir: 3003, type: 2001 }, // oracledb.BIND_OUT, oracledb.NUMBER
      }
    );

    // IDENTITY 컬럼에서 ID 가져오기
    const noticeIdResult = await executeQuery<any>(
      `SELECT MAX(ID) AS ID FROM NOTICE WHERE AUTHOR_ID = :authorId AND TITLE = :title`,
      { authorId: userId, title }
    );
    const noticeId = noticeIdResult.rows[0]?.ID;

    if (!noticeId) {
      return NextResponse.json({ message: 'Failed to create notice' }, { status: 500 });
    }

    // 6. 파일 업로드 처리 (별도 함수, 2101_05에서 상세 구현)
    const uploadedFiles: any[] = [];
    if (files.length > 0) {
      // 파일 업로드 로직은 2101_05에서 구현
      // 여기서는 placeholder
    }

    // 7. 응답 반환
    return NextResponse.json(
      {
        message: '게시물이 등록되었습니다',
        data: {
          id: noticeId,
          title,
          content,
          type,
          authorId: userId,
          authorName: user.name,
          viewCount: 0,
          createdAt: now.toISOString(),
          updatedAt: now.toISOString(),
        },
      },
      { status: 201 }
    );
  } catch (error) {
    console.error('POST /api/notices error:', error);
    return NextResponse.json({ message: 'Internal server error' }, { status: 500 });
  }
}
```

---

## API 스펙

### GET /api/notices

| 파라미터 | 타입 | 기본값 | 설명 |
|----------|------|--------|------|
| page | number | 1 | 페이지 번호 |
| limit | number | 20 | 페이지 크기 (최대 100) |
| type | string | all | 게시판 유형 필터 |
| search | string | - | 검색어 (제목, 내용) |
| sortBy | string | latest | 정렬 (latest, viewCount, commentCount) |
| startDate | string | - | 시작일 (YYYY-MM-DD) |
| endDate | string | - | 종료일 (YYYY-MM-DD) |
| authorId | number | - | 작성자 ID |

### POST /api/notices

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| title | string | O | 제목 (5~200자) |
| content | string | O | 내용 (10~10,000자) |
| type | NoticeType | O | 게시판 유형 |
| files | File[] | X | 첨부파일 (최대 5개, 각 10MB) |

---

## Acceptance Criteria

- [ ] GET /api/notices 구현
  - [ ] 페이지네이션 동작
  - [ ] 유형 필터 동작
  - [ ] 검색 동작 (제목, 내용)
  - [ ] 정렬 동작 (최신순, 조회수, 댓글수)
  - [ ] 날짜 범위 필터 동작
  - [ ] 작성자 필터 동작
- [ ] POST /api/notices 구현
  - [ ] MANAGER/ADMIN만 작성 가능
  - [ ] 필드 검증 동작
  - [ ] 게시물 생성 성공

---

## 테스트 전략

### 단위 테스트

```typescript
// src/app/api/notices/__tests__/route.test.ts

describe('GET /api/notices', () => {
  it('should return notice list with pagination', async () => {
    // Given: 인증된 사용자
    // When: GET /api/notices?page=1&limit=10
    // Then: 200 OK, data 배열, pagination 객체
  });

  it('should filter by type', async () => {
    // Given: 여러 유형의 게시물
    // When: GET /api/notices?type=공지
    // Then: 공지 유형만 반환
  });

  it('should search by keyword', async () => {
    // Given: 특정 키워드가 포함된 게시물
    // When: GET /api/notices?search=키워드
    // Then: 해당 게시물만 반환
  });
});

describe('POST /api/notices', () => {
  it('should create notice for MANAGER', async () => {
    // Given: MANAGER 권한
    // When: POST /api/notices with valid data
    // Then: 201 Created
  });

  it('should return 403 for USER', async () => {
    // Given: USER 권한
    // When: POST /api/notices
    // Then: 403 Forbidden
  });

  it('should validate required fields', async () => {
    // Given: MANAGER 권한
    // When: POST /api/notices with missing fields
    // Then: 400 Bad Request, errors 객체
  });
});
```

---

## 완료 체크리스트

- [ ] `npm run type-check` 통과
- [ ] `npm run lint` 통과
- [ ] 단위 테스트 작성 및 통과
- [ ] 스테이징 서버 배포 및 검증
- [ ] Postman/cURL 수동 테스트

---

**다음 문서**: 2101_05_API_게시물_상세_수정_삭제.md
