<!-- Generated: 2026-01-26 21:30:00 KST -->

# 반응형 디자인

**문서 번호**: 2071_20
**원본 PRD**: `docs/prd/2071_유지보수_고객_관리_prd_v2.md`
**개발 표준**: `CLAUDE.md` (Tailwind CSS 반응형 클래스, Desktop-first 패턴, sm/md/lg 브레이크포인트)
**구현 범위**: Tailwind CSS 반응형 설정 및 검증
**복잡도**: S (0.5-1일)
**의존성**: 2071_15~19 (모든 UI 컴포넌트)

---

## 구현 목표

Tailwind CSS를 사용하여 다음을 구현합니다:
- Desktop-first 반응형 디자인
- Breakpoint: sm (640px), md (768px), lg (1024px), xl (1280px)
- 모든 컴포넌트가 반응형 스타일 적용
- 모바일/태블릿/데스크톱에서 가독성 유지

---

## 구현 내용

### 1. Breakpoint 기준

```
Mobile:  < 640px   (sm)
Tablet:  640px-768px (md)
Desktop: 768px+    (lg+)
```

### 2. 반응형 컴포넌트별 지침

#### MaintenanceList.tsx

```typescript
// 헤더 - 모바일에서는 스택, 데스크톱에서는 플렉스
<div className="flex flex-col md:flex-row items-center justify-between">

// 통계 카드 - 모바일 1열, 태블릿 2열, 데스크톱 4열
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
```

#### MaintenanceDataTable.tsx

```typescript
// 테이블 - 모바일에서는 가로 스크롤, 데스크톱에서는 정상
<div className="rounded-lg border overflow-x-auto md:overflow-visible">

// 페이지네이션 - 모바일에서는 세로, 데스크톱에서는 가로
<div className="flex flex-col md:flex-row items-center justify-between gap-4">
```

#### MaintenanceFilters.tsx

```typescript
// 필터 그리드 - 모바일 1열, 태블릿 2열, 데스크톱 3열
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
```

#### MaintenanceInfo.tsx

```typescript
// 상태 정보 - 모바일 2열, 태블릿 3열, 데스크톱 4열
<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">

// 상세 정보 - 모바일 1열, 태블릿 2열, 데스크톱 4열
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
```

### 3. 폰트 크기 반응형

```typescript
// 제목 - 모바일에서 더 작음
<h1 className="text-2xl md:text-3xl font-bold">

// 부제목
<h2 className="text-lg md:text-xl font-semibold">

// 본문 텍스트
<p className="text-sm md:text-base">
```

### 4. 패딩/마진 반응형

```typescript
// 컨테이너 패딩 - 모바일: p-4, 데스크톱: p-6
<div className="p-4 md:p-6">

// 간격 - 모바일: space-y-4, 데스크톱: space-y-6
<div className="space-y-4 md:space-y-6">

// 갭 - 모바일: gap-2, 데스크톱: gap-4
<div className="flex gap-2 md:gap-4">
```

### 5. 숨김/표시

```typescript
// 모바일에서만 표시
<div className="md:hidden">

// 모바일에서만 숨김
<div className="hidden md:block">

// 태블릿 이상에서만 표시
<div className="hidden lg:flex">
```

### 6. 테이블 모바일 대응

```typescript
// 모바일에서는 카드 레이아웃으로 변경 (필요시)
<div className="md:hidden space-y-4">
  {/* 모바일용 카드 레이아웃 */}
</div>

<div className="hidden md:block">
  {/* 데스크톱용 테이블 */}
</div>
```

---

## Tailwind CSS 설정 (tailwind.config.ts)

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}'
  ],
  theme: {
    extend: {
      colors: {
        // shadcn/ui 기본 색상
      },
      spacing: {
        // 커스텀 스페이싱 (필요시)
      }
    }
  },
  plugins: [require('tailwindcss/plugin')]
};

export default config;
```

---

## Acceptance Criteria

- [ ] 모든 컴포넌트가 반응형 클래스 사용
- [ ] 모바일: 320px~639px 테스트
- [ ] 태블릿: 640px~1023px 테스트
- [ ] 데스크톱: 1024px+ 테스트
- [ ] 텍스트 가독성 유지
- [ ] 레이아웃 깨짐 없음
- [ ] 네비게이션 모바일 친화적
- [ ] 이미지/아이콘 스케일링 정상
- [ ] 버튼 터치 타겟 (min 44px)

---

## 테스트 전략

### 브라우저 개발자 도구

```
1. Chrome DevTools → Device Toolbar
2. 다양한 디바이스 시뮬레이션:
   - iPhone SE (375px)
   - iPad (768px)
   - Desktop (1920px)
3. 각 화면에서 레이아웃 검증
```

### Responsive Design Checker

```bash
# 또는 외부 도구 사용
# - responsively.app
# - viewport-resizer.com
```

---

## 완료 체크리스트

- [ ] 모든 페이지에 반응형 클래스 적용
- [ ] 모바일 화면에서 테스트
- [ ] 태블릿 화면에서 테스트
- [ ] 데스크톱 화면에서 테스트
- [ ] 텍스트 가독성 확인
- [ ] 네비게이션 동작 확인
- [ ] npm run build 성공

---

**다음 문서**: `2071_21_코딩_표준_검증.md`
