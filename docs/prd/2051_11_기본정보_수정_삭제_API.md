<!-- Generated: 2026-01-25 21:30:00 KST -->

# 기본 정보 수정 + 소프트 삭제 API

**문서 번호**: 2051_11
**원본 PRD**: 2051_재고_관리_prd_v2.md (Section 5.2)
**구현 범위**: `src/app/api/inventory/[id]/route.ts` (PUT, DELETE handlers)
**복잡도**: M
**의존성**: 2051_02, 2051_12

---

## 구현 목표

기본 정보(카테고리, 모델, 구매처 등) 수정(PUT, ADMIN only) 및 소프트 삭제(DELETE, ADMIN only) API를 구현한다.

**Endpoints**:
- `PUT /api/inventory/[id]` — 기본 정보 수정
- `DELETE /api/inventory/[id]` — 소프트 삭제

---

## PUT Handler: 기본 정보 수정

### Request Body

```typescript
{
  category?: string;
  model?: string;
  purchase_date?: string;       // ISO 8601 date
  purchase_from?: string;
  notes?: string;
}
```

### Response Format

```typescript
{
  id: number;
  category: string;
  model: string;
  serial_number: string;
  purchase_date: string;
  purchase_from: string;
  current_location: string;
  current_status: string;
  notes: string | null;
  updated_at: string;
}
```

### 구현

```typescript
export async function PUT(
  request: NextRequest,
  { params }: { params: { id: string } }
): Promise<NextResponse> {
  try {
    const session = await getServerSession(authOptions);
    if (!session?.user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    // ADMIN만 수정 가능
    if (session.user.role !== 'ADMIN') {
      return NextResponse.json(
        { error: 'Forbidden: Only ADMIN can modify inventory' },
        { status: 403 }
      );
    }

    const inventoryId = parseInt(params.id);
    if (isNaN(inventoryId)) {
      return NextResponse.json({ error: 'Invalid ID' }, { status: 400 });
    }

    const body = await request.json();

    const repository = getRepository(Inventory);
    const inventory = await repository.findOne({
      where: { id: inventoryId, deleted_at: IsNull() },
    });

    if (!inventory) {
      return NextResponse.json(
        { error: 'Inventory not found' },
        { status: 404 }
      );
    }

    // 선택적 필드만 업데이트
    if (body.category !== undefined) inventory.category = body.category;
    if (body.model !== undefined) inventory.model = body.model;
    if (body.purchase_date !== undefined) {
      inventory.purchase_date = new Date(body.purchase_date);
    }
    if (body.purchase_from !== undefined) {
      inventory.purchase_from = body.purchase_from;
    }
    if (body.notes !== undefined) inventory.notes = body.notes;

    inventory.updated_by_id = session.user.id;
    inventory.updated_at = new Date();

    const updated = await repository.save(inventory);

    return NextResponse.json(
      {
        id: updated.id,
        category: updated.category,
        model: updated.model,
        serial_number: updated.serial_number,
        purchase_date: updated.purchase_date.toISOString().split('T')[0],
        purchase_from: updated.purchase_from,
        current_location: updated.current_location,
        current_status: updated.current_status,
        notes: updated.notes,
        updated_at: updated.updated_at.toISOString(),
      },
      { status: 200 }
    );
  } catch (error) {
    console.error('PUT /api/inventory/[id] error:', error);
    return NextResponse.json(
      { error: 'Internal Server Error' },
      { status: 500 }
    );
  }
}
```

---

## DELETE Handler: 소프트 삭제

### Response Format

```typescript
{
  id: number;
  deleted: true;
  deleted_at: string;           // ISO 8601 timestamp
}
```

### 구현

```typescript
export async function DELETE(
  request: NextRequest,
  { params }: { params: { id: string } }
): Promise<NextResponse> {
  try {
    const session = await getServerSession(authOptions);
    if (!session?.user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    // ADMIN만 삭제 가능
    if (session.user.role !== 'ADMIN') {
      return NextResponse.json(
        { error: 'Forbidden: Only ADMIN can delete inventory' },
        { status: 403 }
      );
    }

    const inventoryId = parseInt(params.id);
    if (isNaN(inventoryId)) {
      return NextResponse.json({ error: 'Invalid ID' }, { status: 400 });
    }

    const repository = getRepository(Inventory);
    const inventory = await repository.findOne({
      where: { id: inventoryId, deleted_at: IsNull() },
    });

    if (!inventory) {
      return NextResponse.json(
        { error: 'Inventory not found' },
        { status: 404 }
      );
    }

    // 소프트 삭제 (deleted_at 설정)
    inventory.deleted_at = new Date();
    const deleted = await repository.save(inventory);

    return NextResponse.json(
      {
        id: deleted.id,
        deleted: true,
        deleted_at: deleted.deleted_at.toISOString(),
      },
      { status: 200 }
    );
  } catch (error) {
    console.error('DELETE /api/inventory/[id] error:', error);
    return NextResponse.json(
      { error: 'Internal Server Error' },
      { status: 500 }
    );
  }
}
```

---

## Acceptance Criteria

- [ ] PUT /api/inventory/[id] handler 구현
- [ ] PUT 권한 검증 (ADMIN only)
- [ ] PUT 선택적 필드 업데이트
- [ ] PUT updated_by_id, updated_at 설정
- [ ] PUT 응답 포맷 (200 OK)

- [ ] DELETE /api/inventory/[id] handler 구현
- [ ] DELETE 권한 검증 (ADMIN only)
- [ ] DELETE 소프트 삭제 (deleted_at 설정)
- [ ] DELETE InventoryHistory는 유지 (ON DELETE RESTRICT)
- [ ] DELETE 응답 포맷 (200 OK)

- [ ] 에러 처리 (400, 401, 403, 404, 500)

---

**다음 문서**: 2051_12_TypeScript_타입_정의.md
