<!-- Generated: 2026-01-25 KST -->

# 단위 테스트

**문서 번호**: 2041_17
**원본 PRD**: 2041_프로젝트_관리_prd_v2.md
**구현 범위**: API Route + Components 단위 테스트
**복잡도**: L
**의존성**: 2041_11 ~ 2041_16 (전체 구현 완료 후)

---

## 구현 목표

프로젝트 관리 모듈 전체에 대한 단위 테스트를 작성한다. API Route Handler와 UI Components를 커버하며, RBAC 검증, 상태 전이, 체크리스트 로직, 파일 업로드/삭제 등 핵심 시나리오를 검증한다.

---

## 구현 내용

### 파일 구조

```
src/__tests__/
├── api/
│   ├── projects/
│   │   ├── route.test.ts                    # GET/POST /api/projects
│   │   ├── [id]/
│   │   │   ├── route.test.ts                # GET/PUT/DELETE /api/projects/[id]
│   │   │   ├── checklist.test.ts            # PATCH /api/projects/[id]/checklist
│   │   │   └── attachments.test.ts          # POST/GET/DELETE attachments
│   │   ├── summary.test.ts                  # GET /api/projects/summary
│   │   └── generate-code.test.ts            # POST /api/projects/generate-code
│   └── employees/
│       └── list.test.ts                     # GET /api/employees/list
└── components/features/projects/
    ├── ProjectFilters.test.tsx              # 필터 컴포넌트
    ├── ProjectCreateDialog.test.tsx         # 등록 Dialog
    ├── ProjectDetailDialog.test.tsx         # 상세 Dialog
    ├── ProjectChecklist.test.tsx            # 체크리스트
    └── ProjectAttachments.test.tsx          # 첨부파일
```

### 테스트 개수 예상

| 카테고리 | 파일 수 | 테스트 수 (예상) |
|----------|---------|-----------------|
| API Routes | 7 | 55~65 |
| Components | 5 | 55~65 |
| **Total** | **12** | **110~130** |

### Mock 패턴

```typescript
// API Route 테스트: next/server, next-auth, TypeORM mock
jest.mock('next/server', () => ({
  NextRequest: jest.fn(),
  NextResponse: { json: jest.fn((data, init) => ({ json: () => data, status: init?.status })) },
}));
jest.mock('next-auth', () => ({ getServerSession: jest.fn() }));

// TypeORM mock
const mockRepository = {
  findOne: jest.fn(),
  find: jest.fn(),
  save: jest.fn(),
  createQueryBuilder: jest.fn(() => mockQueryBuilder),
};
jest.mock('@/lib/database', () => ({
  getDataSource: jest.fn(() => ({ getRepository: () => mockRepository })),
}));

// Component 테스트: hooks + radix UI mock
jest.mock('@/hooks/projects', () => ({
  useProjectListQuery: jest.fn(),
  useProjectDetailQuery: jest.fn(),
  useCreateProjectMutation: jest.fn(),
  useUpdateProjectMutation: jest.fn(),
  useDeleteProjectMutation: jest.fn(),
  useToggleChecklistMutation: jest.fn(),
  useUploadProjectAttachmentMutation: jest.fn(),
  useDeleteProjectAttachmentMutation: jest.fn(),
  useGenerateProjectCodeMutation: jest.fn(),
  useProjectSummaryQuery: jest.fn(),
}));
jest.mock('@/hooks/employees', () => ({ useEmployeeListQuery: jest.fn() }));
jest.mock('@/hooks/customers', () => ({ useCustomerListQuery: jest.fn() }));

// radix UI mock (portal 우회)
jest.mock('@/components/ui/dialog', () => ({
  Dialog: ({ children, open }: any) => open ? <div data-testid="dialog">{children}</div> : null,
  DialogContent: ({ children }: any) => <div>{children}</div>,
  DialogHeader: ({ children }: any) => <div>{children}</div>,
  DialogTitle: ({ children }: any) => <h2>{children}</h2>,
  DialogFooter: ({ children }: any) => <div>{children}</div>,
}));
jest.mock('@/components/ui/alert-dialog', () => ({
  AlertDialog: ({ children, open }: any) => open ? <div data-testid="alert-dialog">{children}</div> : null,
  AlertDialogContent: ({ children }: any) => <div>{children}</div>,
  AlertDialogTitle: ({ children }: any) => <h2>{children}</h2>,
  AlertDialogDescription: ({ children }: any) => <p>{children}</p>,
  AlertDialogFooter: ({ children }: any) => <div>{children}</div>,
  AlertDialogCancel: ({ children, ...props }: any) => <button {...props}>{children}</button>,
  AlertDialogAction: ({ children, ...props }: any) => <button {...props}>{children}</button>,
}));
jest.mock('sonner', () => ({ toast: { success: jest.fn(), error: jest.fn() } }));
```

### 핵심 테스트 시나리오

#### API Route Tests (55~65 tests)

**GET /api/projects (12 tests)**
- 인증 검증 (미인증 시 401)
- 페이지네이션 (기본 page=1, page_size=20)
- customer_id 필터
- status 필터 (복수 선택)
- employee_id 필터
- keyword 검색 (프로젝트명, 코드 기반, 2자 이상)
- 정렬 (project_code, project_name, status, start_date)
- RBAC: USER → 본인 담당만
- RBAC: MANAGER → 부서 내만
- RBAC: ADMIN → 전체
- soft-deleted 제외 (deleted_at IS NULL)
- JOIN 결과 (customer_name, employee_name)

**POST /api/projects (8 tests)**
- 필수 필드 검증 (project_name, customer_id, employee_id 누락 시 400)
- project_name 200자 초과 거부
- 성공 등록 (201, status='PREPARING' 고정)
- project_code 형식 검증
- project_code 중복 검증 (409)
- project_code NULL 허용
- start_date > end_date 거부
- contract_amount 음수 거부

**GET /api/projects/[id] (5 tests)**
- 존재하는 프로젝트 상세 반환 (체크리스트 + 첨부파일 포함)
- 존재하지 않는 ID → 404
- soft-deleted 프로젝트 → 404
- RBAC 권한 검증 (타인 프로젝트 접근 제한)
- 관계 데이터 포함 (customer, employee, attachments)

**PUT /api/projects/[id] (8 tests)**
- 기본 정보 수정 (project_name, description 등)
- 상태 변경: PREPARING → IN_PROGRESS 허용
- 상태 변경: PREPARING → COMPLETED 거부 (400)
- ADMIN 모든 상태 전이 허용
- 소유권 검증 (403)
- MANAGER 부서 내 수정 허용
- updated_at 자동 갱신
- project_code 수정 시 중복 검증

**DELETE /api/projects/[id] (4 tests)**
- soft delete (deleted_at 설정)
- 소유권 검증 (403)
- ADMIN 삭제 허용
- 이미 삭제된 프로젝트 → 404

**PATCH /api/projects/[id]/checklist (6 tests)**
- 유효한 stage 토글 (completed=true → timestamp 기록)
- 완료 해제 (completed=false → null)
- 잘못된 stage 값 → 400
- completed boolean 타입 검증
- 소유권 검증 (403)
- 응답: { stage, completed_at } 반환

**POST /api/projects/[id]/attachments (5 tests)**
- 파일 업로드 성공 (201)
- 파일 크기 초과 거부 (10MB)
- 허용되지 않은 확장자 거부
- category 필수 검증
- category enum 검증 (잘못된 값 → 400)

**GET /api/projects/[id]/attachments/[attachmentId] (3 tests)**
- 파일 다운로드 성공
- 존재하지 않는 첨부파일 → 404
- Content-Disposition 헤더 설정

**DELETE /api/projects/[id]/attachments/[attachmentId] (3 tests)**
- 삭제 성공 (파일 시스템 + DB)
- 존재하지 않는 첨부파일 → 404
- 소유권 검증

**GET /api/projects/summary (5 tests)**
- 상태별 카운트 반환 (preparing, in_progress, completed, on_hold)
- RBAC 적용 카운트 (USER 본인 건만)
- customer_id 필터 적용
- employee_id 필터 적용
- soft-deleted 제외

**POST /api/projects/generate-code (3 tests)**
- 코드 생성 성공 (PJT-YYYYMMDD-NNN 형식)
- SEQUENCE 기반 NNN 생성
- 인증 검증

**GET /api/employees/list (3 tests)**
- 직원 목록 반환 (id, name, department_name)
- RBAC: MANAGER는 부서 내만, ADMIN은 전체
- soft-deleted 제외

---

#### Component Tests (55~65 tests)

**ProjectFilters (10 tests)**
- 렌더링 (고객사 Combobox, 상태 Select, 담당자 Select, 키워드 Input)
- keyword debounce 500ms
- keyword 2자 미만 미전송
- 고객사 Combobox 선택
- 상태 Select 복수 선택
- 담당자 Select 선택
- 필터 변경 시 onFilterChange 호출
- 초기화 버튼 동작
- 외부 params 동기화
- 필터 변경 시 page=1 리셋

**ProjectCreateDialog (16 tests)**
- Dialog 미표시 (open=false)
- Dialog 표시 (open=true)
- 필수 필드 에러 (project_name, customer_id, employee_id)
- project_name 200자 초과 에러
- 고객사 Combobox 동작
- 담당자 Select 동작
- 코드 생성 버튼 클릭
- 코드 수동 입력 형식 검증
- 코드 미입력 허용
- 날짜 범위 검증 (start > end)
- 계약 금액 콤마 포맷팅
- 저장 성공 (toast + close)
- 저장 실패 (toast.error)
- 저장 중 버튼 disabled
- 폼 초기화 (재열기 시)
- mutation에 올바른 데이터 전달

**ProjectDetailDialog (14 tests)**
- Dialog 미표시 (projectId=null)
- 상세 데이터 표시
- project_code NULL → "-"
- 상태 Badge 색상
- 허용된 상태 전이 옵션
- ADMIN 전체 전이 허용
- 상태 변경 호출
- 수정/삭제 버튼 소유자 표시
- 수정/삭제 버튼 비소유자 숨김
- MANAGER 부서 내 표시
- 삭제 확인 AlertDialog
- 삭제 성공 (toast + close)
- 로딩 Skeleton
- ProjectChecklist + ProjectAttachments 렌더링

**ProjectChecklist (10 tests)**
- 8단계 순서 표시
- 완료 단계: 체크 + timestamp
- 미완료 단계: 빈 체크박스
- 비순차 하이라이트 (이후 완료 + 현재 미완료)
- 하이라이트 미적용 (이후도 미완료)
- 체크박스 클릭 → toggleChecklist 호출
- optimistic update
- 전체 완료 시 toast
- canEdit=false → disabled
- 비순차 체크 허용 (순서 무관)

**ProjectAttachments (20 tests)**
- 첨부 개수 표시
- "+ 추가" 버튼 (canEdit=true)
- "+ 추가" 숨김 (canEdit=false)
- "첨부파일 없음" (목록 비어있을 때)
- 파일 목록 렌더링 (아이콘 + Badge + 파일명)
- 카테고리 Badge 색상 5종
- 다운로드 버튼 클릭
- 삭제 버튼 표시 (canEdit=true)
- 삭제 버튼 숨김 (canEdit=false)
- 업로드 영역 열기/닫기
- 파일 크기 초과 에러
- 확장자 에러
- 카테고리 미선택 에러
- 업로드 mutation 호출 (FormData)
- 업로드 성공 toast
- 업로드 실패 toast
- 업로드 중 disabled
- 삭제 AlertDialog 표시
- 삭제 confirm → mutation 호출
- 삭제 성공 toast

---

## Acceptance Criteria

- [ ] 전체 테스트 통과 (`npm run test`)
- [ ] 라인 커버리지 80%+
- [ ] 브랜치 커버리지 75%+
- [ ] API Route: RBAC 검증 테스트 포함 (USER/MANAGER/ADMIN)
- [ ] API Route: 상태 전이 규칙 테스트 포함
- [ ] API Route: 체크리스트 토글 테스트 포함
- [ ] API Route: 첨부파일 업로드/다운로드/삭제 테스트 포함
- [ ] API Route: 프로젝트 코드 생성 테스트 포함
- [ ] Components: 사용자 인터랙션 테스트 (userEvent)
- [ ] Components: RBAC 기반 UI 표시/숨김 테스트
- [ ] Components: 폼 유효성 검증 테스트
- [ ] Components: toast 알림 테스트
- [ ] 기존 테스트 깨지지 않음

---

## 테스트 전략

### 실행 명령어

```bash
# 전체 테스트
npm run test

# 프로젝트 관련만
npm run test -- --testPathPattern="projects|employees"

# 커버리지
npm run test -- --coverage --testPathPattern="projects|employees"
```

### Mock 설정 우선순위

1. next/server (NextRequest, NextResponse)
2. next-auth (getServerSession)
3. TypeORM (DataSource, Repository, QueryBuilder)
4. radix UI components (Dialog, AlertDialog, Select, Command)
5. sonner (toast)
6. next/navigation (useRouter, useSearchParams)
7. hooks (useProjectListQuery, useCreateProjectMutation, etc.)
8. next-auth/react (useSession)

### 커버리지 목표

| 영역 | 목표 | 핵심 포인트 |
|------|------|------------|
| API Routes | 85%+ | RBAC, 상태 전이, 검증 |
| Components | 80%+ | 사용자 인터랙션, 조건부 렌더링 |
| 전체 | 80%+ lines, 75%+ branches | - |

---

**다음 문서**: 없음 (마지막 문서)
