<!-- Generated: 2026-01-28 15:30:00 KST -->

# 시퀀스 및 인덱스 생성

**문서 번호**: 2101_02
**원본 PRD**: 2101_공지사항_prd_v2.md
**PRD 참조**: 원본 PRD의 'Section 6.3 Database - Indexes' 참조
**구현 범위**: 시퀀스 (ID 생성용) 및 성능 최적화 인덱스
**복잡도**: S (0.5~1일)
**의존성**: 2101_01 (테이블 생성)

---

## 구현 목표

공지사항 모듈의 쿼리 성능 최적화를 위한 인덱스를 생성합니다.
PRD에서 정의한 인덱스 전략을 적용합니다.

---

## 구현 내용

### 파일 구조

```
src/
└── migrations/
    └── 2101_002_create_notice_indexes.sql
```

### SQL 스크립트

```sql
-- ============================================================
-- 2101_002_create_notice_indexes.sql
-- 공지사항 모듈 인덱스 생성
-- Generated: 2026-01-28 15:30:00 KST
-- ============================================================

-- ============================================================
-- 1. NOTICE 테이블 인덱스
-- ============================================================

-- 1.1 유형별 + 날짜 필터링 (목록 페이지 기본 쿼리)
CREATE INDEX IDX_NOTICE_TYPE_CREATED ON NOTICE(TYPE, "created_at" DESC);

-- 1.2 작성자별 이력 (내 게시물 조회)
CREATE INDEX IDX_NOTICE_AUTHOR_CREATED ON NOTICE(AUTHOR_ID, "created_at" DESC);

-- 1.3 최신순 정렬 (기본 정렬)
CREATE INDEX IDX_NOTICE_CREATED ON NOTICE("created_at" DESC);

-- 1.4 조회수순 정렬 (인기 게시물)
CREATE INDEX IDX_NOTICE_VIEWCOUNT ON NOTICE(VIEW_COUNT DESC);

-- 1.5 Soft delete 쿼리 최적화 (모든 목록 쿼리에서 사용)
CREATE INDEX IDX_NOTICE_DELETED ON NOTICE("deleted_at");

-- 1.6 제목 검색 (LIKE '%keyword%' 최적화 제한적)
CREATE INDEX IDX_NOTICE_TITLE ON NOTICE(TITLE);

-- 1.7 복합 인덱스: soft delete + 날짜 (가장 빈번한 쿼리 패턴)
CREATE INDEX IDX_NOTICE_ACTIVE_CREATED ON NOTICE("deleted_at", "created_at" DESC);

-- ============================================================
-- 2. COMMENT 테이블 인덱스
-- ============================================================

-- 2.1 게시물별 댓글 조회 (oldest-first 정렬)
CREATE INDEX IDX_COMMENT_NOTICE_CREATED ON COMMENT(NOTICE_ID, "created_at" ASC);

-- 2.2 답글 트리 쿼리 (부모 댓글 + 시간순)
CREATE INDEX IDX_COMMENT_NOTICE_PARENT_CREATED ON COMMENT(NOTICE_ID, PARENT_COMMENT_ID, "created_at" ASC);

-- 2.3 작성자별 댓글 (내 댓글 조회)
CREATE INDEX IDX_COMMENT_AUTHOR ON COMMENT(AUTHOR_ID);

-- 2.4 부모 댓글 조회 (깊이 검증용)
CREATE INDEX IDX_COMMENT_PARENT ON COMMENT(PARENT_COMMENT_ID);

-- 2.5 Soft delete 쿼리 최적화
CREATE INDEX IDX_COMMENT_DELETED ON COMMENT("deleted_at");

-- 2.6 복합 인덱스: 게시물 + soft delete (댓글 목록 기본 쿼리)
CREATE INDEX IDX_COMMENT_NOTICE_ACTIVE ON COMMENT(NOTICE_ID, "deleted_at");

-- ============================================================
-- 3. NOTICE_ATTACHMENT 테이블 인덱스
-- ============================================================

-- 3.1 게시물별 첨부파일 조회
CREATE INDEX IDX_NOTICE_ATTACH_NOTICE ON NOTICE_ATTACHMENT(NOTICE_ID);

-- 3.2 Soft delete 쿼리 최적화
CREATE INDEX IDX_NOTICE_ATTACH_DELETED ON NOTICE_ATTACHMENT("deleted_at");

-- 3.3 복합 인덱스: 게시물 + soft delete
CREATE INDEX IDX_NOTICE_ATTACH_NOTICE_ACTIVE ON NOTICE_ATTACHMENT(NOTICE_ID, "deleted_at");
```

### 인덱스 설계 근거

| 인덱스 | 용도 | 쿼리 패턴 |
|--------|------|-----------|
| `IDX_NOTICE_TYPE_CREATED` | 유형별 필터 + 최신순 | `WHERE TYPE = ? AND deleted_at IS NULL ORDER BY created_at DESC` |
| `IDX_NOTICE_AUTHOR_CREATED` | 내 게시물 | `WHERE AUTHOR_ID = ? ORDER BY created_at DESC` |
| `IDX_NOTICE_VIEWCOUNT` | 조회수순 정렬 | `ORDER BY VIEW_COUNT DESC` |
| `IDX_NOTICE_ACTIVE_CREATED` | 기본 목록 | `WHERE deleted_at IS NULL ORDER BY created_at DESC` |
| `IDX_COMMENT_NOTICE_CREATED` | 댓글 목록 | `WHERE NOTICE_ID = ? ORDER BY created_at ASC` |
| `IDX_COMMENT_NOTICE_PARENT_CREATED` | 답글 트리 | `WHERE NOTICE_ID = ? AND PARENT_COMMENT_ID = ?` |

---

## Acceptance Criteria

- [ ] NOTICE 테이블 인덱스 7개 생성
- [ ] COMMENT 테이블 인덱스 6개 생성
- [ ] NOTICE_ATTACHMENT 테이블 인덱스 3개 생성
- [ ] 인덱스 생성 확인 쿼리 실행

---

## 테스트 전략

### 인덱스 확인

```sql
-- 생성된 인덱스 목록
SELECT index_name, table_name, uniqueness
FROM user_indexes
WHERE table_name IN ('NOTICE', 'COMMENT', 'NOTICE_ATTACHMENT')
ORDER BY table_name, index_name;

-- 인덱스 컬럼 확인
SELECT i.index_name, i.table_name, c.column_name, c.column_position
FROM user_indexes i
JOIN user_ind_columns c ON i.index_name = c.index_name
WHERE i.table_name IN ('NOTICE', 'COMMENT', 'NOTICE_ATTACHMENT')
ORDER BY i.table_name, i.index_name, c.column_position;
```

### 실행 계획 확인

```sql
-- 목록 조회 쿼리 실행 계획
EXPLAIN PLAN FOR
SELECT ID, TITLE, TYPE, AUTHOR_ID, VIEW_COUNT, "created_at"
FROM NOTICE
WHERE "deleted_at" IS NULL
  AND TYPE = '공지'
ORDER BY "created_at" DESC
FETCH FIRST 20 ROWS ONLY;

SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY);

-- 댓글 조회 쿼리 실행 계획
EXPLAIN PLAN FOR
SELECT ID, CONTENT, AUTHOR_ID, PARENT_COMMENT_ID, "created_at"
FROM COMMENT
WHERE NOTICE_ID = 1
  AND "deleted_at" IS NULL
ORDER BY "created_at" ASC;

SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY);
```

---

## 실행 방법

```bash
# SQL*Plus에서 실행
sqlplus sunjin_admin/<password>@localhost:1521/XEPDB1

@src/migrations/2101_002_create_notice_indexes.sql
```

---

## 완료 체크리스트

- [ ] SQL 스크립트 작성 완료
- [ ] 로컬 환경에서 실행 성공
- [ ] 스테이징 환경에서 실행 성공
- [ ] 인덱스 생성 확인 (16개)
- [ ] 실행 계획 확인 (인덱스 사용 여부)

---

**다음 문서**: 2101_03_TypeScript_타입_정의.md
