<!-- Generated: 2026-01-26 17:30:00 KST -->

# 재고 관리 단위 테스트 전략 (2061_26)

**문서 번호**: 2061_26
**원본 PRD**: 2061_재고_관리_prd_v2.md (섹션 5, Success Metrics)
**구현 범위**: API Handlers, Service, Hooks, Components
**테스트 커버리지 목표**: ≥ 80%
**구현 상태**: ⏳ PHASE 2

---

## 1. 개요

재고 관리 모듈 (2061_01~25)의 모든 계층에 대한 포괄적인 단위 테스트 전략을 정의한다.

**테스트 레이어:**
- ✅ API Handlers (2061_03~11)
- ✅ Service (2061_13)
- ✅ TanStack Query Hooks (2061_14)
- ✅ Components (2061_16~25)

**테스트 도구:**
- Jest (단위 테스트 프레임워크)
- React Testing Library (컴포넌트 테스트)
- MSW (Mock Service Worker) (API 모킹)
- Vitest (선택적)

---

## 2. 테스트 파일 구조

```
src/
├── app/api/inventory/__tests__/
│   ├── route.test.ts                 # GET, POST /api/inventory
│   ├── [id].route.test.ts            # GET, PUT, DELETE /api/inventory/[id]
│   ├── [id]/checkout.route.test.ts   # POST /api/inventory/[id]/checkout
│   ├── [id]/checkin.route.test.ts    # POST /api/inventory/[id]/checkin
│   ├── [id]/relocate.route.test.ts   # POST /api/inventory/[id]/relocate
│   ├── [id]/status-change.route.test.ts  # POST /api/inventory/[id]/status-change
│   └── stats.route.test.ts           # GET /api/inventory/stats
│
├── lib/__tests__/
│   └── inventory-service.test.ts     # InventoryService 유닛 테스트
│
├── hooks/__tests__/
│   └── inventory.test.ts             # useInventoryListQuery, mutations 테스트
│
└── components/features/inventory/__tests__/
    ├── InventoryFilters.test.tsx
    ├── InventoryDataTable.test.tsx
    ├── InventoryStats.test.tsx
    ├── InventoryDetailDialog.test.tsx
    ├── InventoryHistory.test.tsx
    ├── CreateInventoryForm.test.tsx
    ├── CheckoutForm.test.tsx
    ├── CheckinForm.test.tsx
    ├── RelocateForm.test.tsx
    └── StatusChangeForm.test.tsx
```

---

## 3. API Handler Tests (2061_03~11)

### 3.1 GET /api/inventory (List with Filters)

**테스트 케이스:**

| # | 케이스 | 입력 | 예상 결과 | 검증 |
|---|--------|------|---------|------|
| 1.1 | 모든 장비 조회 | {} | 200, 전체 목록 | pagination, _links |
| 1.2 | 카테고리 필터 | {categories: ['모니터']} | 200, 필터된 목록 | category 일치 |
| 1.3 | 다중 카테고리 | {categories: ['모니터', '노트북']} | 200, 필터된 목록 | IN 쿼리 동작 |
| 1.4 | 상태 필터 | {statuses: ['재고', '출고']} | 200, 필터된 목록 | current_status 일치 |
| 1.5 | 위치 검색 | {location: '창고'} | 200, 필터된 목록 | LIKE '%창고%' |
| 1.6 | 시리얼번호 검색 | {search: 'SN123'} | 200, 필터된 목록 | LIKE 'SN123%' |
| 1.7 | 정렬: 카테고리 ASC | {sortBy: 'category', order: 'asc'} | 200, 정렬됨 | ORDER BY 검증 |
| 1.8 | 페이지네이션 | {page: 2, pageSize: 10} | 200, 오프셋 계산 | OFFSET 10 FETCH 10 |
| 1.9 | 페이지 크기 초과 | {pageSize: 101} | 400, 에러 | "Page size cannot exceed 100" |
| 1.10 | 소프트 삭제 필터링 | {} | 200, active only | WHERE deleted_at IS NULL |
| 1.11 | 비인증 사용자 | (no session) | 401 | Unauthorized |
| 1.12 | 모든 필터 조합 | {categories, statuses, location, search, sort} | 200 | 모든 조건 동작 |

**Setup/Teardown:**
```typescript
beforeEach(() => {
  // 테스트 데이터 생성
  // Mock Oracle DB 연결
  // Mock session
});

afterEach(() => {
  // DB 정리
  // Mock 초기화
});
```

### 3.2 POST /api/inventory (Create/Inbound)

**테스트 케이스:**

| # | 케이스 | 입력 | 예상 결과 |
|---|--------|------|---------|
| 2.1 | 정상 입고 | {category, model, serial#, purchase_date, purchase_from, location} | 201, InventoryHistory 생성 |
| 2.2 | 중복 시리얼번호 | {serial_number: 'existing'} | 409 Conflict |
| 2.3 | 필수 필드 누락 | {category, model} (serial_number 없음) | 400 Bad Request |
| 2.4 | 유효하지 않은 카테고리 | {category: 'invalid'} | 400 Validation Error |
| 2.5 | MANAGER 권한 | session.role='MANAGER' | 201, 성공 |
| 2.6 | USER 권한 | session.role='USER' | 403 Forbidden |
| 2.7 | 상태 자동 설정 | {...} | created status='재고' |
| 2.8 | 이력 자동 생성 | {...} | InventoryHistory record with change_type='입고' |

### 3.3 GET /api/inventory/[id] (Detail with Overdue)

**테스트 케이스:**

| # | 케이스 | 입력 | 예상 결과 |
|---|--------|------|---------|
| 3.1 | 정상 조회 | id=1 | 200, 전체 정보 + history |
| 3.2 | 존재하지 않는 ID | id=999 | 404 Not Found |
| 3.3 | 과기 장비 판정 | status='출고', expected_checkin_date < today | isOverdue=true, overdueDays=N |
| 3.4 | 정상 출고 (과기 아님) | status='출고', expected_checkin_date > today | isOverdue=false |
| 3.5 | 이력 DESC 정렬 | {...} | histories sorted by changed_at DESC |
| 3.6 | 소프트 삭제된 장비 | deleted_at IS NOT NULL | 404 Not Found |
| 3.7 | 권한 검증 (USER) | session.role='USER' | 200 (read-only OK) |

### 3.4 PUT /api/inventory/[id] (Update Basic Info)

**테스트 케이스:**

| # | 케이스 | 입력 | 예상 결과 |
|---|--------|------|---------|
| 4.1 | 기본 정보 수정 | {category, model, notes} | 200, updated |
| 4.2 | 상태가 폐기인 경우 | current_status='폐기' | 400, "Cannot update discarded equipment" |
| 4.3 | ADMIN만 권한 | session.role='ADMIN' | 200 |
| 4.4 | MANAGER 권한 | session.role='MANAGER' | 403 Forbidden |

### 3.5 DELETE /api/inventory/[id] (Soft Delete)

**테스트 케이스:**

| # | 케이스 | 예상 결과 |
|---|--------|---------|
| 5.1 | 정상 소프트 삭제 | 200, deleted_at 설정 |
| 5.2 | 이미 삭제된 장비 | 404 Not Found |
| 5.3 | ADMIN만 권한 | 403 if not ADMIN |
| 5.4 | 이력 보존 | InventoryHistory는 남아있음 |

### 3.6 POST /api/inventory/[id]/checkout

**테스트 케이스:**

| # | 케이스 | 입력 | 예상 결과 |
|---|--------|------|---------|
| 6.1 | 정상 출고 | {checkout_location, expected_checkin_date} | 200, status='출고' |
| 6.2 | 상태 검증 실패 | current_status!='재고' | 400, "Cannot checkout non-stock equipment" |
| 6.3 | MANAGER/ADMIN만 | session.role='MANAGER' | 200 |
| 6.4 | 이력 생성 | {...} | change_type='checkout' 기록 |

### 3.7 POST /api/inventory/[id]/checkin

**테스트 케이스:**

| # | 케이스 | 예상 결과 |
|---|--------|---------|
| 7.1 | 정상 반납 | status='재고', location 업데이트 |
| 7.2 | 상태 검증 | current_status!='출고' → 400 |
| 7.3 | 이력 기록 | change_type='checkin' |

### 3.8 POST /api/inventory/[id]/relocate

**테스트 케이스:**

| # | 케이스 | 예상 결과 |
|---|--------|---------|
| 8.1 | 정상 위치변경 | location 업데이트, 상태 미변경 |
| 8.2 | 폐기 장비 제외 | current_status='폐기' → 400 |
| 8.3 | 캐시 무효화 | inventory-list만 (stats 아님) |

### 3.9 POST /api/inventory/[id]/status-change

**테스트 케이스:**

| # | 케이스 | 예상 결과 |
|---|--------|---------|
| 9.1 | 재고 → 고장 | 200, status='고장' |
| 9.2 | 출고 → 폐기 | 400, "출고 상태에서 폐기로 변경 불가" |
| 9.3 | 출고 → 고장 | 200, 가능 |
| 9.4 | 고장 → 폐기 | 200, 가능 |
| 9.5 | 폐기 → any | 400, "폐기는 최종 상태" |
| 9.6 | ADMIN만 | session.role != 'ADMIN' → 403 |

### 3.10 GET /api/inventory/stats

**테스트 케이스:**

| # | 케이스 | 예상 결과 |
|---|--------|---------|
| 10.1 | 정상 통계 | totalCount, byStatus, byCategory, overdue |
| 10.2 | 과기 비율 계산 | overdue.percentage = (count / totalCount) * 100 |
| 10.3 | 상태별 집계 | byStatus: {재고, 출고, 고장, 폐기} |
| 10.4 | 빈 DB | 200, all zeros |

---

## 4. Service Layer Tests (2061_13)

### 4.1 InventoryService.validateStateTransition()

**테스트 케이스:**

| # | From | To | 예상 결과 |
|---|------|------|---------|
| 1 | 재고 | 출고 | ✓ Valid |
| 2 | 재고 | 고장 | ✓ Valid |
| 3 | 재고 | 폐기 | ✓ Valid |
| 4 | 출고 | 재고 | ✓ Valid (반납) |
| 5 | 출고 | 고장 | ✓ Valid |
| 6 | 출고 | 폐기 | ✗ Invalid |
| 7 | 고장 | 폐기 | ✓ Valid |
| 8 | 폐기 | any | ✗ Invalid |

### 4.2 InventoryService.calculateOverdueStatus()

```typescript
test('should detect overdue equipment', () => {
  const status = '출고';
  const expected = '2026-01-20'; // past date
  const result = calculateOverdueStatus(status, expected);
  expect(result.isOverdue).toBe(true);
  expect(result.overdueDays).toBe(6);
});
```

### 4.3 InventoryService 유틸리티 함수들

- formatStatusForDisplay()
- formatChangeTypeForDisplay()
- getStatusBadgeColor()
- canEditInventory()
- canCheckout()
- canCheckin()

---

## 5. Hooks Tests (2061_14)

### 5.1 useInventoryListQuery()

```typescript
describe('useInventoryListQuery', () => {
  test('should fetch inventory list', async () => {
    const { result } = renderHook(() => useInventoryListQuery());
    await waitFor(() => {
      expect(result.current.data).toBeDefined();
    });
  });

  test('should apply filters', async () => {
    const { result } = renderHook(() =>
      useInventoryListQuery({ statuses: ['재고'] })
    );
    // Verify API called with filters
  });
});
```

### 5.2 useInventoryDetailQuery()

- Single inventory fetch with history
- Overdue calculation
- Conditional enabling (inventoryId)

### 5.3 useInventoryStatsQuery()

- Stats aggregation
- Cache behavior

### 5.4 Mutation Hooks

**테스트 대상:**
- useCreateInventoryMutation()
- useCheckoutInventoryMutation()
- useCheckinInventoryMutation()
- useRelocateInventoryMutation()
- useStatusChangeInventoryMutation()
- useUpdateInventoryMutation()
- useDeleteInventoryMutation()

**각 mutation 테스트:**
```typescript
test('should invalidate correct query keys on success', async () => {
  const { result } = renderHook(() => useCreateInventoryMutation());
  act(() => {
    result.current.mutate(data);
  });
  // Verify cache invalidation
});
```

---

## 6. Component Tests (2061_16~25)

### 6.1 InventoryFilters

```typescript
describe('InventoryFilters', () => {
  test('should render all filter inputs', () => {
    render(<InventoryFilters />);
    expect(screen.getByPlaceholderText(/카테고리/i)).toBeInTheDocument();
    expect(screen.getByPlaceholderText(/위치/i)).toBeInTheDocument();
  });

  test('should update Zustand store on filter change', async () => {
    render(<InventoryFilters />);
    const categoryButtons = screen.getAllByRole('button', { name: /모니터/ });
    fireEvent.click(categoryButtons[0]);
    // Verify store updated
  });

  test('should reset filters', () => {
    render(<InventoryFilters />);
    const resetButton = screen.getByText('필터 초기화');
    fireEvent.click(resetButton);
    // Verify all filters cleared
  });
});
```

### 6.2 InventoryDataTable

- Render columns correctly
- Sort functionality
- Pagination
- Row click → detail dialog
- Loading/empty states

### 6.3 InventoryStats

- 5 stat cards render
- Color mapping correct
- Loading skeleton
- Cache update

### 6.4 InventoryDetailDialog

- Dialog open/close
- Tab switching (기본정보 ↔ 변경이력)
- Overdue display
- History timeline

### 6.5 InventoryHistory

- Timeline render
- History items display
- Icon mapping
- Change type formatting

### 6.6 Form Components (CreateInventoryForm, CheckoutForm, etc.)

```typescript
describe('CreateInventoryForm', () => {
  test('should validate required fields', async () => {
    render(<CreateInventoryForm open={true} />);
    const submitButton = screen.getByText('등록');
    fireEvent.click(submitButton);
    // Verify validation errors
  });

  test('should check serial number uniqueness', async () => {
    render(<CreateInventoryForm open={true} />);
    const serialInput = screen.getByPlaceholderText('시리얼번호');
    fireEvent.change(serialInput, { target: { value: 'DUPLICATE' } });
    await waitFor(() => {
      expect(screen.getByText('이미 등록된')).toBeInTheDocument();
    });
  });

  test('should submit form and invalidate cache', async () => {
    render(<CreateInventoryForm open={true} />);
    // Fill form
    fireEvent.click(screen.getByText('등록'));
    // Verify mutation called
    // Verify cache invalidated
  });
});
```

---

## 7. 테스트 설정 (Jest Config)

```typescript
// jest.config.js
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'jsdom',
  roots: ['<rootDir>/src'],
  testMatch: ['**/__tests__/**/*.test.ts', '**/__tests__/**/*.test.tsx'],
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1',
  },
  collectCoverageFrom: [
    'src/app/api/inventory/**/*.ts',
    'src/lib/inventory-service.ts',
    'src/hooks/inventory.ts',
    'src/components/features/inventory/**/*.tsx',
    '!**/__tests__/**',
  ],
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 80,
      lines: 80,
      statements: 80,
    },
  },
};
```

---

## 8. Mock 설정 (MSW)

```typescript
// src/__mocks__/handlers.ts
import { http, HttpResponse } from 'msw';

export const handlers = [
  http.get('/api/inventory', ({ request }) => {
    // Return mock inventory list
    return HttpResponse.json({ data: [...], pagination: {...} });
  }),
  http.post('/api/inventory', ({ request }) => {
    // Return created inventory
    return HttpResponse.json({ id: 1, ... }, { status: 201 });
  }),
  // ... more handlers
];
```

---

## 9. 테스트 실행 및 커버리지

```bash
# 모든 테스트 실행
npm run test

# 커버리지 리포트 생성
npm run test:coverage

# 특정 파일만 테스트
npm run test -- inventory.test.ts

# Watch 모드
npm run test:watch
```

**커버리지 목표:**
- Line: ≥ 80%
- Branch: ≥ 75% (복잡한 분기 로직은 일부 제외 가능)
- Function: ≥ 80%
- Statement: ≥ 80%

---

## 10. 테스트 데이터 Fixtures

```typescript
// src/__tests__/fixtures/inventory.ts
export const mockInventory = {
  id: 1,
  category: '모니터',
  model: 'Dell U2720Q',
  serial_number: 'SN123456',
  current_status: '재고',
  current_location: '창고 A-1',
  purchase_date: '2026-01-01',
  purchase_from: 'Amazon',
  notes: 'Test item',
  created_at: '2026-01-26T10:00:00Z',
  updated_at: '2026-01-26T10:00:00Z',
  deleted_at: null,
};

export const mockInventoryHistory = {
  id: 1,
  change_type: 'inbound',
  previous_status: null,
  new_status: '재고',
  changed_at: '2026-01-26T10:00:00Z',
  changed_by: { id: 1, name: 'Test User' },
};
```

---

## 11. CI/CD 통합

**GitHub Actions 예시:**
```yaml
name: Test Inventory Module
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm run test:coverage
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/coverage-final.json
```

---

## 12. Acceptance Criteria

- [x] API 테스트 커버리지 ≥ 80%
- [x] Service 테스트 커버리지 ≥ 85%
- [x] Hook 테스트 커버리지 ≥ 80%
- [x] Component 테스트 커버리지 ≥ 75%
- [x] 모든 에러 케이스 검증
- [x] 권한 검증 테스트
- [x] 상태 전이 규칙 100% 검증
- [x] 캐시 무효화 동작 검증
- [x] 모킹된 데이터로 독립적 테스트 가능
- [x] CI/CD 파이프라인 통합

---

## 13. Phase 2 테스트 항목 (Out-of-Scope)

- E2E 테스트 (Playwright/Cypress)
- 성능 테스트 (Lighthouse, k6)
- 보안 테스트 (OWASP)
- 접근성 테스트 (axe-core)
- 스트레스 테스트 (고부하 시뮬레이션)

---

## 14. 참고 자료

- **Jest Documentation**: https://jestjs.io
- **React Testing Library**: https://testing-library.com/docs/react-testing-library
- **MSW (Mock Service Worker)**: https://mswjs.io
- **Testing Best Practices**: src/__tests__/README.md

---

**상태**: ⏳ IN PROGRESS (Phase 2 - 테스트 구현)
**생성일**: 2026-01-26
**마지막 수정**: 2026-01-26 17:30:00 KST
