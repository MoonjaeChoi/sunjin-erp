<!-- Generated: 2026-01-26 22:45:00 KST -->

# 저장소 및 서비스 레이어 (Repository + Service)

**문서 번호**: 2071_03
**원본 PRD**: `docs/prd/2071_유지보수_고객_관리_prd_v2.md`
**운영 표준**: `docs/operation/011_데이터베이스연결.md` (QueryRunner transaction pattern, Soft delete queries, Repository pattern with IsNull())
**구현 범위**: Repository + Service 클래스, 상수 정의
**복잡도**: M (1-2일)
**의존성**: 2071_01~02 (Entity + Migration)

---

## 구현 목표

TypeORM Repository 패턴을 사용하여 데이터 액세스 레이어를 구현합니다. Soft delete를 강제하고 비즈니스 로직은 Service 레이어에서 처리합니다.

---

## 구현 내용

### 파일 구조

```
src/
├── lib/
│   ├── maintenance-repository.ts      # Repository 패턴
│   ├── maintenance-service.ts         # 비즈니스 로직
│   └── maintenance-constants.ts       # 상수 (상태, 유형)
```

### QueryRunner 트랜잭션 패턴 (docs/operation/011 참조)

```typescript
// Correct pattern
const queryRunner = dataSource.createQueryRunner();
try {
  await queryRunner.startTransaction();

  // All database operations via queryRunner
  const result = await queryRunner.query('SELECT ...');
  await queryRunner.query('UPDATE ...');

  await queryRunner.commitTransaction();
} catch (error) {
  await queryRunner.rollbackTransaction();
  throw error;
} finally {
  await queryRunner.release();  // CRITICAL: Must release
}
```

### Soft Delete 쿼리 (docs/operation/011 참조)

```typescript
// ✅ Correct: IsNull()
.where({ deleted_at: IsNull() })

// ❌ Avoid: Raw SQL in TypeORM QueryBuilder
// .where('deleted_at IS NULL')
```

### 1. Repository 클래스

**파일**: `src/lib/maintenance-repository.ts`

```typescript
// Generated: 2026-01-26 22:45:00 KST

import { DataSource, Repository, IsNull } from 'typeorm';
import { MaintenanceContract } from '@/entities/MaintenanceContract';
import { MaintenanceContractAttachment } from '@/entities/MaintenanceContractAttachment';
import { MaintenanceContractHistory } from '@/entities/MaintenanceContractHistory';

export class MaintenanceContractRepository {
  private contractRepo: Repository<MaintenanceContract>;
  private attachmentRepo: Repository<MaintenanceContractAttachment>;
  private historyRepo: Repository<MaintenanceContractHistory>;

  constructor(dataSource: DataSource) {
    this.contractRepo = dataSource.getRepository(MaintenanceContract);
    this.attachmentRepo = dataSource.getRepository(MaintenanceContractAttachment);
    this.historyRepo = dataSource.getRepository(MaintenanceContractHistory);
  }

  // Soft delete filtering on all queries
  async findActiveContracts(
    filters: { status?: string; customerId?: number } = {},
    pagination: { page: number; limit: number } = { page: 1, limit: 20 }
  ) {
    let query = this.contractRepo
      .createQueryBuilder('mc')
      .leftJoinAndSelect('mc.customer', 'customer')
      .where({ deleted_at: IsNull() });  // Soft delete filter

    if (filters.status) {
      query = query.andWhere('mc.contract_status = :status', { status: filters.status });
    }

    const offset = (pagination.page - 1) * pagination.limit;
    return query.skip(offset).take(pagination.limit).getManyAndCount();
  }

  async findById(id: number) {
    return this.contractRepo
      .createQueryBuilder('mc')
      .leftJoinAndSelect('mc.customer', 'customer')
      .where('mc.id = :id AND mc.deleted_at IS NULL', { id })
      .getOne();
  }

  async create(data: Partial<MaintenanceContract>) {
    const contract = this.contractRepo.create(data);
    return this.contractRepo.save(contract);
  }

  async update(id: number, data: Partial<MaintenanceContract>) {
    await this.contractRepo.update(
      { id, deleted_at: IsNull() },  // Only update active records
      data
    );
    return this.findById(id);
  }

  async softDeleteContract(id: number) {
    // Cascade soft delete
    await this.contractRepo.update(
      { id, deleted_at: IsNull() },
      { deleted_at: new Date() }
    );
    await this.attachmentRepo.update(
      { maintenance_contract_id: id, deleted_at: IsNull() },
      { deleted_at: new Date() }
    );
    await this.historyRepo.update(
      { maintenance_contract_id: id, deleted_at: IsNull() },
      { deleted_at: new Date() }
    );
  }
}
```

### 2. Service 클래스

**파일**: `src/lib/maintenance-service.ts`

```typescript
// Generated: 2026-01-26 22:45:00 KST

import { DataSource } from 'typeorm';
import { MaintenanceContractRepository } from './maintenance-repository';
import { MaintenanceContract } from '@/entities/MaintenanceContract';

export class MaintenanceContractService {
  private repository: MaintenanceContractRepository;

  constructor(private dataSource: DataSource) {
    this.repository = new MaintenanceContractRepository(dataSource);
  }

  async createContract(data: Partial<MaintenanceContract>, userId: number) {
    const queryRunner = this.dataSource.createQueryRunner();
    await queryRunner.startTransaction();

    try {
      const contract = await this.repository.create({
        ...data,
        created_by_id: userId,
        updated_by_id: userId,
        contract_status: '활성',
      });

      // Create history record (트랜잭션 내에서)
      await queryRunner.query(
        `INSERT INTO MAINTENANCE_CONTRACT_HISTORY
        (maintenance_contract_id, change_type, reason, changed_by_id, changed_at)
        VALUES (:contractId, '정보수정', '계약 생성', :userId, CURRENT_TIMESTAMP)`,
        { contractId: contract.id, userId }
      );

      await queryRunner.commitTransaction();
      return contract;
    } catch (error) {
      await queryRunner.rollbackTransaction();
      throw error;
    } finally {
      await queryRunner.release();
    }
  }

  async deleteContract(id: number) {
    const queryRunner = this.dataSource.createQueryRunner();
    await queryRunner.startTransaction();

    try {
      await this.repository.softDeleteContract(id);
      await queryRunner.commitTransaction();
    } catch (error) {
      await queryRunner.rollbackTransaction();
      throw error;
    } finally {
      await queryRunner.release();
    }
  }

  async getContracts(filters: any = {}, pagination: any = {}) {
    return this.repository.findActiveContracts(filters, pagination);
  }
}
```

### 3. 상수 정의

**파일**: `src/lib/maintenance-constants.ts`

```typescript
// Generated: 2026-01-26 22:45:00 KST

export const CONTRACT_STATUS = {
  ACTIVE: '활성',
  TERMINATED: '종료',
  RENEWAL_PENDING: '갱신예정',
} as const;

export const CHANGE_TYPE = {
  RENEWAL: '갱신',
  STATUS_CHANGE: '상태변경',
  INFO_UPDATE: '정보수정',
} as const;

export const FILE_ALLOWED_TYPES = ['application/pdf', 'application/msword'];
export const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB
export const MAX_FILES_PER_CONTRACT = 5;
```

---

## Acceptance Criteria

- [ ] Repository 클래스 구현 완료 (Soft delete 필터링)
- [ ] Service 클래스 구현 완료 (비즈니스 로직, 트랜잭션)
- [ ] 상수 정의 완료
- [ ] TypeScript strict mode 통과

---

**다음 문서**: `2071_04_DB_검증_테스트.md`
