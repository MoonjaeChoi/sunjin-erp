<!-- Generated: 2026-01-25 21:30:00 KST -->

# Inventory + InventoryHistory Entity 정의

**문서 번호**: 2051_01
**원본 PRD**: 2051_재고_관리_prd_v2.md
**구현 범위**: Inventory, InventoryHistory TypeORM Entity 정의
**복잡도**: M
**의존성**: 없음 (선행 작업)

---

## 구현 목표

재고 관리 모듈의 핵심 데이터 모델인 Inventory와 InventoryHistory Entity를 TypeORM 기반으로 정의한다. Oracle XE 21c 타입 규칙(VARCHAR2, NUMBER, CLOB, TIMESTAMP)을 준수하며, 상태 관리(재고/출고/고장/폐기), 위치 추적, 불변 감사 추적(audit trail)을 포함한다.

---

## 구현 내용

### 파일 구조

```
src/
├── entities/
│   ├── Inventory.ts              # Inventory Entity 정의
│   └── InventoryHistory.ts       # InventoryHistory Entity 정의
```

### 구현 상세

#### 1. Inventory Entity (`src/entities/Inventory.ts`)

```typescript
import {
  Entity,
  PrimaryGeneratedColumn,
  Column,
  CreateDateColumn,
  UpdateDateColumn,
  DeleteDateColumn,
  ManyToOne,
  JoinColumn,
  OneToMany,
  Check,
} from 'typeorm';
import { Employee } from './Employee';
import { InventoryHistory } from './InventoryHistory';

export type InventoryStatus = '재고' | '출고' | '고장' | '폐기';

@Entity('INVENTORY')
@Check(`"CURRENT_STATUS" IN ('재고', '출고', '고장', '폐기')`)
export class Inventory {
  @PrimaryGeneratedColumn({ name: 'id', type: 'number' })
  id!: number;

  @Column({ name: 'category', type: 'varchar2', length: 50, nullable: false })
  category!: string;
  // 예: '모니터', '노트북', '라우터', '프린터', '기타'

  @Column({ name: 'model', type: 'varchar2', length: 255, nullable: false })
  model!: string;

  @Column({
    name: 'serial_number',
    type: 'varchar2',
    length: 100,
    nullable: false,
    unique: true,
  })
  serial_number!: string;

  @Column({ name: 'purchase_date', type: 'date', nullable: false })
  purchase_date!: Date;

  @Column({ name: 'purchase_from', type: 'varchar2', length: 255, nullable: false })
  purchase_from!: string;

  @Column({
    name: 'current_location',
    type: 'varchar2',
    length: 255,
    nullable: false,
  })
  current_location!: string;
  // 물리적 보관 위치 (창고, 사무실 등)

  @Column({
    name: 'current_status',
    type: 'varchar2',
    length: 20,
    nullable: false,
  })
  current_status!: InventoryStatus;
  // 기본값: '재고'

  @Column({ name: 'notes', type: 'clob', nullable: true })
  notes!: string | null;

  @Column({ name: 'created_by_id', type: 'number', nullable: false })
  created_by_id!: number;

  @Column({ name: 'updated_by_id', type: 'number', nullable: false })
  updated_by_id!: number;

  @CreateDateColumn({
    name: 'created_at',
    type: 'timestamp',
    default: () => 'CURRENT_TIMESTAMP',
  })
  created_at!: Date;

  @UpdateDateColumn({
    name: 'updated_at',
    type: 'timestamp',
    default: () => 'CURRENT_TIMESTAMP',
    onUpdate: 'CURRENT_TIMESTAMP',
  })
  updated_at!: Date;

  @DeleteDateColumn({ name: 'deleted_at', type: 'timestamp', nullable: true })
  deleted_at!: Date | null;

  // Relations
  @ManyToOne(() => Employee)
  @JoinColumn({ name: 'created_by_id' })
  created_by!: Employee;

  @ManyToOne(() => Employee)
  @JoinColumn({ name: 'updated_by_id' })
  updated_by!: Employee;

  @OneToMany(() => InventoryHistory, (history) => history.inventory, {
    lazy: true,
  })
  histories!: InventoryHistory[];
}
```

#### 2. InventoryHistory Entity (`src/entities/InventoryHistory.ts`)

```typescript
import {
  Entity,
  PrimaryGeneratedColumn,
  Column,
  CreateDateColumn,
  ManyToOne,
  JoinColumn,
} from 'typeorm';
import { Inventory } from './Inventory';
import { Employee } from './Employee';

export type ChangeType = '입고' | '출고' | '반납' | '위치변경' | '상태변경';

@Entity('INVENTORY_HISTORY')
export class InventoryHistory {
  @PrimaryGeneratedColumn({ name: 'id', type: 'number' })
  id!: number;

  @Column({ name: 'inventory_id', type: 'number', nullable: false })
  inventory_id!: number;

  @Column({ name: 'change_type', type: 'varchar2', length: 20, nullable: false })
  change_type!: ChangeType;

  @Column({
    name: 'previous_location',
    type: 'varchar2',
    length: 255,
    nullable: true,
  })
  previous_location!: string | null;

  @Column({ name: 'new_location', type: 'varchar2', length: 255, nullable: true })
  new_location!: string | null;

  @Column({
    name: 'previous_status',
    type: 'varchar2',
    length: 20,
    nullable: true,
  })
  previous_status!: string | null;

  @Column({ name: 'new_status', type: 'varchar2', length: 20, nullable: true })
  new_status!: string | null;

  @Column({
    name: 'checkout_location',
    type: 'varchar2',
    length: 255,
    nullable: true,
  })
  checkout_location!: string | null;
  // 출고 시만 기록 (논리적 사용처: 프로젝트, 사람 등)

  @Column({ name: 'expected_checkin_date', type: 'date', nullable: true })
  expected_checkin_date!: Date | null;

  @Column({ name: 'reason', type: 'varchar2', length: 500, nullable: true })
  reason!: string | null;

  @Column({ name: 'changed_by_id', type: 'number', nullable: false })
  changed_by_id!: number;

  @CreateDateColumn({
    name: 'changed_at',
    type: 'timestamp',
    default: () => 'CURRENT_TIMESTAMP',
  })
  changed_at!: Date;

  // Relations
  @ManyToOne(() => Inventory, (inv) => inv.histories, {
    onDelete: 'RESTRICT',
  })
  @JoinColumn({ name: 'inventory_id' })
  inventory!: Inventory;

  @ManyToOne(() => Employee)
  @JoinColumn({ name: 'changed_by_id' })
  changed_by!: Employee;
}
```

### 핵심 설계 결정

#### 1. 상태 관리 (Current Status)
- 값: `'재고' | '출고' | '고장' | '폐기'`
- CHECK 제약: Oracle CHECK 조건
- 기본값: `'재고'` (입고 시)
- 상태 전이 규칙은 애플리케이션 레벨에서 검증

#### 2. 위치 개념 분리
- **current_location**: 물리적 보관 위치 (Inventory 엔티티에만)
- **checkout_location**: 논리적 사용처 (InventoryHistory에만 기록)

#### 3. 이력 불변성 (Immutability)
- InventoryHistory는 `deleted_at` 컬럼 없음
- 감사 목적으로 영구 보존
- 부모 삭제 후에도 조회 가능 (ON DELETE RESTRICT)

#### 4. 감사 필드 (Audit Trail)
- Inventory: created_by_id, updated_by_id (작업 수행자 추적)
- InventoryHistory: changed_by_id (변경 수행자)
- created_at, updated_at, changed_at (시간 추적)

---

## Acceptance Criteria

- [ ] Inventory Entity 파일 생성 완료 (`src/entities/Inventory.ts`)
- [ ] InventoryHistory Entity 파일 생성 완료 (`src/entities/InventoryHistory.ts`)
- [ ] Oracle 타입 규칙 준수 (VARCHAR2, NUMBER, CLOB, TIMESTAMP)
- [ ] InventoryStatus type 정의 ('재고', '출고', '고장', '폐기')
- [ ] ChangeType type 정의 ('입고', '출고', '반납', '위치변경', '상태변경')
- [ ] current_location 필드 (물리적 위치)
- [ ] checkout_location 필드는 InventoryHistory에만 (논리적 사용처)
- [ ] InventoryHistory에는 deleted_at 컬럼 없음 (불변 audit trail)
- [ ] ManyToOne/OneToMany 관계 설정 (Employee, Inventory)
- [ ] ON DELETE RESTRICT 적용 (FK 제약)
- [ ] CreateDateColumn, UpdateDateColumn, DeleteDateColumn 설정
- [ ] `npm run type-check` 통과
- [ ] `npm run build` 성공

---

## 테스트 전략

### TypeScript & Lint

```bash
npm run type-check
npm run build
npm run lint
```

### 검증 방법

1. Entity 파일 작성 후 TypeScript 빌드 성공 확인
2. TypeORM decorator 문법 오류 없음 확인
3. 관계 설정 양방향 참조 정확성 확인
4. 후속 Migration(2051_02)에서 실제 테이블 생성 시 검증

---

**다음 문서**: 2051_02_Migration_생성.md
