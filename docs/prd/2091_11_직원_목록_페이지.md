<!-- Generated: 2026-01-28 05:00:00 KST -->

# 직원 목록 페이지

**문서 번호**: 2091_11
**원본 PRD**: 2091_직원_관리_prd_v2.md
**PRD 참조**: 원본 PRD의 'Section 6 UI/UX Considerations', 'US-5, US-6' 참조
**구현 범위**: 직원 목록 페이지 및 EmployeeList 컴포넌트
**복잡도**: M (1-2일)
**의존성**: 2091_10

---

## 구현 목표

직원 목록 페이지와 EmployeeList 컴포넌트를 구현합니다. ADMIN은 전체 직원, MANAGER는 자부서 직원만 조회 가능합니다.

---

## 구현 내용

### 파일 구조

```
src/
├── app/(main)/
│   └── employees/
│       └── page.tsx                    # 직원 목록 페이지 (SC)
└── components/features/employees/
    ├── EmployeeList.tsx                # 직원 목록 컴포넌트 (CC)
    └── EmployeeFilters.tsx             # 필터 컴포넌트 (CC)
```

### 페이지 구성 요소

1. **검색 입력** - 이름/이메일 검색
2. **부서 필터** - 드롭다운 (ADMIN만)
3. **직급 필터** - 드롭다운
4. **활성 상태 필터** - 활성/비활성 (ADMIN만)
5. **신규 등록 버튼** - ADMIN만 표시
6. **직원 테이블** - DataTable 컴포넌트
7. **페이지네이션** - 이전/다음 버튼

### 핵심 컴포넌트

```typescript
// src/components/features/employees/EmployeeList.tsx

'use client';

import { useState } from 'react';
import Link from 'next/link';
import { useEmployees, useDepartments, usePositions } from '@/hooks/useEmployees';
import { DataTable } from '@/components/ui/data-table';
import { Input } from '@/components/ui/input';
import { Select } from '@/components/ui/select';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Plus, Search } from 'lucide-react';

interface EmployeeListProps {
  userRole: 'ADMIN' | 'MANAGER';
  userDepartmentId?: number;
}

export function EmployeeList({ userRole, userDepartmentId }: EmployeeListProps) {
  const [page, setPage] = useState(1);
  const [search, setSearch] = useState('');
  const [departmentId, setDepartmentId] = useState<number | undefined>(
    userRole === 'MANAGER' ? userDepartmentId : undefined
  );
  const [positionId, setPositionId] = useState<number>();
  const [isActive, setIsActive] = useState<boolean>();

  const { data: departments } = useDepartments();
  const { data: positions } = usePositions();
  const { data, isLoading, error } = useEmployees({
    page,
    limit: 20,
    search: search || undefined,
    departmentId,
    positionId,
    isActive,
    sortBy: 'name',
    sortOrder: 'ASC',
  });

  // ... 컴포넌트 렌더링 로직
}
```

---

## Acceptance Criteria

- [ ] 직원 목록 테이블 표시 (이름, 부서, 직급, 이메일, 입사일)
- [ ] 검색 기능 (이름, 이메일)
- [ ] 부서/직급/활성상태 필터링
- [ ] 페이지네이션
- [ ] ADMIN: 전체 직원, 신규 등록 버튼 표시
- [ ] MANAGER: 자부서만, 신규 등록 버튼 숨김
- [ ] 행 클릭 시 상세 페이지 이동
- [ ] 로딩/에러 상태 처리

---

## 완료 체크리스트

- [ ] EmployeeList 컴포넌트 구현
- [ ] EmployeeFilters 컴포넌트 구현
- [ ] 목록 페이지 구현
- [ ] RBAC 처리
- [ ] TypeScript 빌드 성공

---

**다음 문서**: 2091_12_직원_등록_수정_폼.md
