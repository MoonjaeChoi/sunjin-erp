<!-- Generated: 2026-01-26 21:30:00 KST -->

# 코딩 표준 검증

**문서 번호**: 2071_21
**원본 PRD**: `docs/prd/2071_유지보수_고객_관리_prd_v2.md`
**개발 표준**: `CLAUDE.md` (TypeScript strict, ESLint, Prettier, SC/CC 분리), `docs/standard/FRONTEND_CODING_STANDARDS_IMPLEMENTATION_CHECKLIST.md`
**구현 범위**: 모든 구현 항목의 코딩 표준 검증 및 최종 확인
**복잡도**: M (1-2일)
**의존성**: 2071_01~20 (모든 구현 완료)

---

## 구현 목표

다음의 코딩 표준을 검증합니다:
- TypeScript strict mode 호환성
- ESLint 규칙 준수
- Prettier 포맷팅
- SC/CC 분리 패턴
- RBAC 권한 검증
- Soft delete 적용
- Error handling
- 테스트 커버리지

---

## 구현 내용

### 1. TypeScript 검증

```bash
# 빌드 및 타입 검증
npm run build                  # Next.js 빌드 (TypeScript 포함)
npm run type-check             # TypeScript strict mode 검증

# 예상 출력
✓ src/entities/MaintenanceContract.ts
✓ src/app/api/maintenance/route.ts
✓ src/components/features/maintenance/MaintenanceList.tsx
✓ src/hooks/useMaintenanceContracts.ts
...
```

### 2. ESLint & Prettier 검증

```bash
# ESLint 검사
npm run lint                   # ESLint 검사 실행
npm run lint -- --fix          # 자동 수정 가능한 부분 수정

# Prettier 포맷팅
npm run format                 # Prettier 포맷팅 적용

# 검증 결과
✓ No eslint errors found
✓ No prettier formatting issues
```

### 3. SC/CC 분리 패턴 검증

#### ❌ 잘못된 예

```typescript
// app/(main)/maintenance/page.tsx - CC에서 useState 사용 (위반)
'use client';
export default function MaintenancePage() {
  const [filters, setFilters] = useState(); // ❌ SC여야 함
}

// components/MaintenanceList.tsx - CC에서 직접 fetch (위반)
async function fetchContracts() { // ❌ async는 SC에서만
  const res = await fetch('/api/maintenance');
}
```

#### ✅ 올바른 예

```typescript
// app/(main)/maintenance/page.tsx (SC)
export default function MaintenancePage() {
  return <MaintenanceListWrapper />;
}

// components/MaintenanceListWrapper.tsx (CC)
'use client';
export function MaintenanceListWrapper() {
  const [filters, setFilters] = useState();
  const { data } = useMaintenanceContracts(filters);
  return <MaintenanceList data={data} />;
}

// components/MaintenanceList.tsx (CC)
export function MaintenanceList({ data }) {
  return <Table data={data} />;
}
```

### 4. RBAC 권한 검증

#### 서버 레이어 (API)

```typescript
// src/app/api/maintenance/route.ts
export async function GET(req: NextRequest) {
  const session = await getServerSession();
  if (!session) return new Response('Unauthorized', { status: 401 });

  // USER+ 권한 체크 (모두 가능)
  // ...
}

export async function POST(req: NextRequest) {
  const session = await getServerSession();
  if (session?.user?.role !== 'MANAGER' && session?.user?.role !== 'ADMIN') {
    return new Response('Forbidden', { status: 403 });
  }

  // MANAGER+ 권한 체크
  // ...
}
```

#### 클라이언트 레이어 (UI)

```typescript
// components/MaintenanceList.tsx
'use client';
export function MaintenanceList() {
  const session = useSession();

  // MANAGER만 "신규 등록" 버튼 표시
  {session?.user?.role === 'MANAGER' && (
    <Button onClick={...}>신규 등록</Button>
  )}

  // ADMIN만 "삭제" 버튼 표시
  {session?.user?.role === 'ADMIN' && (
    <Button variant="destructive">삭제</Button>
  )}
}
```

### 5. Soft Delete 검증

#### ❌ 잘못된 예

```typescript
// 물리적 삭제 (위반)
await dataSource.query(`DELETE FROM MAINTENANCE_CONTRACT WHERE id = :id`);
```

#### ✅ 올바른 예

```typescript
// Soft delete (올바름)
await dataSource.query(`
  UPDATE MAINTENANCE_CONTRACT SET deleted_at = CURRENT_TIMESTAMP WHERE id = :id
`);

// Cascade soft delete
await dataSource.query(`
  UPDATE MAINTENANCE_CONTRACT_ATTACHMENT SET deleted_at = CURRENT_TIMESTAMP
  WHERE maintenance_contract_id = :id
`);
```

### 6. 에러 처리 검증

```typescript
// API error handling
export async function POST(req: NextRequest) {
  try {
    const body = await req.json();

    // ✅ 입력 검증
    if (!body.customer_id) {
      return Response.json({ error: 'customer_id required' }, { status: 400 });
    }

    // ✅ DB 쿼리
    const result = await dataSource.query(...);

    // ✅ 정상 응답
    return Response.json({ id: result[0].id }, { status: 201 });
  } catch (error) {
    // ✅ 에러 로깅
    console.error('Failed to create contract:', error);

    // ✅ 500 응답
    return Response.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}
```

### 7. 테스트 커버리지 검증

```bash
# 커버리지 리포트 생성
npm run test:coverage -- --testPathPattern=maintenance

# 목표: 라인 80%+, 브랜치 75%+
TOTAL
  Lines: 1,020/1,200 (85%)
  Branches: 480/600 (80%)
  Functions: 95/110 (86%)
  Statements: 1,050/1,200 (87.5%)
```

### 8. 완전한 검증 체크리스트

```bash
#!/bin/bash

echo "=== TypeScript 검증 ==="
npm run type-check || exit 1
echo "✓ TypeScript 검증 통과"

echo "=== Build 검증 ==="
npm run build || exit 1
echo "✓ Build 성공"

echo "=== ESLint 검증 ==="
npm run lint || exit 1
echo "✓ ESLint 통과"

echo "=== Prettier 검증 ==="
npm run format --check || exit 1
echo "✓ Prettier 통과"

echo "=== 테스트 검증 ==="
npm run test -- --testPathPattern=maintenance || exit 1
echo "✓ 모든 테스트 통과"

echo "✅ 모든 코딩 표준 검증 완료!"
```

---

## Acceptance Criteria

- [ ] npm run build 성공 (TypeScript 검증)
- [ ] npm run type-check 통과 (strict mode)
- [ ] npm run lint 통과 (ESLint)
- [ ] npm run format 통과 (Prettier)
- [ ] npm run test 모두 통과
- [ ] 테스트 커버리지 80%+ (라인)
- [ ] SC/CC 분리 패턴 준수
- [ ] RBAC 검증 완료 (3단계: API/UI/요청)
- [ ] Soft delete 적용
- [ ] 에러 처리 완성

---

## 검증 항목별 상세

### TypeScript Strict Mode

- [ ] 모든 함수에 return type 지정
- [ ] 모든 변수에 타입 지정
- [ ] as never 사용 최소화
- [ ] unknown/any 타입 금지
- [ ] null/undefined 처리

### ESLint 규칙

- [ ] no-unused-vars (미사용 변수 없음)
- [ ] no-console (console 문 검토)
- [ ] prefer-const (const/let 올바름)
- [ ] eqeqeq (==  대신 ===)
- [ ] no-implicit-coercion (타입 강제 변환 금지)

### Accessibility

- [ ] 모든 버튼에 aria-label (필요시)
- [ ] 입력 필드에 label
- [ ] 색상만으로 정보 전달 금지
- [ ] 충분한 색상 대비
- [ ] 포커스 표시 가능

### Performance

- [ ] 불필요한 re-render 없음
- [ ] useMemo/useCallback 적절히 사용
- [ ] 번들 크기 확인
- [ ] 이미지 최적화 (next/image)
- [ ] 동적 import 사용 (필요시)

---

## 완료 체크리스트

- [ ] npm run build 성공
- [ ] npm run type-check 통과
- [ ] npm run lint 통과
- [ ] npm run format 통과
- [ ] npm run test 모두 통과
- [ ] 커버리지 80% 이상
- [ ] SC/CC 분리 검증 완료
- [ ] RBAC 3단계 검증 완료
- [ ] Soft delete 검증 완료
- [ ] 에러 처리 검증 완료
- [ ] 접근성 검증 완료
- [ ] 성능 검증 완료

---

**다음 문서**: `2071_22_단위_테스트.md`
