<!-- Generated: 2026-01-25 21:30:00 KST -->

# Component Implementation (2061_16~20)

**문서 번호**: 2061_16~20
**원본 PRD**: 2061_재고_관리_prd_v2.md
**복잡도**: M (각각)
**의존성**: 2061_12, 2061_14

---

## 개요

다음 5개 컴포넌트는 재고 목록 페이지와 상세 페이지의 UI를 구성한다:

1. **2061_16**: InventoryFilters (필터 패널)
2. **2061_17**: InventoryTable (목록 테이블)
3. **2061_18**: InventoryStats (통계 패널)
4. **2061_19**: 재고 상세 페이지 (InventoryDetailView)
5. **2061_20**: InventoryHistory (타임라인)

각 컴포넌트는 다음 사항을 포함해야 한다:
- Client Component 선언 ('use client')
- shadcn/ui 컴포넌트 사용
- TypeScript 타입 안정성
- 권한 기반 렌더링 (userRole)
- Responsive design
- 접근성 지원

---

## 공통 구현 가이드

### 파일 위치

```
src/components/features/inventory/
├── InventoryFilters.tsx
├── InventoryTable.tsx
├── InventoryStats.tsx
├── InventoryDetailView.tsx
└── InventoryHistory.tsx
```

### shadcn/ui 컴포넌트 활용

| 컴포넌트 | 용도 |
|---------|------|
| Button | 액션 버튼 (필터, 입고, 출고 등) |
| Input | 텍스트 입력 (검색, 위치) |
| Select | 드롭다운 (카테고리, 정렬) |
| Checkbox | 다중 선택 (상태 필터) |
| Badge | 상태 표시 |
| Table | 목록 렌더링 |
| Card | 통계 패널 |
| Dialog | 모달 폼 |
| Toast | 성공/에러 메시지 |
| Skeleton | 로딩 UI |

### Tailwind CSS 클래스

- 색상: green (재고), blue (출고), amber (고장), gray (폐기)
- 반응형: md:, lg:, xl: breakpoints
- 스페이싱: p-6, gap-4, space-y-6

---

## 2061_16: InventoryFilters 컴포넌트

**위치**: `src/components/features/inventory/InventoryFilters.tsx`

### Props

```typescript
interface InventoryFiltersProps {
  filters: InventoryFilters;
  onFilterChange: (filters: Partial<InventoryFilters>) => void;
}
```

### 기능

- 카테고리 다중 선택 (체크박스)
- 상태 다중 선택 (재고, 출고, 고장, 폐기)
- 위치 자유 텍스트 검색
- 시리얼번호/모델명 검색
- 정렬 컬럼 선택 (카테고리, 모델, 시리얼, 위치, 상태, 구매일)
- 정렬 순서 (오름차순/내림차순)
- "초기화" 버튼 (필터 리셋)

### Acceptance Criteria

- [ ] 모든 필터 입력 렌더링
- [ ] 필터 변경 시 onFilterChange 호출
- [ ] "초기화" 버튼으로 기본값 복원
- [ ] Responsive layout
- [ ] 접근성 (ARIA labels)

---

## 2061_17: InventoryTable 컴포넌트

**위치**: `src/components/features/inventory/InventoryTable.tsx`

### Props

```typescript
interface InventoryTableProps {
  data: InventoryListItem[];
  loading: boolean;
  pagination?: {
    page: number;
    pageSize: number;
    total: number;
    totalPages: number;
  };
  onPageChange: (page: number) => void;
  onPageSizeChange: (pageSize: number) => void;
  userRole: string;
}
```

### 기능

- 테이블 헤더 (ID, 카테고리, 모델, 시리얼번호, 위치, 상태, 구매일)
- 상태 배지 (색상 + 아이콘)
- 행 클릭 → 상세 페이지 이동
- 페이지네이션 (이전/다음/페이지 선택)
- 페이지 크기 선택 (20, 50, 100)
- 로딩 스켈레톤
- 빈 상태 (No data)

### Acceptance Criteria

- [ ] 모든 컬럼 렌더링
- [ ] 상태별 색상/아이콘 정확성
- [ ] 행 클릭 상세 페이지 네비게이션
- [ ] 페이지네이션 기능
- [ ] 로딩 상태 처리
- [ ] 빈 상태 메시지

---

## 2061_18: InventoryStats 컴포넌트

**위치**: `src/components/features/inventory/InventoryStats.tsx`

### Props

```typescript
interface InventoryStatsProps {
  data: InventoryStatistics;
}
```

### 기능

- 총 수량 카드
- 상태별 수량 (재고, 출고, 고장, 폐기) 4개 카드
- 카테고리별 상태 분포 차트 (Bar or Pie)
- 갱신 시간 표시

### 라이브러리

- recharts (Bar/Pie chart)

### Acceptance Criteria

- [ ] 총 수량 표시
- [ ] 상태별 수량 카드 4개
- [ ] 차트 렌더링
- [ ] 갱신 시간 표시 (ISO 8601)
- [ ] Responsive grid layout

---

## 2061_19: 재고 상세 페이지 + InventoryDetailView

**위치**:
- `src/app/(main)/inventory/[id]/page.tsx` (Server Component)
- `src/components/features/inventory/InventoryDetailView.tsx` (Client Component)

### 기능

- 기본 정보 섹션 (ID, 카테고리, 모델, 시리얼번호, 구매일, 구매처, 위치, 상태)
- 과기 판정 표시 (상태=출고 AND 기대 반납일 초과 시 빨간색으로 "과기 (N일)" 표시)
- 액션 버튼 (출고, 반납, 위치변경, 상태변경 - 권한/상태에 따라 활성화)
- 이력 타임라인 (InventoryHistory 컴포넌트)

### Acceptance Criteria

- [ ] 기본 정보 섹션 렌더링
- [ ] 과기 판정 정확성
- [ ] 액션 버튼 권한 검증
- [ ] 상태에 따른 버튼 활성화/비활성화
- [ ] 이력 타임라인 렌더링
- [ ] 모달 폼 표시 (출고/반납/위치변경/상태변경)

---

## 2061_20: InventoryHistory 타임라인 컴포넌트

**위치**: `src/components/features/inventory/InventoryHistory.tsx`

### Props

```typescript
interface InventoryHistoryProps {
  histories: InventoryHistoryItem[];
}
```

### 기능

- 시간순 타임라인 (최신순 역순)
- 각 항목: 변경 유형, 이전/새 위치/상태, 담당자, 변경일시, 비고
- 변경 유형별 아이콘 (입고, 출고, 반납, 위치변경, 상태변경)
- 담당자 정보 표시

### Acceptance Criteria

- [ ] 타임라인 구조 (세로 선)
- [ ] 모든 이력 항목 표시
- [ ] 변경 유형별 아이콘
- [ ] 담당자 정보
- [ ] 날짜/시간 포맷 (readable format)

---

## 공통 Acceptance Criteria

- [ ] 모든 컴포넌트 'use client' 선언
- [ ] shadcn/ui 컴포넌트 정확히 사용
- [ ] TypeScript 타입 안정성
- [ ] userRole 기반 권한 검증
- [ ] Responsive design (md, lg, xl breakpoints)
- [ ] 접근성 (ARIA labels, semantic HTML)
- [ ] 에러 처리 (fallback UI)
- [ ] 로딩 상태 (skeleton, spinner)
- [ ] Lucide React 아이콘 사용

---

**각 컴포넌트의 상세 구현은 별도 스펙을 참조하세요.**

