<!-- Generated: 2026-01-26 21:30:00 KST -->

# 폼 컴포넌트

**문서 번호**: 2071_17
**원본 PRD**: `docs/prd/2071_유지보수_고객_관리_prd_v2.md`
**구현 범위**: 계약 생성/수정 폼 컴포넌트
**복잡도**: M (1-2일)
**의존성**: 2071_11~13 (타입 + hooks)

---

## 구현 목표

React Hook Form + Zod을 사용한 폼 컴포넌트를 구현합니다:
- 고객 선택 (자동완성)
- 기간 선택 (날짜 유효성)
- 계약 정보 입력 (명, 유형, 금액)
- 담당자 선택
- 실시간 유효성 검증
- 에러 메시지 표시
- 제출 로딩 상태

---

## 구현 내용

### 파일 구조

```
src/components/features/maintenance/
├── MaintenanceForm.tsx              # 생성/수정 폼 (CC)
├── CustomerSelect.tsx               # 고객 선택 드롭다운 (CC)
├── DateRangeInput.tsx               # 기간 선택 (CC)
├── EmployeeSelect.tsx               # 담당자 선택 드롭다운 (CC)
└── __tests__/
    ├── MaintenanceForm.test.tsx
    └── fixtures.ts
```

### 1. MaintenanceForm.tsx - 생성/수정 폼 (CC)

```typescript
// Generated: 2026-01-26 HH:MM:SS KST

'use client';

import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { MaintenanceFormInput, maintenanceFormSchema } from '@/types/maintenance';
import { useCreateMaintenanceContract, useUpdateMaintenanceContract } from '@/hooks/useMaintenanceContractMutation';
import { useRouter } from 'next/navigation';
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage
} from '@/components/ui/form';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import { Loader2 } from 'lucide-react';

interface MaintenanceFormProps {
  mode: 'create' | 'edit';
  contractId?: number;
  initialData?: MaintenanceFormInput;
}

/**
 * 계약 생성/수정 폼
 */
export default function MaintenanceForm({
  mode,
  contractId,
  initialData
}: MaintenanceFormProps) {
  const router = useRouter();
  const createMutation = useCreateMaintenanceContract();
  const updateMutation = useUpdateMaintenanceContract(contractId || 0);

  const isLoading = createMutation.isPending || updateMutation.isPending;

  const form = useForm<MaintenanceFormInput>({
    resolver: zodResolver(maintenanceFormSchema),
    defaultValues: initialData || {
      customer_id: 0,
      contract_name: '',
      contract_type: '',
      start_date: '',
      end_date: '',
      assigned_employee_id: 0,
      contract_amount: undefined,
      notes: ''
    }
  });

  async function onSubmit(data: MaintenanceFormInput) {
    try {
      if (mode === 'create') {
        await createMutation.mutateAsync(data);
        router.push('/maintenance');
      } else if (contractId) {
        await updateMutation.mutateAsync(data);
        router.push(`/maintenance/${contractId}`);
      }
    } catch (error) {
      console.error('Form submission error:', error);
    }
  }

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">
        {/* 고객 선택 */}
        <FormField
          control={form.control}
          name="customer_id"
          render={({ field }) => (
            <FormItem>
              <FormLabel>고객 *</FormLabel>
              <FormControl>
                <CustomerSelect
                  value={field.value}
                  onChange={field.onChange}
                  disabled={mode === 'edit'}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {/* 계약명 */}
        <FormField
          control={form.control}
          name="contract_name"
          render={({ field }) => (
            <FormItem>
              <FormLabel>계약명 *</FormLabel>
              <FormControl>
                <Input placeholder="계약명 입력" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {/* 계약 유형 */}
        <FormField
          control={form.control}
          name="contract_type"
          render={({ field }) => (
            <FormItem>
              <FormLabel>계약 유형 *</FormLabel>
              <FormControl>
                <Input placeholder="계약 유형 입력" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {/* 기간 선택 */}
        <FormField
          control={form.control}
          name="start_date"
          render={({ field }) => (
            <FormItem>
              <FormLabel>시작일 *</FormLabel>
              <FormControl>
                <Input type="date" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="end_date"
          render={({ field }) => (
            <FormItem>
              <FormLabel>종료일 *</FormLabel>
              <FormControl>
                <Input type="date" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {/* 계약 금액 */}
        <FormField
          control={form.control}
          name="contract_amount"
          render={({ field }) => (
            <FormItem>
              <FormLabel>계약 금액</FormLabel>
              <FormControl>
                <Input
                  type="number"
                  placeholder="금액 입력"
                  {...field}
                  value={field.value || ''}
                  onChange={(e) => field.onChange(e.target.value ? parseInt(e.target.value) : undefined)}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {/* 담당자 선택 */}
        <FormField
          control={form.control}
          name="assigned_employee_id"
          render={({ field }) => (
            <FormItem>
              <FormLabel>담당자 *</FormLabel>
              <FormControl>
                <EmployeeSelect
                  value={field.value}
                  onChange={field.onChange}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {/* 노트 */}
        <FormField
          control={form.control}
          name="notes"
          render={({ field }) => (
            <FormItem>
              <FormLabel>노트</FormLabel>
              <FormControl>
                <textarea
                  placeholder="추가 정보"
                  className="w-full p-2 border rounded text-sm"
                  rows={4}
                  {...field}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {/* 버튼 */}
        <div className="flex gap-2 justify-end pt-4">
          <Button
            type="button"
            variant="outline"
            onClick={() => router.back()}
            disabled={isLoading}
          >
            취소
          </Button>
          <Button type="submit" disabled={isLoading}>
            {isLoading && <Loader2 className="w-4 h-4 mr-2 animate-spin" />}
            {mode === 'create' ? '등록' : '수정'}
          </Button>
        </div>
      </form>
    </Form>
  );
}
```

### 2. CustomerSelect.tsx - 고객 선택 드롭다운 (CC)

```typescript
// Generated: 2026-01-26 HH:MM:SS KST

'use client';

import { useState, useCallback } from 'react';
import { useQuery } from '@tanstack/react-query';
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue
} from '@/components/ui/select';

interface CustomerSelectProps {
  value: number;
  onChange: (value: number) => void;
  disabled?: boolean;
}

/**
 * 고객 선택 드롭다운
 */
export default function CustomerSelect({
  value,
  onChange,
  disabled
}: CustomerSelectProps) {
  const [search, setSearch] = useState('');

  const { data: customers } = useQuery({
    queryKey: ['customers', 'list'],
    queryFn: async () => {
      const res = await fetch('/api/customers');
      return res.json();
    }
  });

  const filtered = customers?.data?.filter((c: any) =>
    c.name.toLowerCase().includes(search.toLowerCase())
  ) || [];

  return (
    <Select value={value.toString()} onValueChange={(v) => onChange(parseInt(v))}>
      <SelectTrigger disabled={disabled}>
        <SelectValue placeholder="고객 선택" />
      </SelectTrigger>
      <SelectContent>
        {filtered.map((customer: any) => (
          <SelectItem key={customer.id} value={customer.id.toString()}>
            {customer.name}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  );
}
```

### 3. EmployeeSelect.tsx - 담당자 선택 드롭다운 (CC)

```typescript
// Generated: 2026-01-26 HH:MM:SS KST

'use client';

import { useQuery } from '@tanstack/react-query';
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue
} from '@/components/ui/select';

interface EmployeeSelectProps {
  value: number;
  onChange: (value: number) => void;
}

/**
 * 담당자 선택 드롭다운
 */
export default function EmployeeSelect({
  value,
  onChange
}: EmployeeSelectProps) {
  const { data: employees } = useQuery({
    queryKey: ['employees', 'list'],
    queryFn: async () => {
      const res = await fetch('/api/employees');
      return res.json();
    }
  });

  return (
    <Select value={value.toString()} onValueChange={(v) => onChange(parseInt(v))}>
      <SelectTrigger>
        <SelectValue placeholder="담당자 선택" />
      </SelectTrigger>
      <SelectContent>
        {employees?.data?.map((emp: any) => (
          <SelectItem key={emp.id} value={emp.id.toString()}>
            {emp.name}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  );
}
```

---

## Acceptance Criteria

- [ ] MaintenanceForm.tsx 구현 (모든 필드)
- [ ] CustomerSelect.tsx 구현
- [ ] EmployeeSelect.tsx 구현
- [ ] React Hook Form + Zod 통합
- [ ] 실시간 유효성 검증
- [ ] 에러 메시지 표시
- [ ] 제출 로딩 상태
- [ ] 취소 버튼 (뒤로 가기)
- [ ] npm run build 성공

---

## 완료 체크리스트

- [ ] MaintenanceForm.tsx 작성
- [ ] CustomerSelect.tsx 작성
- [ ] EmployeeSelect.tsx 작성
- [ ] Component 테스트 작성
- [ ] npm run build 성공

---

**다음 문서**: `2071_18_모달_다이얼로그.md`
