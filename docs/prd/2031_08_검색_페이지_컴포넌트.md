<!-- Generated: 2026-01-25 05:10:00 KST -->

# 검색 페이지 + TechSupportClient

**문서 번호**: 2031_08
**원본 PRD**: 2031_기술지원_관리_prd_v2.md
**PRD 참조**: 원본 PRD의 'Section 5.1 — App Router Structure' 참조
**구현 범위**: page.tsx (SC) + TechSupportClient.tsx (CC) — URL 상태 관리
**복잡도**: S
**의존성**: 2031_06, 2031_07

---

## 구현 목표

기술지원 목록 페이지를 SC/CC 분리 패턴으로 구현한다. URL searchParams 기반 상태 관리를 TechSupportClient에서 처리하고, 하위 컴포넌트들을 통합한다.

---

## 구현 내용

### 파일 구조

```
src/app/(main)/support/
└── page.tsx                              # Server Component
src/components/features/support/
└── TechSupportClient.tsx                 # Client Component (메인 컨테이너)
```

### 구현 상세

#### page.tsx (Server Component)

- `export const metadata` 정의
- `<Suspense>` wrapping
- TechSupportClient 렌더링

#### TechSupportClient.tsx (Client Component)

- `'use client'` 선언
- `useSearchParams()` + `useRouter()` 조합으로 URL 기반 상태 관리
- URL params → TechSupportSearchParams 변환
- 하위 컴포넌트 통합: Filters, DataTable, CreateDialog, DetailDialog
- `onUpdate(partial)` 핸들러: params를 머지하여 URL 업데이트

### 핵심 인터페이스

```typescript
// src/app/(main)/support/page.tsx
import { Suspense } from 'react';
import { TechSupportClient } from '@/components/features/support/TechSupportClient';

export const metadata = { title: '기술지원 관리 | 선진ERP' };

export default function SupportPage() {
  return (
    <Suspense fallback={<div className="animate-pulse" />}>
      <TechSupportClient />
    </Suspense>
  );
}
```

```typescript
// src/components/features/support/TechSupportClient.tsx
'use client';

import { useSearchParams, useRouter } from 'next/navigation';
import { useTechSupportSearchQuery } from '@/hooks/support';
import { TechSupportSearchParams } from '@/types/tech-support';

// URL → params 변환
function parseSearchParams(sp: URLSearchParams): TechSupportSearchParams {
  const today = new Date();
  const monthAgo = new Date(today);
  monthAgo.setMonth(monthAgo.getMonth() - 1);

  return {
    date_from: sp.get('date_from') || monthAgo.toISOString().split('T')[0],
    date_to: sp.get('date_to') || today.toISOString().split('T')[0],
    customer_id: sp.get('customer_id') ? Number(sp.get('customer_id')) : undefined,
    support_type: sp.get('support_type') as SupportType | undefined,
    support_method: sp.get('support_method') as SupportMethod | undefined,
    status: sp.get('status') as SupportStatus | undefined,
    keyword: sp.get('keyword') || undefined,
    page: Number(sp.get('page') || '1'),
    page_size: Number(sp.get('page_size') || '20'),
    sort_by: sp.get('sort_by') || 'support_date',
    sort_order: (sp.get('sort_order') || 'DESC') as 'ASC' | 'DESC',
  };
}

export function TechSupportClient() {
  const searchParams = useSearchParams();
  const router = useRouter();
  const params = parseSearchParams(searchParams);

  const { data, isLoading, isPlaceholderData } = useTechSupportSearchQuery(params);

  // 상세 Dialog용 ID
  const detailId = searchParams.get('detail') ? Number(searchParams.get('detail')) : null;
  // 등록 Dialog 열기
  const isCreateOpen = searchParams.get('create') === 'true';

  const handleUpdate = (partial: Partial<TechSupportSearchParams> & { detail?: number | null; create?: boolean }) => {
    const newParams = new URLSearchParams(searchParams.toString());

    if ('detail' in partial) {
      if (partial.detail === null) newParams.delete('detail');
      else newParams.set('detail', String(partial.detail));
    }
    if ('create' in partial) {
      if (!partial.create) newParams.delete('create');
      else newParams.set('create', 'true');
    }

    // 검색 params 업데이트
    Object.entries(partial).forEach(([key, value]) => {
      if (key === 'detail' || key === 'create') return;
      if (value === undefined || value === '') newParams.delete(key);
      else newParams.set(key, String(value));
    });

    router.push(`/support?${newParams.toString()}`, { scroll: false });
  };

  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">기술지원 관리</h1>
        <Button onClick={() => handleUpdate({ create: true })}>등록</Button>
      </div>

      <TechSupportFilters params={params} onUpdate={handleUpdate} />
      <TechSupportDataTable
        data={data}
        isLoading={isLoading}
        isPlaceholderData={isPlaceholderData}
        params={params}
        onUpdate={handleUpdate}
      />

      <TechSupportCreateDialog
        open={isCreateOpen}
        onClose={() => handleUpdate({ create: false })}
      />
      <TechSupportDetailDialog
        supportId={detailId}
        onClose={() => handleUpdate({ detail: null })}
      />
    </div>
  );
}
```

---

## Acceptance Criteria

- [ ] page.tsx: metadata export 정상
- [ ] page.tsx: Suspense + TechSupportClient 렌더링
- [ ] TechSupportClient: URL params 파싱 (기본값 적용)
- [ ] TechSupportClient: onUpdate로 URL 업데이트 (router.push)
- [ ] TechSupportClient: detail param으로 상세 Dialog 제어
- [ ] TechSupportClient: create param으로 등록 Dialog 제어
- [ ] TechSupportClient: useTechSupportSearchQuery 연동
- [ ] `npm run build` 성공

---

## 완료 체크리스트

- [ ] TypeScript 빌드 성공
- [ ] ESLint/Prettier 통과
- [ ] SC/CC 분리 패턴 준수
- [ ] URL 기반 상태 동기화 확인
- [ ] 기본 날짜 범위 (최근 1개월)

---

**다음 문서**: 2031_09_TechSupportFilters_컴포넌트.md
