<!-- Generated: 2026-01-28 05:00:00 KST -->

# API - 직원 이력 조회

**문서 번호**: 2091_09
**원본 PRD**: 2091_직원_관리_prd_v2.md
**PRD 참조**: 원본 PRD의 'Section 5.2 API Route Handlers', 'US-9' 참조
**구현 범위**: `/api/employees/[id]/history` 엔드포인트 (GET)
**복잡도**: S (0.5-1일)
**의존성**: 2091_08

---

## 구현 목표

직원 정보 변경 이력을 조회하는 API를 구현합니다. 변경자 정보가 ON DELETE RESTRICT로 보존되므로 항상 조회 가능합니다(Decision #3).

---

## 구현 내용

### 파일 구조

```
src/app/api/
└── employees/
    └── [id]/
        └── history/
            └── route.ts     # GET (history list)
```

### API 명세

| Method | Path | 설명 | 권한 |
|--------|------|------|------|
| GET | `/api/employees/[id]/history` | 변경 이력 조회 | ADMIN |

### 구현 상세

#### GET /api/employees/[id]/history - 이력 조회

```typescript
// src/app/api/employees/[id]/history/route.ts

import { NextRequest, NextResponse } from 'next/server';
import { getServerSession } from 'next-auth';
import { authOptions } from '@/lib/auth';
import { executeQuery } from '@/lib/db-direct';

export const dynamic = 'force-dynamic';

export async function GET(
  req: NextRequest,
  { params }: { params: Promise<{ id: string }> }
): Promise<NextResponse> {
  try {
    const { id } = await params;
    const employeeId = parseInt(id, 10);

    if (isNaN(employeeId)) {
      return NextResponse.json({ message: 'Invalid employee ID' }, { status: 400 });
    }

    // 1. 인증/권한 검증
    const session = await getServerSession(authOptions);
    if (!session?.user) {
      return NextResponse.json({ message: 'Unauthorized' }, { status: 401 });
    }

    const user = session.user as any;
    if (user.role !== 'ADMIN') {
      return NextResponse.json({ message: 'Forbidden' }, { status: 403 });
    }

    // 2. 직원 존재 확인
    const empExists = await executeQuery(
      `SELECT "id" FROM EMPLOYEE WHERE "id" = :id`,
      { id: employeeId }
    );

    if ((empExists.rows || []).length === 0) {
      return NextResponse.json({ message: '직원을 찾을 수 없습니다.' }, { status: 404 });
    }

    // 3. 쿼리 파라미터
    const searchParams = req.nextUrl.searchParams;
    const page = Math.max(1, parseInt(searchParams.get('page') || '1', 10));
    const limit = Math.min(100, Math.max(1, parseInt(searchParams.get('limit') || '20', 10)));
    const changeType = searchParams.get('changeType');

    // 4. WHERE 절 구성
    const whereClauses = ['h.EMPLOYEE_ID = :employeeId'];
    const queryParams: any = {
      employeeId,
      offset: (page - 1) * limit,
      pageSize: limit,
    };

    if (changeType && ['CREATE', 'UPDATE', 'DELETE', 'DEACTIVATE'].includes(changeType)) {
      whereClauses.push('h.CHANGE_TYPE = :changeType');
      queryParams.changeType = changeType;
    }

    // 5. 이력 조회
    const listQuery = `
      SELECT
        h."id" as ID,
        h.EMPLOYEE_ID as EMPLOYEEID,
        h.CHANGE_TYPE as CHANGETYPE,
        h.CHANGED_FIELDS as CHANGEDFIELDS,
        h.CHANGED_BY_ID as CHANGEDBYID,
        e."name" as CHANGEDBYNAME,
        h.CHANGED_AT as CHANGEDAT
       FROM EMPLOYEE_HISTORY h
       LEFT JOIN EMPLOYEE e ON h.CHANGED_BY_ID = e."id"
       WHERE ${whereClauses.join(' AND ')}
       ORDER BY h.CHANGED_AT DESC
       OFFSET :offset ROWS FETCH NEXT :pageSize ROWS ONLY
    `;

    const countQuery = `
      SELECT COUNT(*) as TOTAL
      FROM EMPLOYEE_HISTORY h
      WHERE ${whereClauses.join(' AND ')}
    `;

    const [listResult, countResult] = await Promise.all([
      executeQuery<any>(listQuery, queryParams),
      executeQuery<{ TOTAL: number }>(countQuery, queryParams),
    ]);

    // 6. 응답 변환
    const history = (listResult.rows || []).map(row => {
      let changedFields = {};
      try {
        changedFields = row.CHANGEDFIELDS ? JSON.parse(row.CHANGEDFIELDS) : {};
      } catch (e) {
        changedFields = {};
      }

      return {
        id: row.ID,
        employeeId: row.EMPLOYEEID,
        changeType: row.CHANGETYPE,
        changedFields,
        changedById: row.CHANGEDBYID,
        changedByName: row.CHANGEDBYNAME,
        changedAt: row.CHANGEDAT,
      };
    });

    const total = (countResult.rows || [])[0]?.TOTAL || 0;

    return NextResponse.json({
      data: history,
      pagination: {
        page,
        limit,
        total,
        totalPages: Math.ceil(total / limit),
      },
    });

  } catch (error) {
    console.error('GET /api/employees/[id]/history error:', error);
    return NextResponse.json(
      { message: 'Internal server error' },
      { status: 500 }
    );
  }
}
```

---

## 응답 예시

```json
{
  "data": [
    {
      "id": 5,
      "employeeId": 1,
      "changeType": "UPDATE",
      "changedFields": {
        "role": { "before": "USER", "after": "MANAGER" }
      },
      "changedById": 2,
      "changedByName": "관리자",
      "changedAt": "2026-01-28T10:30:00.000Z"
    },
    {
      "id": 4,
      "employeeId": 1,
      "changeType": "DEACTIVATE",
      "changedFields": {
        "isActive": { "before": true, "after": false }
      },
      "changedById": 2,
      "changedByName": "관리자",
      "changedAt": "2026-01-27T15:00:00.000Z"
    },
    {
      "id": 1,
      "employeeId": 1,
      "changeType": "CREATE",
      "changedFields": {
        "name": { "before": null, "after": "김영업" },
        "email": { "before": null, "after": "kim@company.com" }
      },
      "changedById": 2,
      "changedByName": "관리자",
      "changedAt": "2026-01-20T09:00:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 3,
    "totalPages": 1
  }
}
```

---

## Acceptance Criteria

- [ ] GET /api/employees/[id]/history - 이력 목록 조회 (페이지네이션)
- [ ] GET /api/employees/[id]/history - 최신순 정렬
- [ ] GET /api/employees/[id]/history - changeType 필터링
- [ ] GET /api/employees/[id]/history - changedFields JSON 파싱
- [ ] GET /api/employees/[id]/history - changedByName 포함
- [ ] GET /api/employees/[id]/history - ADMIN만 접근 가능

---

## 테스트 전략

### 단위 테스트

```typescript
describe('GET /api/employees/[id]/history', () => {
  it('should return history list sorted by date desc', async () => { });
  it('should filter by changeType', async () => { });
  it('should parse changedFields JSON', async () => { });
  it('should include changedByName', async () => { });
  it('should reject non-ADMIN users', async () => { });
});
```

---

## 완료 체크리스트

- [ ] GET /api/employees/[id]/history 구현
- [ ] JSON 파싱 처리
- [ ] TypeScript 빌드 성공
- [ ] 단위 테스트 통과
- [ ] 스테이징 서버 검증

---

**다음 문서**: 2091_10_TanStack_Query_Hooks.md
