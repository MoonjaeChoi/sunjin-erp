<!-- Generated: 2026-01-26 21:30:00 KST -->

# 로딩 및 오류 화면

**문서 번호**: 2071_19
**원본 PRD**: `docs/prd/2071_유지보수_고객_관리_prd_v2.md`
**구현 범위**: Next.js error.tsx, loading.tsx, not-found.tsx
**복잡도**: S (0.5-1일)
**의존성**: 2071_14~18 (모든 페이지/컴포넌트)

---

## 구현 목표

다음을 구현합니다:
- error.tsx: 계약 모듈 전역 에러 처리
- loading.tsx: 페이지 로딩 스켈레톤
- not-found.tsx: 404 페이지

---

## 구현 내용

### 1. src/app/(main)/maintenance/error.tsx

```typescript
// Generated: 2026-01-26 HH:MM:SS KST

'use client';

import { useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/button';
import { AlertCircle } from 'lucide-react';

/**
 * 계약 모듈 에러 바운더리
 */
export default function MaintenanceError({
  error,
  reset
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  useEffect(() => {
    console.error('Maintenance module error:', error);
  }, [error]);

  const router = useRouter();

  return (
    <div className="min-h-screen flex items-center justify-center bg-muted">
      <div className="max-w-md w-full text-center space-y-4">
        <AlertCircle className="w-12 h-12 mx-auto text-red-500" />
        <h1 className="text-2xl font-bold">문제가 발생했습니다</h1>
        <p className="text-muted-foreground">
          {error.message || '예상치 못한 오류가 발생했습니다'}
        </p>
        {error.digest && (
          <p className="text-xs text-muted-foreground break-all">
            Error ID: {error.digest}
          </p>
        )}
        <div className="flex gap-2 justify-center pt-4">
          <Button variant="outline" onClick={() => reset()}>
            다시 시도
          </Button>
          <Button onClick={() => router.push('/maintenance')}>
            목록으로 이동
          </Button>
        </div>
      </div>
    </div>
  );
}
```

### 2. src/app/(main)/maintenance/loading.tsx

```typescript
// Generated: 2026-01-26 HH:MM:SS KST

import { Skeleton } from '@/components/ui/skeleton';

/**
 * 계약 목록/상세 로딩 화면
 */
export default function MaintenanceLoading() {
  return (
    <div className="space-y-6">
      {/* 헤더 */}
      <div className="space-y-2">
        <Skeleton className="h-10 w-64" />
        <Skeleton className="h-4 w-40" />
      </div>

      {/* 통계 카드 */}
      <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
        {Array.from({ length: 4 }).map((_, i) => (
          <Skeleton key={i} className="h-24" />
        ))}
      </div>

      {/* 필터 */}
      <div className="space-y-4 p-4 border rounded-lg">
        <Skeleton className="h-10 w-full" />
        <div className="grid grid-cols-3 gap-4">
          {Array.from({ length: 3 }).map((_, i) => (
            <Skeleton key={i} className="h-10" />
          ))}
        </div>
      </div>

      {/* 테이블 */}
      <div className="space-y-2 border rounded-lg p-4">
        {Array.from({ length: 5 }).map((_, i) => (
          <Skeleton key={i} className="h-12" />
        ))}
      </div>
    </div>
  );
}
```

### 3. src/app/(main)/maintenance/[id]/loading.tsx

```typescript
// Generated: 2026-01-26 HH:MM:SS KST

import { Skeleton } from '@/components/ui/skeleton';

/**
 * 상세 페이지 로딩 화면
 */
export default function MaintenanceDetailLoading() {
  return (
    <div className="space-y-6">
      {/* 헤더 */}
      <div className="space-y-2">
        <Skeleton className="h-8 w-32" />
        <Skeleton className="h-10 w-64" />
        <Skeleton className="h-4 w-48" />
      </div>

      {/* 기본 정보 */}
      <div className="space-y-4 rounded-lg border p-6">
        <Skeleton className="h-6 w-24" />
        <div className="grid grid-cols-4 gap-4">
          {Array.from({ length: 8 }).map((_, i) => (
            <Skeleton key={i} className="h-16" />
          ))}
        </div>
      </div>

      {/* 첨부 파일 */}
      <div className="space-y-4 rounded-lg border p-6">
        <Skeleton className="h-6 w-24" />
        <Skeleton className="h-32" />
      </div>

      {/* 이력 */}
      <div className="space-y-4 rounded-lg border p-6">
        <Skeleton className="h-6 w-24" />
        <div className="space-y-2">
          {Array.from({ length: 3 }).map((_, i) => (
            <Skeleton key={i} className="h-12" />
          ))}
        </div>
      </div>
    </div>
  );
}
```

### 4. src/app/(main)/maintenance/[id]/not-found.tsx

```typescript
// Generated: 2026-01-26 HH:MM:SS KST

import { Button } from '@/components/ui/button';
import Link from 'next/link';

/**
 * 계약을 찾을 수 없음 (404)
 */
export default function MaintenanceNotFound() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="max-w-md w-full text-center space-y-4">
        <h1 className="text-4xl font-bold">404</h1>
        <p className="text-lg text-muted-foreground">
          찾을 수 없는 계약입니다
        </p>
        <p className="text-sm text-muted-foreground">
          계약이 삭제되었거나 존재하지 않을 수 있습니다
        </p>
        <Link href="/maintenance">
          <Button>목록으로 돌아가기</Button>
        </Link>
      </div>
    </div>
  );
}
```

### 5. src/app/(main)/maintenance/[[...slug]]/error.tsx (Optional)

```typescript
// Generated: 2026-01-26 HH:MM:SS KST

'use client';

import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/button';

/**
 * 전역 에러 바운더리
 */
export default function Error({
  error,
  reset
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  const router = useRouter();

  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="max-w-md w-full text-center space-y-4">
        <h1 className="text-2xl font-bold">오류 발생</h1>
        <p className="text-muted-foreground">{error.message}</p>
        <div className="flex gap-2 justify-center">
          <Button variant="outline" onClick={reset}>
            다시 시도
          </Button>
          <Button onClick={() => router.push('/maintenance')}>
            목록으로
          </Button>
        </div>
      </div>
    </div>
  );
}
```

---

## Acceptance Criteria

- [ ] error.tsx 구현 (전역 에러)
- [ ] loading.tsx 구현 (목록 페이지)
- [ ] [id]/loading.tsx 구현 (상세 페이지)
- [ ] [id]/not-found.tsx 구현 (404)
- [ ] 모든 화면이 Skeleton 컴포넌트 사용
- [ ] 에러 페이지에서 다시 시도/홈으로 버튼
- [ ] npm run build 성공

---

## 완료 체크리스트

- [ ] error.tsx 작성
- [ ] loading.tsx 작성
- [ ] [id]/loading.tsx 작성
- [ ] [id]/not-found.tsx 작성
- [ ] npm run build 성공

---

**다음 문서**: `2071_20_반응형_디자인.md`
