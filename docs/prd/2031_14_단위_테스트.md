<!-- Generated: 2026-01-25 05:10:00 KST -->

# 단위 테스트

**문서 번호**: 2031_14
**원본 PRD**: 2031_기술지원_관리_prd_v2.md
**PRD 참조**: 원본 PRD 전체 기능 범위
**구현 범위**: API Route + Hooks + Components 단위 테스트
**복잡도**: L
**의존성**: 2031_01 ~ 2031_13 (전체 구현 완료 후)

---

## 구현 목표

기술지원 모듈 전체에 대한 단위 테스트를 작성한다. API Route, Hooks, Components, 유틸리티 함수를 커버한다. 기존 2021 Task 테스트 패턴을 참조한다.

---

## 구현 내용

### 파일 구조

```
src/__tests__/
├── api/
│   ├── customers/
│   │   └── list.test.ts                    # GET /api/customers/list
│   └── support/
│       ├── route.test.ts                   # GET/POST /api/support
│       ├── detail.test.ts                  # GET/PUT/DELETE /api/support/[id]
│       └── attachment.test.ts              # POST/GET/DELETE attachment
├── hooks/
│   ├── support.test.ts                     # 기술지원 hooks
│   └── customers.test.ts                   # 고객사 목록 hook
├── components/features/support/
│   ├── TechSupportFilters.test.tsx         # 필터 컴포넌트
│   ├── TechSupportDataTable.test.tsx       # 데이터 테이블
│   ├── TechSupportCreateDialog.test.tsx    # 등록 Dialog
│   ├── TechSupportDetailDialog.test.tsx    # 상세/수정 Dialog
│   ├── TierBar.test.tsx                    # 진행 단계 UI
│   └── AttachmentUpload.test.tsx           # 파일 첨부
└── lib/
    └── file-utils.test.ts                  # 파일 유틸리티
```

### 테스트 개수 예상

| 카테고리 | 파일 수 | 테스트 수 (예상) |
|----------|---------|-----------------|
| API Routes | 4 | 35~40 |
| Hooks | 2 | 15~20 |
| Components | 6 | 50~60 |
| Utilities | 1 | 8~10 |
| **Total** | **13** | **108~130** |

### Mock 패턴 (기존 2021 참조)

```typescript
// API Route 테스트: next/server, next-auth, TypeORM mock
jest.mock('next/server', () => ({ NextResponse: { json: jest.fn() } }));
jest.mock('next-auth', () => ({ getServerSession: jest.fn() }));

// Hook 테스트: QueryClient wrapper
const createWrapper = () => {
  const queryClient = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return ({ children }) => <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>;
};

// Component 테스트: radix UI mock (portals)
jest.mock('@/components/ui/select', () => ({ /* simple HTML elements */ }));
jest.mock('@/components/ui/dialog', () => ({ /* direct render without portal */ }));
jest.mock('@/components/ui/command', () => ({ /* combobox mock */ }));
jest.mock('@/components/ui/alert-dialog', () => ({ /* alert dialog mock */ }));
```

### 핵심 테스트 시나리오

#### API Route Tests (35~40 tests)

**GET /api/customers/list (4 tests)**
- 인증 검증 (401)
- 목록 반환 (name ASC)
- soft-deleted 제외
- 필드 제한 (id, name, category)

**GET /api/support (12 tests)**
- 인증 검증
- 페이지네이션
- 날짜 범위 필터
- 날짜 범위 365일 제한
- customer_id 필터
- support_type 필터
- support_method 필터
- status 필터
- keyword 검색 (2자+)
- 정렬 (support_date, title, status)
- RBAC (USER 본인 건, ADMIN 전체)
- customer_name JOIN

**POST /api/support (5 tests)**
- 필수 필드 검증
- 성공 등록 (201)
- employee_id 자동 설정
- status = RECEIVED 고정
- 시간 범위 CHECK

**PUT /api/support/[id] (8 tests)**
- 필드 업데이트
- 상태 전이 검증 (RECEIVED→IN_PROGRESS 허용)
- 상태 전이 거부 (RECEIVED→COMPLETED)
- completed_at 자동 설정/제거
- ADMIN 모든 전이 허용
- 소유권 검증 (403)
- XSS sanitize

**DELETE /api/support/[id] (3 tests)**
- soft delete
- 소유권 검증
- ADMIN 삭제 허용

**Attachment API (8 tests)**
- 업로드 성공
- 파일 크기 초과 거부
- 확장자 거부
- MIME 불일치 거부
- 기존 파일 교체
- 다운로드
- 첨부 없을 때 다운로드 404
- 삭제

#### Hook Tests (15~20 tests)

- useTechSupportSearchQuery: fetch, enabled, error
- useTechSupportDetailQuery: fetch, enabled
- useCreateTechSupportMutation: POST, invalidation
- useUpdateTechSupportMutation: PUT, invalidation, error message
- useDeleteTechSupportMutation: DELETE, invalidation
- useUploadAttachmentMutation: FormData, invalidation
- useDeleteAttachmentMutation: DELETE, invalidation
- useCustomerListQuery: fetch, staleTime

#### Component Tests (50~60 tests)

**TechSupportFilters (9 tests)**
- 렌더링, debounce (keyword 500ms, date 300ms)
- keyword 2자 미만 미전송
- Combobox 동작
- Select 동작
- 초기화
- 외부 params 동기화

**TechSupportDataTable (12 tests)**
- Skeleton, 빈 상태, 데이터 표시
- Badge 색상
- 시간 포맷
- 정렬, 페이지네이션
- 행 클릭
- Placeholder overlay

**TechSupportCreateDialog (11 tests)**
- Dialog 열기/닫기
- 필수 필드 에러
- Combobox 고객사 선택
- 시간 변환
- 시간 검증
- 저장 성공/실패
- 폼 초기화

**TechSupportDetailDialog (10 tests)**
- 상세 표시
- 소유자 편집/비소유자 읽기 전용
- TierBar 연동
- 저장/삭제
- ADMIN 담당자 변경
- Skeleton

**TierBar (6 tests)**
- 현재 단계 표시
- 유효 전이 클릭
- 무효 전이 차단
- canEdit false 비활성
- ADMIN 전체 허용

**AttachmentUpload (12 tests)**
- 업로드 영역 / 파일 정보 표시
- 읽기 전용 모드
- 파일 크기/확장자 에러
- 교체 AlertDialog
- 삭제 AlertDialog
- 다운로드

#### Utility Tests (8~10 tests)

**file-utils (8 tests)**
- validateFile: 크기, 확장자, MIME
- sanitizeFilename: ../, /, <, >, &
- getMimeType: 각 확장자별

---

## Acceptance Criteria

- [ ] 전체 테스트 통과 (`npm run test`)
- [ ] 라인 커버리지 80%+
- [ ] 브랜치 커버리지 75%+
- [ ] API Route: RBAC 검증 테스트 포함
- [ ] API Route: 상태 전이 매트릭스 테스트 포함
- [ ] Components: 사용자 인터랙션 테스트 (userEvent)
- [ ] Hooks: 캐시 무효화 검증
- [ ] 기존 테스트 깨지지 않음

---

## 테스트 전략

### 실행 명령어

```bash
# 전체 테스트
npm run test

# 기술지원 관련만
npm run test -- --testPathPattern="support|customers|file-utils"

# 커버리지
npm run test -- --coverage --testPathPattern="support|customers|file-utils"
```

### Mock 설정 우선순위

1. next/server (NextRequest, NextResponse)
2. next-auth (getServerSession)
3. TypeORM (DataSource, Repository, QueryBuilder)
4. radix UI components (portal 관련)
5. sonner (toast)
6. next/navigation (useRouter, useSearchParams)
7. hooks (각 테스트 범위에 따라)

---

## 완료 체크리스트

- [ ] 13개 테스트 파일 생성
- [ ] 총 108~130개 테스트 작성
- [ ] `npm run test` 전체 통과
- [ ] 커버리지 80%+ (라인)
- [ ] 기존 2021 테스트 미영향 확인
- [ ] `npm run build` 성공

---

**이전 문서**: 2031_13_AttachmentUpload_컴포넌트.md
