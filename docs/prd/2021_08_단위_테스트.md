<!-- Generated: 2026-01-25 03:00:00 KST -->

# 단위 테스트

**문서 번호**: 2021_08
**원본 PRD**: 2021_업무_검색_prd_v2.md
**PRD 참조**: 원본 PRD 전체 기능 요구사항
**구현 범위**: API Route + Hooks + Components 단위 테스트
**복잡도**: M
**의존성**: 2021_01~07 (전체 구현 후)

---

## 구현 목표

업무 검색 기능의 전체 구현에 대한 단위 테스트를 작성한다. API Route Handler, TanStack Query Hooks, React Component의 핵심 동작을 검증한다.

---

## 구현 내용

### 파일 구조

```
src/__tests__/
├── api/tasks/
│   └── route.test.ts                          # API Route 테스트
├── hooks/tasks/
│   ├── useTaskSearchQuery.test.ts             # 검색 Hook 테스트
│   └── useUpdateTaskMutation.test.ts          # 수정 Mutation 테스트
└── components/features/tasks/
    ├── TaskSearchClient.test.tsx               # 메인 컨테이너 테스트
    ├── TaskSearchFilters.test.tsx              # 필터 테스트
    ├── TaskDataTable.test.tsx                  # 테이블 테스트
    └── TaskDetailDialog.test.tsx               # Dialog 테스트
```

### 구현 상세

#### 1. API Route 테스트 (`route.test.ts`)

**테스트 범위:**
- 인증 검증 (401)
- 하위 호환 모드 (page 미지정 → 전체 반환)
- 페이지네이션 모드 (page 지정 → paginated 반환)
- 날짜 범위 검증 (365일 초과 → 400)
- keyword 필터 (2자 미만 무시, 100자 초과 에러)
- work_type 필터
- 정렬 (sort_by + sort_order)
- RBAC (USER/MANAGER 본인만, ADMIN 전체)
- 잘못된 enum 값 검증

**Mock 대상:**
- `getServerSession` — 세션 mock
- `getDataSource` — TypeORM DataSource mock
- QueryBuilder mock (where, andWhere, orderBy, skip, take, getManyAndCount)

#### 2. Hook 테스트

**useTaskSearchQuery:**
- 유효한 params로 fetch 성공
- date_from/date_to 없으면 enabled=false
- 페이지 변경 시 이전 데이터 유지 (placeholderData)
- API 에러 시 error 상태

**useUpdateTaskMutation:**
- mutation 성공 시 캐시 무효화
- 403 에러 처리

**Mock 대상:**
- `fetch` — API 응답 mock
- `@tanstack/react-query` — QueryClient provider

#### 3. Component 테스트

**TaskSearchClient:**
- URL params 파싱
- 기본 날짜 범위 계산
- 하위 컴포넌트 렌더링

**TaskSearchFilters:**
- 필터 UI 렌더링
- debounce 동작 (jest.advanceTimersByTime)
- 초기화 버튼
- 날짜 범위 에러

**TaskDataTable:**
- 데이터 행 렌더링
- 정렬 헤더 클릭
- 페이지네이션
- 로딩/빈 상태
- 행 클릭

**TaskDetailDialog:**
- Dialog 열림/닫힘
- 폼 데이터 표시
- 수정 권한 분기
- 저장 동작

### 테스트 유틸리티

```typescript
// test-utils.tsx
import { render } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { SessionProvider } from 'next-auth/react';

function createTestQueryClient() {
  return new QueryClient({
    defaultOptions: {
      queries: { retry: false },
      mutations: { retry: false },
    },
  });
}

export function renderWithProviders(
  ui: React.ReactElement,
  { session = null, ...options } = {}
) {
  const queryClient = createTestQueryClient();
  return render(
    <SessionProvider session={session}>
      <QueryClientProvider client={queryClient}>
        {ui}
      </QueryClientProvider>
    </SessionProvider>,
    options
  );
}
```

### 커버리지 목표

| 대상 | 라인 커버리지 | 브랜치 커버리지 |
|------|-------------|---------------|
| API Route | 85%+ | 80%+ |
| Hooks | 80%+ | 75%+ |
| Components | 80%+ | 75%+ |
| 전체 | 80%+ | 75%+ |

---

## Acceptance Criteria

- [ ] 모든 테스트 파일 작성 완료
- [ ] `npm run test` 전체 통과
- [ ] 라인 커버리지 80%+ 달성
- [ ] 브랜치 커버리지 75%+ 달성
- [ ] API Route: RBAC, 페이지네이션, 필터링, 검증 테스트 완료
- [ ] Hooks: 데이터 fetching, 캐시 무효화 테스트 완료
- [ ] Components: 렌더링, 인터랙션, 상태 변경 테스트 완료
- [ ] debounce 타이머 테스트 (jest.useFakeTimers)
- [ ] 에러 케이스 테스트 포함

---

## 테스트 전략

### 실행 명령

```bash
# 전체 테스트
npm run test

# 특정 파일
npm run test -- --testPathPattern="tasks"

# 커버리지
npm run test -- --coverage --collectCoverageFrom="src/**/*.{ts,tsx}" --coveragePathIgnorePatterns="node_modules,__tests__"
```

### 테스트 우선순위

1. **Critical Path** (100% 커버리지):
   - RBAC 권한 분기 (API Route)
   - 날짜 범위 365일 검증
   - keyword SQL injection 방지 (parameterized 확인)
   - 수정 권한 (본인만)

2. **High Priority** (85%+):
   - 페이지네이션 동작
   - debounce 타이밍
   - 하위 호환 동작

3. **Normal** (80%+):
   - UI 렌더링
   - 빈 상태/로딩 상태
   - Badge 색상
   - 시간 포맷

---

## 완료 체크리스트

- [ ] `npm run test` 전체 통과
- [ ] 커버리지 80%+ 라인, 75%+ 브랜치
- [ ] Critical Path 100% 커버리지
- [ ] API Route 테스트 완료
- [ ] Hook 테스트 완료
- [ ] Component 테스트 완료
- [ ] Mock 설정 적절함 (과도한 mock 없음)
- [ ] 테스트 독립성 확보 (순서 무관하게 통과)

---

**끝 — 2021 업무 검색 구현 문서 시리즈 완료**
