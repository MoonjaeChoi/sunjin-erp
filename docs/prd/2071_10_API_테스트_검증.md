<!-- Generated: 2026-01-26 21:30:00 KST -->

# API 테스트 검증

**문서 번호**: 2071_10
**원본 PRD**: `docs/prd/2071_유지보수_고객_관리_prd_v2.md`
**구현 범위**: 모든 API Route Handler의 단위 테스트 작성 및 검증
**복잡도**: M (1-2일)
**의존성**: 2071_05~09 (모든 API 구현 완료)

---

## 구현 목표

모든 API 엔드포인트(6개 집합)에 대한 단위 테스트를 작성하여 다음을 검증합니다:
- 정상 요청 처리 (200, 201, 204)
- 입력 검증 오류 (400)
- 인증 오류 (401)
- 권한 오류 (403)
- 리소스 미존재 (404)
- 서버 오류 (500)
- RBAC 권한별 접근 제어

---

## 구현 내용

### 파일 구조

```
src/app/api/maintenance/__tests__/
├── route.test.ts                          # GET/POST /api/maintenance
├── [id].route.test.ts                     # GET/PUT/DELETE /api/maintenance/[id]
├── [id]/status.route.test.ts              # POST /api/maintenance/[id]/status
├── [id]/attachments.route.test.ts         # GET/POST /api/maintenance/[id]/attachments
├── [id]/attachments/[attachmentId]/route.test.ts  # GET/DELETE attachment
└── fixtures/
    └── maintenance.ts                     # Mock data fixtures
```

### 테스트 파일 상세

#### 1. route.test.ts - List & Create

```typescript
// Generated: 2026-01-26 HH:MM:SS KST

import { GET, POST } from '@/app/api/maintenance/route';
import { getServerSession } from 'next-auth';
import { getDataSource } from '@/lib/db';

// Mock 설정
jest.mock('next-auth');
jest.mock('@/lib/db');

describe('GET /api/maintenance', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    (getServerSession as jest.Mock).mockResolvedValue({
      user: { id: 1, name: 'kim', role: 'USER' }
    });
  });

  it('should return paginated list with default pagination', async () => {
    const mockData = [
      { id: 1, contract_name: '유지보수계약1', customer: { id: 1, name: 'ABC Corp' } }
    ];
    const mockCount = [{ TOTAL: 1 }];

    (getDataSource as jest.Mock).mockResolvedValue({
      createQueryRunner: jest.fn(() => ({
        query: jest.fn()
          .mockResolvedValueOnce(mockData)
          .mockResolvedValueOnce(mockCount),
        release: jest.fn()
      }))
    });

    const req = new Request('http://localhost/api/maintenance');
    const res = await GET(req);
    const data = await res.json();

    expect(res.status).toBe(200);
    expect(data).toHaveProperty('data');
    expect(data).toHaveProperty('pagination');
    expect(data.data).toHaveLength(1);
    expect(data.pagination.total).toBe(1);
  });

  it('should filter by status', async () => {
    (getDataSource as jest.Mock).mockResolvedValue({
      createQueryRunner: jest.fn(() => ({
        query: jest.fn()
          .mockResolvedValueOnce([])
          .mockResolvedValueOnce([{ TOTAL: 0 }]),
        release: jest.fn()
      }))
    });

    const req = new Request('http://localhost/api/maintenance?status=활성');
    const res = await GET(req);

    expect(res.status).toBe(200);
  });

  it('should return 401 when not authenticated', async () => {
    (getServerSession as jest.Mock).mockResolvedValue(null);

    const req = new Request('http://localhost/api/maintenance');
    const res = await GET(req);

    expect(res.status).toBe(401);
  });
});

describe('POST /api/maintenance', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    (getServerSession as jest.Mock).mockResolvedValue({
      user: { id: 1, name: 'kim', role: 'MANAGER' }
    });
  });

  it('should create contract with valid data', async () => {
    (getDataSource as jest.Mock).mockResolvedValue({
      createQueryRunner: jest.fn(() => ({
        startTransaction: jest.fn(),
        query: jest.fn()
          .mockResolvedValueOnce([{ id: 1 }])
          .mockResolvedValueOnce([]),
        commitTransaction: jest.fn(),
        rollbackTransaction: jest.fn(),
        release: jest.fn()
      }))
    });

    const body = {
      customer_id: 1,
      contract_name: '신규계약',
      contract_type: 'Maintenance',
      start_date: '2026-01-01',
      end_date: '2026-12-31',
      contract_amount: 5000000,
      assigned_employee_id: 1
    };

    const req = new Request('http://localhost/api/maintenance', {
      method: 'POST',
      body: JSON.stringify(body)
    });

    const res = await POST(req);
    expect(res.status).toBe(201);
  });

  it('should return 400 for missing required fields', async () => {
    const body = { contract_name: 'Test' }; // Missing customer_id, dates

    const req = new Request('http://localhost/api/maintenance', {
      method: 'POST',
      body: JSON.stringify(body)
    });

    const res = await POST(req);
    expect(res.status).toBe(400);
  });

  it('should return 403 for USER role (MANAGER+ required)', async () => {
    (getServerSession as jest.Mock).mockResolvedValue({
      user: { id: 1, name: 'kim', role: 'USER' }
    });

    const body = {
      customer_id: 1,
      contract_name: '신규계약',
      start_date: '2026-01-01',
      end_date: '2026-12-31',
      assigned_employee_id: 1
    };

    const req = new Request('http://localhost/api/maintenance', {
      method: 'POST',
      body: JSON.stringify(body)
    });

    const res = await POST(req);
    expect(res.status).toBe(403);
  });

  it('should return 400 for invalid date range (start > end)', async () => {
    const body = {
      customer_id: 1,
      contract_name: '신규계약',
      start_date: '2026-12-31',
      end_date: '2026-01-01',
      assigned_employee_id: 1
    };

    const req = new Request('http://localhost/api/maintenance', {
      method: 'POST',
      body: JSON.stringify(body)
    });

    const res = await POST(req);
    expect(res.status).toBe(400);
  });
});
```

#### 2. [id].route.test.ts - Detail, Update, Delete

```typescript
describe('GET /api/maintenance/[id]', () => {
  it('should return contract with relationships', async () => {
    // Test detail retrieval with customer, assignedEmployee, history
  });

  it('should return 404 for non-existent contract', async () => {
    // Test 404 handling
  });

  it('should exclude soft-deleted contracts', async () => {
    // Test soft delete filtering
  });
});

describe('PUT /api/maintenance/[id]', () => {
  it('should update contract and record history on end_date change', async () => {
    // Test update with history recording
  });

  it('should prevent customer_id modification', async () => {
    // Test immutable field
  });

  it('should return 403 for USER role (MANAGER+ required)', async () => {
    // Test RBAC
  });
});

describe('DELETE /api/maintenance/[id]', () => {
  it('should soft-delete contract and cascade to attachments/history', async () => {
    // Test cascade soft delete
  });

  it('should return 403 for non-ADMIN roles', async () => {
    // Test RBAC (ADMIN only)
  });
});
```

#### 3. [id]/status.route.test.ts

```typescript
describe('POST /api/maintenance/[id]/status', () => {
  it('should change status and record history', async () => {
    // Test status transition with history
  });

  it('should validate status transitions', async () => {
    // Test invalid transitions (e.g., 종료 → 갱신예정)
  });

  it('should require reason field', async () => {
    // Test reason validation
  });
});
```

#### 4. [id]/attachments.route.test.ts

```typescript
describe('POST /api/maintenance/[id]/attachments', () => {
  it('should upload file with validation', async () => {
    // Test file upload, MIME type validation, size limit
  });

  it('should reject non-PDF/DOCX files', async () => {
    // Test MIME type validation
  });

  it('should enforce 5-file limit', async () => {
    // Test file count limit
  });

  it('should return 403 for USER role (MANAGER+ required)', async () => {
    // Test RBAC
  });
});

describe('GET /api/maintenance/[id]/attachments', () => {
  it('should return paginated attachment list', async () => {
    // Test list with pagination
  });
});

describe('DELETE /api/maintenance/[id]/attachments/[attachmentId]', () => {
  it('should soft-delete attachment (ADMIN only)', async () => {
    // Test soft delete with RBAC
  });
});
```

### 테스트 커버리지 요구사항

**목표**: 라인 커버리지 85%+, 브랜치 커버리지 80%+

```bash
# 커버리지 리포트 생성
npm run test:coverage -- --testPathPattern=maintenance

# 예상 결과
TOTAL: 42 files, 1,200+ lines, 85%+ coverage
├── Lines: 1,020/1,200 (85%)
├── Branches: 480/600 (80%)
├── Functions: 95/110 (86%)
└── Statements: 1,050/1,200 (87.5%)
```

---

## Acceptance Criteria

- [ ] route.test.ts 완성 (GET/POST, 12+ test cases)
- [ ] [id].route.test.ts 완성 (GET/PUT/DELETE, 15+ test cases)
- [ ] status.route.test.ts 완성 (4+ test cases)
- [ ] attachments.route.test.ts 완성 (10+ test cases)
- [ ] 모든 RBAC 시나리오 테스트 (401, 403)
- [ ] 모든 입력 검증 시나리오 (400)
- [ ] 모든 데이터 무결성 시나리오 테스트
- [ ] 라인 커버리지 85% 이상
- [ ] 브랜치 커버리지 80% 이상
- [ ] npm run test 모두 통과

---

## 테스트 전략

### TypeScript & Lint

```bash
npm run build              # Next.js 빌드
npm run type-check         # TypeScript 검증
npm run lint               # ESLint
```

### 단위 테스트

```bash
# 개별 테스트 실행
npm test -- src/app/api/maintenance/__tests__/route.test.ts
npm test -- src/app/api/maintenance/__tests__/[id].route.test.ts
npm test -- src/app/api/maintenance/__tests__/[id]/status.route.test.ts
npm test -- src/app/api/maintenance/__tests__/[id]/attachments.route.test.ts

# 전체 API 테스트
npm test -- src/app/api/maintenance/__tests__/

# 커버리지 확인
npm run test:coverage -- --testPathPattern=maintenance
```

### Fixtures 파일 구성

```typescript
// src/app/api/maintenance/__tests__/fixtures/maintenance.ts

export const mockContracts = {
  active: {
    id: 1,
    contract_name: '유지보수계약1',
    customer: { id: 1, name: 'ABC Corp' },
    contract_status: '활성',
    start_date: '2026-01-01',
    end_date: '2026-12-31'
  }
};

export const mockAttachments = [
  { id: 1, file_name: 'contract.pdf', file_size: 102400 }
];

export const mockHistories = [
  {
    id: 1,
    change_type: '생성',
    changed_by: { id: 1, name: 'kim' },
    changed_at: '2026-01-01T00:00:00Z'
  }
];
```

---

## 완료 체크리스트

- [ ] Mock 설정 (getServerSession, getDataSource, createQueryRunner)
- [ ] route.test.ts 작성 (12+ cases)
- [ ] [id].route.test.ts 작성 (15+ cases)
- [ ] [id]/status.route.test.ts 작성 (4+ cases)
- [ ] [id]/attachments.route.test.ts 작성 (10+ cases)
- [ ] fixtures/maintenance.ts 작성
- [ ] npm run test 모두 통과
- [ ] 커버리지 85% 이상
- [ ] npm run build 성공
- [ ] npm run type-check 통과
- [ ] npm run lint 통과

---

**다음 문서**: `2071_11_TypeScript_타입_정의.md`
